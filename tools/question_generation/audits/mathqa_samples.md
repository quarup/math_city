# MathQA — sampled items per `(category, sub_bucket)` bucket

Auto-generated by [audit_mathqa.py](../audit_mathqa.py); 8 samples per `(category, sub_bucket)` bucket, seed=20260521. Re-run to refresh.

Pre-verdicts (in italics) are first-pass guesses; the audit pass in [mathqa.md](mathqa.md) checks each against the actual samples.

**Bucket key:** `(category, sub_bucket)`. `category` is MathQA's top-level field; `sub_bucket` is computed by inspecting which ops appear in `annotated_formula` (see the script's `classify_formula` docstring for the ladder).

**Item display:** problem text → correct option (decoded from a/b/c/d/e), plus the `annotated_formula` for shape inspection.

**Total items across all four splits:** 37,901

## Cross-tab — counts per `(category, sub_bucket)`

| category | arith_basic | arith_power | arith_sqrt | arith_log_fact | arith_gcd_lcm | geom_2d_encoded | geom_3d_encoded | gain_encoded | physics_encoded | prob | trig | misc_encoded | total |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| general | 14734 | 1137 | 398 | 299 | 255 | 30 | 5 | · | · | 3 | · | · | **16,861** |
| physics | 8133 | 200 | 155 | 33 | 8 | 197 | 61 | · | 173 | · | 2 | · | **8,962** |
| gain | 5936 | 342 | 103 | 4 | 1 | 49 | · | 38 | · | 5 | · | · | **6,478** |
| geometry | 1072 | 311 | 354 | 17 | · | 650 | 283 | · | · | 3 | 1 | · | **2,691** |
| other | 2022 | 77 | 63 | 55 | 34 | 26 | 2 | · | · | 35 | · | · | **2,314** |
| probability | 274 | 55 | · | 37 | · | · | 6 | · | · | 223 | · | · | **595** |

---

## Samples per bucket

### Category: `general`

#### `(general, arith_basic)` — *variety*  (14,734 items)

Mixed G6–G8 pre-algebra / ratio word problems. Variety candidate for `add_word_problems_within_100`, `add_sub_2step_word_problems`, `mult_div_word_2step`, `ratio_table`, `unit_rate_with_fractions`. Heavy filtering: drop items with K–5-out-of-range numbers, drop HS-level phrasings, drop items whose only correct option is 'none of these'.

- `if w is 40 percent less than x , x is 40 percent less than y , and z is 46 percent less than y , then z is greater than w by what percent of w ?`  →  `e` (`50 %`)
  - formula: `multiply(divide(subtract(multiply(subtract(const_1, divide(46, const_100)), const_100), subtract(subtract(const_100, 40), multiply(divide(40, const_100), subtract(const_100, 40)))), subtract(subtract(const_100, 40), multiply(divide(40, const_100), subtract(const_100, 40)))), const_100)`
- `find 96 × × 98`  →  `a` (`92 / 8`)
  - formula: `divide(96, 98)`
- `a and b together have rs . 1210 . if of a ' s amount is equal to of b ' s amount , how much amount does b have ?`  →  `a` (`484`)
  - formula: `multiply(divide(const_2, add(const_3, const_2)), 1210)`
- `if jake loses 32 pounds , he will weigh twice as much as his sister . together they now weigh 212 pounds . what is jake ’ s present weight , in pounds ?`  →  `d` (`152`)
  - formula: `add(multiply(divide(subtract(212, 32), const_3), const_2), 32)`
- `if 2 / w + 2 / x = 2 / y and wx = y , then the average ( arithmetic mean ) of w and x is`  →  `a` (`1 / 2`)
  - formula: `divide(2, add(2, 2))`
- `jaya two digits age when appended with the two digits age of amitabh the 4 digits number formed is a perfect square . what is the the sum of the 4 digits no . ?`  →  `b` (`7`)
  - formula: `add(add(const_1, const_2), 4)`
- `a sum of money lent out at s . i . amounts to rs . 780 after 2 years and to rs . 1020 after a further period of 5 years . the sum is ?`  →  `a` (`s . 684`)
  - formula: `subtract(780, multiply(divide(subtract(1020, 780), 5), 2))`
- `the cost of producing x tools by a company is given by c ( x ) = 600 x + 5500 ( in $ ) a ) what is the cost of 100 tools ?`  →  `a` (`65500 $`)
  - formula: `add(multiply(100, 600), 5500)`

#### `(general, arith_power)` — *variety*  (1,137 items)

Exponentiation items, K–8 reach only via G8 (8.EE.A.1) basic exponents. Most items here will be HS-level — filter required.

- `if n = 2 ^ 0.20 and n ^ b = 16 , b must equal`  →  `c` (`20`)
  - formula: `divide(log(16), log(power(2, 0.20)))`
- `the expenditure of an woman increase consistently by 30 % per year . if her present expenditure is rs . 1000 then what will her expenditure be after 3 years ?`  →  `b` (`2197`)
  - formula: `multiply(power(add(const_1, divide(30, const_100)), 3), 1000)`
- `if w = x ^ 4 + y ^ 4 = 100 , then the greatest possible value of x is between`  →  `b` (`3 and 6`)
  - formula: `add(multiply(floor(power(100, divide(const_1, 4))), const_10), multiply(floor(power(100, divide(const_1, 4))), const_2))`
- `if f ( x ) = 12 - x ^ 2 / 2 and f ( 2 k ) = 4 k , what is one possible value for k ?`  →  `c` (`4`)
  - formula: `add(divide(power(2, 2), 2), const_1)`
- `the sum of two numbers is 73 and their difference is 28 . find the difference between their squares .`  →  `c` (`2044`)
  - formula: `divide(subtract(28, power(73, const_2)), const_2)`
- `what is the decimal equivalent of ( 1 / 4 ) ^ 2 ?`  →  `b` (`0.0625`)
  - formula: `power(divide(1, 4), 2)`
- `jack takes a loan of $ 120000 with 12 % annual interest : the interest is paid once , at the end of the year . jill takes a loan of $ 120000 with 12 % annual interest , compounding monthly at the end of each month . at the end of one full year , compared to jack ' s loan inter…`  →  `d` (`$ 819.00`)
  - formula: `subtract(subtract(multiply(120000, power(add(const_1, divide(12, multiply(12, const_100))), 12)), 120000), divide(multiply(120000, 12), const_100))`
- `the temperature of a certain cup of coffee 15 minutes after it was poured was 120 degrees fahrenheit . if the temperature f of the coffee t minutes after it was poured can be determined by the formula f = 120 * 2 ^ ( - at ) + 60 , where f is in degrees fahrenheit and a is a co…`  →  `e` (`90`)
  - formula: `add(multiply(power(2, multiply(divide(60, 15), subtract(const_1, 2))), 120), 60)`

#### `(general, arith_sqrt)` — *variety*  (398 items)

Square-root items; K–8 only via G8 (8.EE.A.2, perfect squares). Filter for perfect-square radicand.

- `if n is an integer and 102 n ^ 2 is less than or equal to 8100 , what is the greatest possible value of n ?`  →  `b` (`8`)
  - formula: `floor(sqrt(divide(8100, 102)))`
- `a group of students decided to collect as many paise from each member of group as is the number of members . if the total collection amounts to rs . 98.01 , the number of the member is the group is :`  →  `d` (`99`)
  - formula: `sqrt(multiply(98.01, const_100))`
- `the product of two numbers is 192 and the sum of these two numbers is 28 . what is the smaller of these two numbers ?`  →  `b` (`12`)
  - formula: `sqrt(add(power(sqrt(subtract(28, multiply(const_2, 192))), const_2), multiply(const_4, 192)))`
- `15 lts are taken of from a container full of liquid a and replaced with liquid b . again 15 more lts of the mixture is taken and replaced with liquid b . after this process , if the container contains liquid a and b in the ratio 9 : 16 , what is the capacity of the container q ?`  →  `c` (`c : 37.5`)
  - formula: `divide(15, subtract(const_1, sqrt(divide(9, add(9, 16)))))`
- `tough and tricky questions : distance / rate . on a reconnaissance mission , a state - of - the - art nuclear powered submarine traveled 300 miles to reposition itself in the proximity of an aircraft carrier . this journey would have taken 1 hour less if the submarine had trav…`  →  `e` (`50`)
  - formula: `divide(subtract(sqrt(add(multiply(multiply(300, 10), const_4), power(10, const_2))), 10), const_2)`
- `the sum of the squares of three numbers is 138 , while the sum of their products taken two at a time is 131 . their sum is :`  →  `a` (`20`)
  - formula: `add(multiply(sqrt(divide(subtract(138, 131), const_2)), const_100), sqrt(subtract(138, divide(subtract(138, 131), const_2))))`
- `if x and y are integers such that ( x + 1 ) ^ 2 is less than or equal to 49 and ( y - 1 ) ^ 2 is less than 64 , what is the sum of the maximum possible value of xy and the minimum possible value of xy ?`  →  `a` (`- 16`)
  - formula: `add(sqrt(49), sqrt(64))`
- `what is the value of n if the sum of the consecutive odd intergers r from 1 to n equals 169 ?`  →  `b` (`25`)
  - formula: `add(subtract(multiply(sqrt(169), const_2), multiply(const_2, 1)), 1)`

#### `(general, arith_log_fact)` — *out_of_scope*  (299 items)

Logarithms and factorials are HS+ in CCSS.

- `what is the units digit of 23 ^ 2 * 17 ^ 2 * 39 ^ 2 ?`  →  `a` (`1`)
  - formula: `divide(add(multiply(factorial(23), factorial(2)), multiply(factorial(23), factorial(2))), 23)`
- `49 ã — 49 = 7 ^ ?`  →  `a` (`4`)
  - formula: `divide(add(log(49), log(49)), log(7))`
- `in a box of 10 pencils , a total of 2 are defective . if a customer buys 3 pencils selected at random from the box , what is the probability that neither pencils will be defective ?`  →  `b` (`7 / 15`)
  - formula: `divide(divide(factorial(subtract(10, 2)), multiply(factorial(subtract(subtract(10, 2), 3)), factorial(3))), divide(factorial(10), multiply(factorial(subtract(10, 3)), factorial(3))))`
- `8 ! / ( 8 - 2 ) ! = ?`  →  `c` (`56`)
  - formula: `divide(factorial(8), factorial(subtract(8, 2)))`
- `how many positive integers less than 100 are neither multiples of 4 or 5 .`  →  `b` (`b ) 60`)
  - formula: `divide(factorial(subtract(add(const_4, 4), const_1)), multiply(factorial(4), factorial(subtract(const_4, const_1))))`
- `a company that ships boxes to a total of 12 distribution centers uses color coding to identify each center . if either a single color or a pair of two different colors is chosen to represent each center and if each center is uniquely represented by that choice of one or two co…`  →  `b` (`5`)
  - formula: `subtract(divide(factorial(subtract(divide(12, const_2), const_1)), multiply(factorial(const_3), factorial(const_2))), subtract(divide(12, const_2), const_1))`
- `for any integer n greater than 1 , n * denotes the product of all the integers from 1 to n , inclusive . how many prime numbers q are there between 6 * + 2 and 6 * + 6 , inclusive ?`  →  `a` (`none`)
  - formula: `divide(add(factorial(6), 6), add(factorial(6), 6))`
- `find the least number of complete years in which a sum of money put out at 45 % compound interest will be more than double of itself ?`  →  `a` (`2`)
  - formula: `floor(add(divide(log(const_2), log(add(const_1, divide(45, const_100)))), const_1))`

#### `(general, arith_gcd_lcm)` — *variety*  (255 items)

G6 number theory (6.NS.B.4). Overlap with DeepMind gcd / lcm submodules — secondary source.

- `find the greatest number that exactly divides 35 , 91 and 840 ?`  →  `c` (`7`)
  - formula: `gcd(gcd(subtract(subtract(840, 91), 35), subtract(91, 35)), subtract(840, 91))`
- `n is the greatest number which divides 1305 , 4665 and 6905 and gives the same remainder in each case . what is the sum of the digits in n ?`  →  `c` (`4`)
  - formula: `add(add(divide(subtract(divide(subtract(divide(gcd(gcd(subtract(4665, 1305), subtract(6905, 4665)), subtract(6905, 1305)), const_10), reminder(divide(gcd(gcd(subtract(4665, 1305), subtract(6905, 4665)), subtract(6905, 1305)), const_10), const_10)), const_10), reminder(divide(subtract(divide(gcd(gcd(subtract(4665, 1305), subtract(6905, 4665)), subtract(6905, 1305)), const_10), reminder(divide(gcd(gcd(subtract(4665, 1305), subtract(6905, 4665)), subtract(6905, 1305)), const_10), const_10)), const_10), const_10)), const_10), reminder(divide(subtract(divide(gcd(gcd(subtract(4665, 1305), subtract(6905, 4665)), subtract(6905, 1305)), const_10), reminder(divide(gcd(gcd(subtract(4665, 1305), subtract(6905, 4665)), subtract(6905, 1305)), const_10), const_10)), const_10), const_10)), reminder(divide(gcd(gcd(subtract(4665, 1305), subtract(6905, 4665)), subtract(6905, 1305)), const_10), const_10))`
- `the least number which when divided by 5 , 6 , 7 and 8 leaves a remainder 3 , but when divided by 9 leaves no remainder , is`  →  `b` (`1683`)
  - formula: `multiply(lcm(lcm(lcm(5, 6), 7), 8), const_2)`
- `a drink vendor has 60 liters of maaza , 144 liters of pepsi and 368 liters of sprite . he wants to pack them in cans , so that each can contains the same number of liters of a drink , and does n ' t want to mix any two drinks in a can . what is the least number of cans required ?`  →  `c` (`143`)
  - formula: `add(divide(368, gcd(gcd(60, 144), 368)), add(divide(60, gcd(gcd(60, 144), 368)), divide(144, gcd(gcd(60, 144), 368))))`
- `how many numbers between 190 and 580 are divisible by 4,5 and 6 ?`  →  `a` (`6`)
  - formula: `subtract(floor(divide(580, lcm(lcm(const_4, add(const_4, const_1)), 6))), floor(divide(190, lcm(lcm(const_4, add(const_4, const_1)), 6))))`
- `find the lowest common multiple of 24 , 36 and 40 .`  →  `a` (`360`)
  - formula: `lcm(lcm(24, 36), 40)`
- `how many integers between 1 and 1400 are divisible by 25 , and 35 ?`  →  `e` (`8`)
  - formula: `divide(1400, lcm(25, 35))`
- `if 18888 – n is divisible by 11 , and 0 < n < 11 , what is n ?`  →  `a` (`1`)
  - formula: `gcd(18888, 11)`

#### `(general, geom_2d_encoded)` — *review*  (30 items)

Tiny — likely mis-categorized geometry items. Merge into geometry verdict during ingestion, or skip.

- `the largest 4 digit number exactly divisible by 90 is ?`  →  `e` (`9990`)
  - formula: `square_area(const_pi)`
- `the largest 4 digit number exactly divisible by 88 is ?`  →  `e` (`9944`)
  - formula: `square_area(const_pi)`
- `what is 982 * 982 ?`  →  `c` (`964,324`)
  - formula: `circle_area(divide(982, multiply(const_2, const_pi)))`
- `what is 5 + 7`  →  `d` (`12`)
  - formula: `circle_area(divide(5, multiply(const_2, const_pi)))`
- `the largest 4 digit number exactly divisible by 44 is ?`  →  `a` (`9988`)
  - formula: `square_area(const_pi)`
- `what is 992 * 992 ?`  →  `c` (`984,064`)
  - formula: `circle_area(divide(992, multiply(const_2, const_pi)))`
- `what will be the vulgar fraction of 0.60`  →  `b` (`3 / 5`)
  - formula: `divide(circle_area(divide(0.60, const_2)), const_2)`
- `the largest 4 digit number exactly divisible by 88 is :`  →  `a` (`9944`)
  - formula: `square_area(const_pi)`

#### `(general, geom_3d_encoded)` — *review*  (5 items)

Tiny — likely mis-categorized.

- `a hollow iron pipe is 21 cm long and its external diameter is 8 cm . if the thickness of the pipe is 1 cm and iron weighs 8 g / cm ^ 3 , then the weight of the pipe is :`  →  `b` (`3.696 kg`)
  - formula: `divide(multiply(subtract(volume_cylinder(divide(8, const_2), 21), volume_cylinder(subtract(divide(8, const_2), 1), 21)), 8), const_1000)`
- `if the radius of a cylinder is doubled and height 4 times , what is the new volume of the cylinder divided by the old one ?`  →  `e` (`16 .`)
  - formula: `divide(volume_cylinder(multiply(const_1, const_2), multiply(const_1, const_2)), volume_cylinder(const_1, const_1))`
- `a hollow iron pipe is 21 cm long and its external diameter is 8 cm . if the thickness of the pipe is 1 cm and iron weighs 8 g / cm ^ 3 , then the weight of the pipe is :`  →  `b` (`3.696 kg`)
  - formula: `divide(multiply(subtract(volume_cylinder(divide(8, const_2), 21), volume_cylinder(subtract(divide(8, const_2), 1), 21)), 8), const_1000)`
- `if the radius of a cylinder is doubled and height 5 times , what is the new volume of the cylinder divided by the old one ?`  →  `d` (`20 .`)
  - formula: `divide(volume_cylinder(multiply(const_1, const_2), multiply(const_1, const_2)), volume_cylinder(const_1, const_1))`
- `82 * 83 * 84 * . . 89 what is the digit in units place ?`  →  `a` (`0`)
  - formula: `divide(volume_rectangular_prism(82, 83, 84), volume_cube(89))`

#### `(general, prob)` — *review*  (3 items)

Tiny — likely mis-categorized.

- `a basket contains 5 apples , of which 1 is spoiled and the rest are good . if henry is to select 2 apples from the basket simultaneously and at random , what is the possibility that the 2 apples selected will include the spoiled apple ?`  →  `c` (`2 / 5`)
  - formula: `divide(choose(subtract(5, 1), 1), choose(5, 2))`
- `what is the product of first three whole no .`  →  `a` (`0`)
  - formula: `divide(const_2, choose(add(const_3, const_3), const_3))`
- `what is the probability of always having x + y > 0 , where y < 0`  →  `d` (`1 / 4`)
  - formula: `divide(const_2, choose(add(const_3, const_3), const_3))`

### Category: `physics`

#### `(physics, arith_basic)` — *skip*  (8,133 items)

Physics word problems (d=rt, work, stream/current, projectile). Even when the underlying math is K–8 the framing is HS physics-class style; mismatch with our K–8 audience. Filter cost too high for the slice that remains.

- `there is food for 760 men for 22 days . how many more men should join after two days so that the same food may last for 10 days more ?`  →  `b` (`760`)
  - formula: `subtract(divide(multiply(760, subtract(22, const_2)), 10), 760)`
- `a boatman goes 3 km against the current of the stream in 1 hour and goes 1 km along the current in 10 minutes . how long will it take to go 5 km in stationary water ?`  →  `b` (`1 hour 7 min`)
  - formula: `divide(5, divide(add(multiply(divide(1, 10), const_60), divide(3, 3)), const_2))`
- `a and b can do a piece of work in 6 days , b and c in 8 days , c and a in 12 days . how long will c take to do it ?`  →  `a` (`48 days`)
  - formula: `divide(multiply(6, const_3), subtract(divide(add(divide(multiply(6, const_3), 12), add(divide(multiply(6, const_3), 6), divide(multiply(6, const_3), 8))), const_2), divide(multiply(6, const_3), 6)))`
- `a man can do a piece of work in 4 days , but with the help of his daughter , he can do it in 3 days . in what time can his daughter do it alone ?`  →  `a` (`12`)
  - formula: `divide(multiply(4, 3), subtract(4, 3))`
- `there is a 55 cm line marked at each centimeter and an insect is placed at every centimeter . 9 frogs are trained to jump a constant distance . the first one jumps 2 cm in every leap , the second one jumps 3 cm and so on until the 9 th one jumps 10 cm in every leap and they ea…`  →  `e` (`12`)
  - formula: `add(10, 2)`
- `a man swims downstream 100 km and upstream 30 km taking 10 hours each time ; what is the speed of the current ?`  →  `b` (`3.5`)
  - formula: `divide(add(divide(30, 10), divide(100, 10)), const_2)`
- `a boat moves upstream at the rate of 1 km in 20 minutes and down stream 1 km in 9 minutes . then the speed of the current is :`  →  `e` (`1.85 kmph`)
  - formula: `divide(subtract(multiply(divide(1, 9), const_60), multiply(divide(1, 20), const_60)), const_2)`
- `a boat can travel with a speed of 5 km / hr in still water . if the speed of the stream is 5 km / hr , find the time taken by the boat to go 100 km downstream .`  →  `e` (`10 hrs`)
  - formula: `divide(100, add(5, 5))`

#### `(physics, arith_power)` — *out_of_scope*  (200 items)

Likely projectile / freefall — HS.

- `the sum of four consecutive even numbers is 84 . what would be the largest number ?`  →  `c` (`24`)
  - formula: `add(add(power(add(add(divide(subtract(subtract(84, const_10), const_2), const_4), const_2), const_2), const_2), power(add(add(add(divide(subtract(subtract(84, const_10), const_2), const_4), const_2), const_2), const_2), const_2)), add(power(divide(subtract(subtract(84, const_10), const_2), const_4), const_2), power(add(divide(subtract(subtract(84, const_10), const_2), const_4), const_2), const_2)))`
- `when a stone is dropped from a building 200 m high , its speed is proportional to the time elapsed after dropping . the distance traveled is proportional to the square of the time elapsed . after 1 second the speed of the train was 10 m / sec and it was 190 m above the ground …`  →  `b` (`137.5 m`)
  - formula: `subtract(200, multiply(power(divide(25, subtract(200, 190)), const_2), 10))`
- `the population of a bacteria colony doubles every day . if it was started 6 days ago with 2 bacteria and each bacteria lives for 12 days , how large is the colony today ?`  →  `e` (`128`)
  - formula: `subtract(power(2, add(6, const_1)), const_1)`
- `the sum of four consecutive even numbers is 36 . find the sum of the squares of these numbers ?`  →  `c` (`<missing-option:c>`)
  - formula: `add(add(power(add(add(divide(subtract(subtract(36, const_10), const_2), const_4), const_2), const_2), const_2), power(add(add(add(divide(subtract(subtract(36, const_10), const_2), const_4), const_2), const_2), const_2), const_2)), add(power(divide(subtract(subtract(36, const_10), const_2), const_4), const_2), power(add(divide(subtract(subtract(36, const_10), const_2), const_4), const_2), const_2)))`
- `one copy machine can make 30 copies a minute , and a second copy machine makes 15 copies a minute . if the two copiers work together , how long would it take them to make 900 copies ?`  →  `b` (`20 minutes`)
  - formula: `divide(power(15, const_3), add(30, 15))`
- `the sum of two consecutive number is 49 . which is the larger number ?`  →  `c` (`25`)
  - formula: `add(add(power(add(add(divide(subtract(subtract(49, const_10), const_2), const_4), const_2), const_2), const_2), power(add(add(add(divide(subtract(subtract(49, const_10), const_2), const_4), const_2), const_2), const_2), const_2)), add(power(divide(subtract(subtract(49, const_10), const_2), const_4), const_2), power(add(divide(subtract(subtract(49, const_10), const_2), const_4), const_2), const_2)))`
- `the sum of two consecutive integers is 31 . find the numbers .`  →  `c` (`15 , 16`)
  - formula: `add(add(power(add(add(divide(subtract(subtract(31, const_10), const_2), const_4), const_2), const_2), const_2), power(add(add(add(divide(subtract(subtract(31, const_10), const_2), const_4), const_2), const_2), const_2), const_2)), add(power(divide(subtract(subtract(31, const_10), const_2), const_4), const_2), power(add(divide(subtract(subtract(31, const_10), const_2), const_4), const_2), const_2)))`
- `the sum of four consecutive even numbers is 140 . what would be the smallest number ?`  →  `a` (`32`)
  - formula: `add(add(power(add(add(divide(subtract(subtract(140, const_10), const_2), const_4), const_2), const_2), const_2), power(add(add(add(divide(subtract(subtract(140, const_10), const_2), const_4), const_2), const_2), const_2), const_2)), add(power(divide(subtract(subtract(140, const_10), const_2), const_4), const_2), power(add(divide(subtract(subtract(140, const_10), const_2), const_4), const_2), const_2)))`

#### `(physics, arith_sqrt)` — *out_of_scope*  (155 items)

Likely projectile / freefall — HS.

- `two trains , one from howrah to patna and the other from patna to howrah , start simultaneously . after they meet , the trains reach their destinations after 10 hours and 8 hours respectively . the ratio of their speeds is`  →  `e` (`4 : 5`)
  - formula: `divide(sqrt(8), sqrt(10))`
- `two trains , one from howrah to patna and the other from patna to howrah , start simultaneously . after they meet , the trains reach their destinations after 9 hours and 16 hours respectively . the ratio of their speeds is ?`  →  `b` (`4 : 3`)
  - formula: `divide(sqrt(16), sqrt(9))`
- `two trains a and b starting from two points and travelling in opposite directions , reach their destinations 9 hours and 4 hours respectively after meeting each other . if the train a travels at 110 kmph , find the rate at which the train b runs .`  →  `b` (`165`)
  - formula: `multiply(110, sqrt(divide(9, 4)))`
- `the length of the longest rod that can fit in a cubical room of 4 m side is ?`  →  `b` (`<missing-option:b>`)
  - formula: `sqrt(add(power(multiply(sqrt(const_2), 4), const_2), power(4, const_2)))`
- `two trains , one from howrah to patna and the other from patna to howrah , start simultaneously . after they meet , the trains reach their destinations after 49 hours and 25 hours respectively . the ratio of their speeds is ?`  →  `b` (`5 : 7`)
  - formula: `divide(sqrt(25), sqrt(49))`
- `if a car had traveled 40 kmh faster than it actually did , the trip would have lasted 30 minutes less . if the car went exactly 120 km , at what speed did it travel ?`  →  `d` (`80 kmh`)
  - formula: `divide(subtract(sqrt(add(multiply(multiply(const_2, multiply(120, 40)), const_4), power(40, const_2))), 40), const_2)`
- `a truck travels 20 miles due north , 30 miles due east , and then 20 miles due north . how many miles is the truck from the starting point ?`  →  `c` (`50`)
  - formula: `add(sqrt(add(power(20, const_2), power(divide(30, const_2), const_2))), sqrt(add(power(20, const_2), power(divide(30, const_2), const_2))))`
- `an engine moves at the speed of 60 kmph without any coaches attached to it . speed of the train reduces at the rate that varies directly as the square root of the number of coaches attached . when 4 coaches are attached speed decreases to 48 kmph . what will be the speed of tr…`  →  `a` (`24`)
  - formula: `subtract(60, multiply(sqrt(36), divide(subtract(60, 48), sqrt(4))))`

#### `(physics, arith_log_fact)` — *review*  (33 items)

(no pre-classification — newly added bucket)

- `mariah has decided to hire three workers . to determine whom she will hire , she has selected a group of 15 candidates . she plans to have one working interview with 3 of the 15 candidates every day to see how well they work together . how many days will it take her to have wo…`  →  `d` (`360`)
  - formula: `subtract(subtract(subtract(divide(divide(divide(factorial(15), factorial(subtract(15, 3))), factorial(3)), const_2), 15), 15), const_10)`
- `jack and jill work at a hospital with 5 other workers . for an internal review , 2 of the 7 workers will be randomly chosen to be interviewed . what is the probability that jack and jill will both be chosen ?`  →  `c` (`1 / 21`)
  - formula: `inverse(divide(factorial(7), multiply(factorial(2), factorial(5))))`
- `joshua and jose work at an auto repair center with 4 other workers . for a survey on health care insurance , 2 of the 6 workers will be randomly chosen to be interviewed . what is the probability that joshua and jose will both be chosen ?`  →  `a` (`1 / 15`)
  - formula: `divide(divide(factorial(6), multiply(factorial(2), factorial(4))), factorial(6))`
- `mariah has decided to hire three workers . to determine whom she will hire , she has selected a group of 20 candidates . she plans to have one working interview with 3 of the 20 candidates every day to see how well they work together . how many days will it take her to have wo…`  →  `a` (`520`)
  - formula: `subtract(subtract(subtract(divide(divide(divide(factorial(20), factorial(subtract(20, 3))), factorial(3)), const_2), 20), 20), const_10)`
- `if 23 lines are drawn in a plane such that no two of them are parallel and no three are concurrent , then in how many points do they intersect ?`  →  `b` (`253`)
  - formula: `divide(factorial(23), multiply(factorial(subtract(23, const_2)), factorial(const_2)))`
- `jack and jill work at a hospital with 6 other workers . for an internal review , 2 of the 8 workers will be randomly chosen to be interviewed . what is the probability that jack and jill will both be chosen ?`  →  `d` (`1 / 28`)
  - formula: `inverse(divide(factorial(8), multiply(factorial(2), factorial(6))))`
- `if 22 lines are drawn in a plane such that no two of them are parallel and no three are concurrent , then in how many points do they intersect ?`  →  `c` (`231`)
  - formula: `divide(factorial(22), multiply(factorial(subtract(22, const_2)), factorial(const_2)))`
- `jack and jill work at a hospital with 2 other workers . for an internal review , 2 of the 4 workers will be randomly chosen to be interviewed . what is the probability that jack and jill will both be chosen ?`  →  `b` (`1 / 6`)
  - formula: `inverse(divide(factorial(4), multiply(factorial(2), factorial(2))))`

#### `(physics, arith_gcd_lcm)` — *review*  (8 items)

(no pre-classification — newly added bucket)

- `lamp a flashes every 6 seconds , lamp b flashes every 8 seconds , lamp c flashes every 10 seconds . at a certain instant of time all 3 lamps flash simultaneously . during the period of 6 minutes after that how many times will exactly two lamps flash ? ( please include any flas…`  →  `d` (`27`)
  - formula: `subtract(add(add(divide(multiply(6, const_60), lcm(6, 8)), divide(multiply(6, const_60), lcm(6, 10))), divide(multiply(6, const_60), lcm(8, 10))), multiply(divide(multiply(6, const_60), lcm(lcm(6, 8), 10)), 3))`
- `a sum of 25 was paid for a work which a can do in 32 days , b in 20 days , b and c in 12 days and d in 24 days . how much did c receive if all the 4 work together ?`  →  `b` (`16 ⁄ 3`)
  - formula: `multiply(divide(lcm(lcm(32, 20), 24), divide(const_1, subtract(divide(const_1, 12), divide(const_1, 20)))), divide(25, add(add(add(divide(lcm(lcm(32, 20), 24), 32), divide(lcm(lcm(32, 20), 24), 20)), divide(lcm(lcm(32, 20), 24), divide(const_1, subtract(divide(const_1, 12), divide(const_1, 20))))), divide(lcm(lcm(32, 20), 24), 24))))`
- `a takes twice as much time as b & c takes thrice as much time as b to finish a piece of work . working together they can finish the work in 18 days . the no . of days need for b to do the work alone is ?`  →  `c` (`33 days`)
  - formula: `divide(multiply(add(add(divide(lcm(const_2, const_3), const_1), divide(lcm(const_2, const_3), const_2)), divide(lcm(const_2, const_3), const_3)), 18), lcm(const_2, const_3))`
- `3 different containers contain 50 litres , 100 litres and 150 litres of mixture of milk and water respectively . what is the biggest measure can measure all the different quantities exactly ?`  →  `d` (`50 litres`)
  - formula: `gcd(gcd(50, 100), 150)`
- `6 bells commence tolling together and toll at intervals of 2 , 4 , 6 , 8 10 and 12 seconds respectively . in 30 minutes , how many times do they toll together ?`  →  `d` (`16`)
  - formula: `add(divide(multiply(30, const_60), lcm(lcm(12, 10), 8)), const_1)`
- `3 different containers contain 50 litres , 100 litres and 150 litres of mixture of milk and water respectively . what is the biggest measure can measure all the different quantities exactly ?`  →  `d` (`50 litres`)
  - formula: `gcd(gcd(50, 100), 150)`
- `6 lights begin to flash together at 12 : 00 noon and flash respectively at intervals of 3 , 4 , 5 , 6 , 7 , and 8 seconds . not including the flash at 12 : 00 noon , how many more times will all 6 lights flash together before 1 : 00 pm ( i . e . one hour later ) ?`  →  `a` (`4`)
  - formula: `floor(divide(multiply(const_1, const_3600), lcm(lcm(7, 8), lcm(lcm(3, 4), lcm(5, 6)))))`
- `lamp a flashes every 6 seconds , lamp b flashes every 8 seconds , lamp c flashes every 10 seconds . at a certain instant of time all 3 lamps flash simultaneously . during the period of 6 minutes after that how many times will exactly two lamps flash ? ( please include any flas…`  →  `d` (`27`)
  - formula: `subtract(add(add(divide(multiply(6, const_60), lcm(6, 8)), divide(multiply(6, const_60), lcm(6, 10))), divide(multiply(6, const_60), lcm(8, 10))), multiply(divide(multiply(6, const_60), lcm(lcm(6, 8), 10)), 3))`

#### `(physics, geom_2d_encoded)` — *review*  (197 items)

Tiny — likely mis-categorized.

- `find the cost of fencing around a circular field of diameter 34 m at the rate of rs . 2 a meter ?`  →  `b` (`212`)
  - formula: `multiply(circumface(divide(34, const_2)), 2)`
- `a rectangular plot measuring 90 metres by 50 metres is to be enclosed by wire fencing . if the poles of the fence are kept 10 metres apart , how many poles will be needed ?`  →  `a` (`28`)
  - formula: `divide(rectangle_perimeter(90, 50), 10)`
- `the sides of a rectangular field are in the ratio 3 : 4 . if the area of the field is 9408 sq . m , the cost of fencing the field @ 25 paise per metre is`  →  `d` (`<missing-option:d>`)
  - formula: `divide(multiply(rectangle_perimeter(multiply(3, sqrt(divide(9408, multiply(3, 4)))), multiply(4, sqrt(divide(9408, multiply(3, 4))))), 25), const_100)`
- `find the area of trapezium whose parallel sides are 20 cm and 18 cm long , and the distance between them is 15 cm`  →  `c` (`285 cm 2`)
  - formula: `quadrilateral_area(15, 18, 20)`
- `calculate the circumference of a circular field whose radius is 10 centimeters .`  →  `a` (`20 π cm`)
  - formula: `circumface(10)`
- `the ratio between the length and the breadth of a rectangular park is 2 : 3 if a man cycling along the boundary of the park at the speed of 10 km / hr completes one round in 18 min , then the area of the park ( in sq . m ) is ?`  →  `c` (`540000 m`)
  - formula: `rectangle_area(divide(divide(multiply(multiply(divide(10, multiply(const_10, multiply(const_3, const_2))), 18), const_1000), add(2, 3)), const_2), multiply(divide(divide(multiply(multiply(divide(10, multiply(const_10, multiply(const_3, const_2))), 18), const_1000), add(2, 3)), const_2), 3))`
- `calculate the circumference of a circular field whose radius is 5 centimeters .`  →  `e` (`<missing-option:e>`)
  - formula: `circumface(5)`
- `find the area of trapezium whose parallel sides are 20 cm and 18 cm long , and the distance between them is 14 cm .`  →  `e` (`266 cm 2`)
  - formula: `quadrilateral_area(14, 18, 20)`

#### `(physics, geom_3d_encoded)` — *review*  (61 items)

Tiny — likely mis-categorized.

- `find the expenditure on digging a well 14 m deep and of 3 m diameter at rs . 18 per cubic meter ?`  →  `c` (`<missing-option:c>`)
  - formula: `multiply(volume_cylinder(divide(3, const_2), 14), 18)`
- `three cubes of iron whose edges are 6 cm , 8 cm and 10 cm respectively are melted and formed into a single cube . the edge of the new cube formed is`  →  `b` (`<missing-option:b>`)
  - formula: `cube_edge_by_volume(add(volume_cube(10), add(volume_cube(6), volume_cube(8))))`
- `9 spheres of the same size are made from melting a solid cylinder of 8 cm diameter and 48 cm height . what is the diameter of each sphere ?`  →  `c` (`8 cm`)
  - formula: `multiply(divide(divide(divide(divide(multiply(divide(volume_cylinder(divide(8, const_2), 48), const_pi), const_3), const_4), 9), const_4), const_4), const_2)`
- `a hollow iron pipe is 42 cm long and its external diameter is 8 cm . if the thickness of the pipe is 1 cm and iron weighs 3 g / cm 3 , then the weight of the pipe is :`  →  `e` (`2.772 kg`)
  - formula: `divide(multiply(subtract(volume_cylinder(divide(8, const_2), 42), volume_cylinder(subtract(divide(8, const_2), const_1), 42)), 8), const_1000)`
- `a rectangular box measures internally 1.6 m long , 1 m broad and 60 cm deep . the number of cubical box each of edge 20 cm that can be packed inside the box is :`  →  `d` (`120`)
  - formula: `volume_rectangular_prism(divide(1.6, divide(20, const_100)), divide(1, divide(20, const_100)), divide(divide(60, const_100), divide(20, const_100)))`
- `the edges of a cuboid are 4 cm , 4 cm and 6 cm . find the volume of the cuboid ?`  →  `c` (`96`)
  - formula: `volume_rectangular_prism(4, 4, 6)`
- `three cubes of metal whose edges are 9 , 12 and 21 cm respectively , are melted and one new cube is made . find the edge of the new cube ?`  →  `b` (`22.7`)
  - formula: `power(add(add(volume_cube(9), volume_cube(12)), volume_cube(21)), const_0_33)`
- `how many cubes of 10 cm edge can be put in a cubical box of 1 m edge ?`  →  `d` (`1000`)
  - formula: `divide(volume_cube(1), volume_cube(divide(10, const_100)))`

#### `(physics, physics_encoded)` — *skip*  (173 items)

Explicit speed / work / stream ops. Same HS-physics framing.

- `working alone at its constant rate , machine a produces x boxes in 10 minutes and working alone at its constant rate , machine b produces 2 x boxes in 5 minutes . how many minutes does it take machines a and b , working simultaneously at their respective constant rates , to pr…`  →  `e` (`10 minutes`)
  - formula: `divide(multiply(5, 10), add(speed(10, 10), speed(multiply(2, 10), 5)))`
- `two trains of equal lengths take 12 sec and 15 sec respectively to cross a telegraph post . if the length of each train be 120 m , in what time will they cross other travelling in opposite direction ?`  →  `b` (`13`)
  - formula: `divide(multiply(120, const_2), add(speed(120, 15), speed(120, 12)))`
- `wilson can eat 25 marshmallows is 20 minutes . dylan can eat 25 in one hour . in how much time will the two eat 150 marshmallows ?`  →  `c` (`1 hour .`)
  - formula: `stream_speed(speed(25, 20), divide(25, multiply(const_1, const_60)))`
- `the length of the bridge , which a train 120 metres long and travelling at 45 km / hr can cross in 30 seconds , is ?`  →  `b` (`255 m`)
  - formula: `subtract(multiply(divide(multiply(45, speed(const_1000, const_1)), speed(const_3600, const_1)), 30), 120)`
- `the length of the bridge , which a train 500 metres long and travelling at 42 km / hr can cross in 60 seconds , is ?`  →  `b` (`200 m`)
  - formula: `subtract(multiply(divide(multiply(42, speed(const_1000, const_1)), speed(const_3600, const_1)), 60), 500)`
- `in one hour , a boat goes 15 km / hr along the stream and 9 km / hr against the stream . the speed of the boat in still water ( in km / hr ) is :`  →  `a` (`12 kmph`)
  - formula: `stream_speed(15, 9)`
- `the length of the bridge , which a train 110 meters long and travelling at 45 km / hr can cross in 30 seconds , is :`  →  `a` (`265`)
  - formula: `subtract(multiply(divide(multiply(45, speed(const_1000, const_1)), speed(const_3600, const_1)), 30), 110)`
- `a 300 m long train crosses a platform in 39 sec while it crosses a signal pole in 20 sec . what is the length of the platform ?`  →  `c` (`285 m`)
  - formula: `subtract(multiply(speed(300, 20), 39), 300)`

#### `(physics, trig)` — *review*  (2 items)

(no pre-classification — newly added bucket)

- `when 0.232323 . . . . . is converted into a fraction , then the result is`  →  `a` (`23 / 99`)
  - formula: `tangent(0.232323)`
- `a ladder learning against a wall makes an angle of 60 ° with the ground . if the length of the ladder is 19 m , find the distance of the foot of the ladder from the wall .`  →  `b` (`9.5 m`)
  - formula: `multiply(cosine(divide(multiply(60, divide(add(19, const_3), add(const_4, const_3))), multiply(const_60, const_3))), 19)`

### Category: `gain`

#### `(gain, arith_basic)` — *variety*  (5,936 items)

G7 percent applications (7.RP.A.3 — discount, markup, tax, simple interest, percent change). Strong fit. Filter for exact-decimal answers and drop items where 'none of these' is the correct choice.

- `a certain sky blue paint contains 10 percent blue pigment and 90 percent red pigment by weight . a certain green paint contains 70 percent blue pigment and 30 percent yellow pigment . when these paints are mixed to produce a brown paint , the brown paint contains 40 percent bl…`  →  `d` (`4.5`)
  - formula: `multiply(divide(90, multiply(const_100, const_2)), 10)`
- `a trader cheats both his supplier and customer by using faulty weights . when he buys from the supplier , he takes 10 % more than the indicated weight . when he sells to his customer , he gives the customer a weight such that 30 % of that is added to the weight , the weight cl…`  →  `b` (`43 %`)
  - formula: `subtract(multiply(divide(add(const_100, 30), const_100), add(const_100, 10)), const_100)`
- `at what rate percent per annum will a sum of money double in 6 years .`  →  `e` (`16.6 %`)
  - formula: `divide(const_100, 6)`
- `the percentage profit earned by selling an article for rs . 1920 is equal to the percentage loss incurred by selling the same article for rs . 1280 . at what price should the article be sold to make 25 % profit ?`  →  `a` (`rs . 2000`)
  - formula: `multiply(divide(add(const_100, 25), const_100), divide(add(1920, 1280), const_2))`
- `company p had 15 percent more employees in december than it had in january . if company p had 490 employees in december , how many employees did it have in january ?`  →  `b` (`426`)
  - formula: `divide(490, add(const_1, divide(15, const_100)))`
- `last year elaine spent 20 % of her annual earnings on rent . this year she earned 15 % more than last year and she spent 25 % of her annual earnings on rent . the amount she spent on rent this year is what percent of the amount spent on rent last year ?`  →  `b` (`143.75`)
  - formula: `multiply(divide(multiply(add(divide(15, const_100), const_1), 25), 20), const_100)`
- `a chemist mixes one liter of pure water with x liters of a 60 % salt solution , and the resulting mixture is a 20 % salt solution . what is the value of x ?`  →  `c` (`1 / 2`)
  - formula: `divide(20, subtract(60, 20))`
- `if 20 % of a number = 100 , then 120 % of that number will be ?`  →  `d` (`600`)
  - formula: `divide(multiply(120, 100), 20)`

#### `(gain, arith_power)` — *review*  (342 items)

Compound interest is HS; simple interest is G7. Need to inspect.

- `a chartered bank offers a 5 - year escalator guaranteed investment certificate . in successive years it pays annual interest rates of 4 % , 4.5 % , 5 % , 5.5 % , and 6 % , respectively , compounded at the end of each year . the bank also offers regular 5 - year gics paying a f…`  →  `a` (`1276.28`)
  - formula: `multiply(power(add(divide(5, const_100), const_1), 5), 1000)`
- `carl is facing very difficult financial times and can only pay the interest on a $ 30,000 loan he has taken . the bank charges him a quarterly compound rate of 5 % . what is the approximate interest he pays annually ?`  →  `d` (`$ 6000`)
  - formula: `subtract(multiply(multiply(const_100, const_100), power(add(const_1, divide(5, const_100)), const_4)), multiply(const_100, const_100))`
- `what annual payment will discharge a debt of rs . 1050 due in 2 years at the rate of 5 % compound interest ?`  →  `e` (`564.69`)
  - formula: `divide(multiply(power(add(divide(5, const_100), const_1), 2), 1050), 2)`
- `albert invested rs . 8000 in a scheme for 2 years at compound interest rate 5 % p . a . how much amount will albert get on maturity of the fixed deposit ?`  →  `e` (`8820`)
  - formula: `multiply(8000, power(add(const_1, divide(5, const_100)), 2))`
- `what is the compound interest on rs . 8500 at 7.5 % p . a . compounded half - yearly for 2 1 / 2 years .`  →  `b` (`1717.85`)
  - formula: `multiply(8500, subtract(power(divide(add(divide(7.5, const_2), const_100), const_100), multiply(2, const_2)), const_1))`
- `find the compound interest on $ 1200 for 5 years at 20 % p . a . if ci is component yearly ?`  →  `c` (`$ 1786`)
  - formula: `subtract(multiply(1200, power(add(const_1, divide(20, const_100)), 5)), 1200)`
- `there are two concentric circles with radii 8 and 6 . if the radius of the outer circle is increased by 25 % and the radius of the inner circle decreased by 50 % , by what percent does the area between the circles increase ?`  →  `a` (`225`)
  - formula: `multiply(divide(subtract(subtract(power(multiply(8, divide(add(const_100, 25), const_100)), const_2), power(multiply(6, divide(subtract(const_100, 50), const_100)), const_2)), subtract(power(8, const_2), power(6, const_2))), subtract(power(8, const_2), power(6, const_2))), const_100)`
- `the principal that amounts to rs . 4893 in 3 years at 6 1 / 4 % per annum c . i . compounded annually , is ?`  →  `c` (`s . 4085`)
  - formula: `divide(4893, power(add(const_1, divide(add(6, divide(1, 4)), const_100)), 3))`

#### `(gain, arith_sqrt)` — *out_of_scope*  (103 items)

Likely compound-interest reversing — HS.

- `√ 5 percent of 5 √ 5 =`  →  `b` (`0.25`)
  - formula: `divide(multiply(multiply(sqrt(5), sqrt(5)), 5), const_100)`
- `find the compound interest on rs . 10000 at 12 % rate of interest for 1 year , compounded half - yearly`  →  `b` (`rs . 1236`)
  - formula: `subtract(multiply(power(add(const_1, divide(divide(12, const_4), const_100)), const_3), multiply(multiply(multiply(const_4, const_4), const_100), sqrt(const_100))), multiply(multiply(multiply(const_4, const_4), const_100), sqrt(const_100)))`
- `alex takes a loan of $ 4,000 to buy a used truck at the rate of 6 % simple interest . calculate the annual interest to be paid for the loan amount .`  →  `c` (`240`)
  - formula: `divide(multiply(multiply(multiply(6, const_100), sqrt(const_100)), 6), const_100)`
- `a ladder 30 feet long is leaning against a wall that is perpendicular to level ground . the bottom of the ladder is 10 feet from the base of the wall . if the top of the ladder slips down 5 feet , how many feet will the bottom of the ladder slip ?`  →  `c` (`18.92`)
  - formula: `sqrt(subtract(power(30, const_2), power(subtract(sqrt(subtract(power(30, const_2), power(10, const_2))), 5), const_2)))`
- `the length of a rectangular floor is more than its breadth by 200 % . if rs . 150 is required to paint the floor at the rate of rs . 2 per sq m , then what would be the length of the floor ?`  →  `b` (`15`)
  - formula: `multiply(sqrt(divide(divide(150, 2), const_3)), const_3)`
- `reena took a loan of $ . 1200 with simple interest for as many years as the rate of interest . if she paid $ 972 as interest at the end of the loan period , what was the rate of interest ?`  →  `e` (`none of these`)
  - formula: `sqrt(divide(multiply(972, const_100), 1200))`
- `the length of a rectangular floor is more than its breadth by 200 % . if rs . 361 is required to paint the floor at the rate of rs . 3.00001 / sq m , what would be the length of the floor ?`  →  `b` (`19`)
  - formula: `multiply(sqrt(divide(divide(361, 3.00001), const_3)), const_3)`
- `the length of a rectangular floor is more than its breadth by 200 % . if rs . 450 is required to paint the floor at the rate of rs . 5 per sq m , then what would be the length of the floor ?`  →  `c` (`21.21`)
  - formula: `multiply(sqrt(divide(divide(450, 5), const_3)), const_3)`

#### `(gain, arith_log_fact)` — *review*  (4 items)

(no pre-classification — newly added bucket)

- `an investment compounds annually at an interest rate of 34.1 % what is the smallest investment period by which time the investment will more than triple in value ?`  →  `b` (`4`)
  - formula: `add(floor(divide(log(const_3), log(add(const_1, divide(34.1, const_100))))), const_1)`
- `an investment compounds annually at an interest rate of 30 % what is the smallest investment period by which time the investment will more than double in value ?`  →  `a` (`3`)
  - formula: `add(floor(divide(log(const_3), log(add(const_1, divide(30, const_100))))), const_1)`
- `an investment compounds annually at an interest rate of 33.34 % what is the smallest investment period by which time the investment will more than triple in value ?`  →  `e` (`4`)
  - formula: `add(floor(divide(log(const_3), log(add(const_1, divide(33.34, const_100))))), const_1)`
- `two pipes a and b fill at a certain rate . b is filled at 1020 , 4080 ( 10 in 1 hour , 20 in 2 hours , 40 in 3 hrs and so on ) . if 1 / 16 of b is filled in 17 hrs , what time it will take to get completely filled ?`  →  `b` (`21 hrs`)
  - formula: `add(17, log(16))`

#### `(gain, arith_gcd_lcm)` — *review*  (1 items)

(no pre-classification — newly added bucket)

- `a number with an interesting property : when i divide it by 2 , the remainder is 1 . when i divide it by 3 , the remainder is 2 . when i divide it by 4 , the remainder is 3 . when i divide it by 5 , the remainder is 4 . when i divide it by 6 , the remainder is 5 . when i divid…`  →  `d` (`2519`)
  - formula: `subtract(multiply(multiply(lcm(lcm(9, 10), 7), 2), 2), const_1)`

#### `(gain, geom_2d_encoded)` — *review*  (49 items)

Mis-categorized.

- `an error 10 % in excess is made while measuring the side of a square . now what is the percentage of error in the calculated area of the square ?`  →  `d` (`21 %`)
  - formula: `divide(multiply(subtract(square_area(add(const_100, 10)), square_area(const_100)), const_100), square_area(const_100))`
- `an error 5 % in excess is made while measuring the side of a square . the percentage of error in the calculated area of the square is`  →  `b` (`10.25 %`)
  - formula: `divide(multiply(subtract(square_area(add(const_100, 5)), square_area(const_100)), const_100), square_area(const_100))`
- `what is the value of â ˆ š 36 % ?`  →  `d` (`60 %`)
  - formula: `circle_area(divide(36, multiply(const_2, const_pi)))`
- `if each side of a square is increased by 6 % , find the percentage change in its area ?`  →  `d` (`12.36 %`)
  - formula: `square_area(add(const_1, divide(6, const_100)))`
- `an error 8 % in excess is made while measuring the side of a square . now what is the percentage of error in the calculated area of the square ?`  →  `b` (`16.64 %`)
  - formula: `divide(multiply(subtract(square_area(add(const_100, 8)), square_area(const_100)), const_100), square_area(const_100))`
- `an error 17 % in excess is made while measuring the side of a square . now what is the percentage of error in the calculated area of the square ?`  →  `b` (`36.89 %`)
  - formula: `divide(multiply(subtract(square_area(add(const_100, 17)), square_area(const_100)), const_100), square_area(const_100))`
- `what will be the fraction of 33.33 %`  →  `c` (`1 / 3`)
  - formula: `divide(circle_area(divide(33.33, const_2)), const_2)`
- `what is the value of √ 16 % ?`  →  `c` (`40 %`)
  - formula: `circle_area(divide(16, multiply(const_2, const_pi)))`

#### `(gain, gain_encoded)` — *variety*  (38 items)

Same scope — explicit gain ops. Tiny bucket.

- `what percent of 14 is 14 percent of 1 ?`  →  `e` (`1`)
  - formula: `p_after_gain(14, 14)`
- `a fruit seller had some apples . he sells 40 % apples and still has 420 apples . originally , he had :`  →  `e` (`700 apples`)
  - formula: `original_price_before_loss(40, 420)`
- `what percent of 13 is 13 percent of 1 ?`  →  `c` (`1`)
  - formula: `p_after_gain(13, 13)`
- `by selling a book for 250 , 20 % profit was earned . what is the cost price of the book ?`  →  `b` (`b ) 208`)
  - formula: `original_price_before_gain(20, 250)`
- `a fruit seller had some apples . he sells 35 % and still has 6500 apples . originally , he had ?`  →  `e` (`none`)
  - formula: `original_price_before_loss(35, 6500)`
- `a man sold 20 articles for $ 60 and gained 20 % . how many articles should he sell for $ 90 to incur a loss 10 % ?`  →  `b` (`41.2`)
  - formula: `divide(original_price_before_loss(10, 90), divide(original_price_before_gain(20, 60), 20))`
- `a store ’ s selling price of $ 2500 for a certain printer would yield a profit of 40 percent of the store ’ s cost for the printer . what selling price would yield a profit of 50 percent of the printer ’ s cost ?`  →  `d` (`$ 2478`)
  - formula: `multiply(add(const_1, divide(40, const_100)), original_price_before_gain(40, 2500))`
- `by selling a book for 260 , 20 % profit was earned . what is the cost price of the book ?`  →  `b` (`b ) 216`)
  - formula: `original_price_before_gain(20, 260)`

#### `(gain, prob)` — *review*  (5 items)

(no pre-classification — newly added bucket)

- `according to a recent student poll , 5 / 6 out of 30 members of the finance club are interested in a career in investment banking . if two students are chosen at random , what is the probability that at least one of them is interested in investment banking ?`  →  `d` (`28 / 29`)
  - formula: `divide(subtract(choose(30, const_2), choose(subtract(30, multiply(30, divide(5, 6))), const_2)), choose(30, const_2))`
- `according to a recent student poll , 4 / 6 out of 24 members of the finance club are interested in a career in investment banking . if two students are chosen at random , what is the probability that at least one of them is interested in investment banking ?`  →  `d` (`62 / 69`)
  - formula: `divide(subtract(choose(24, const_2), choose(subtract(24, multiply(24, divide(4, 6))), const_2)), choose(24, const_2))`
- `according to a recent student poll , 4 / 5 out of 20 members of the finance club are interested in a career in investment banking . if two students are chosen at random , what is the probability that at least one of them is interested in investment banking ?`  →  `c` (`92 / 95`)
  - formula: `divide(subtract(choose(20, const_2), choose(subtract(20, multiply(20, divide(4, 5))), const_2)), choose(20, const_2))`
- `according to a recent student poll , 4 / 5 out of 25 members of the finance club are interested in a career in investment banking . if two students are chosen at random , what is the probability that at least one of them is interested in investment banking ?`  →  `a` (`29 / 30`)
  - formula: `divide(subtract(choose(25, const_2), choose(subtract(25, multiply(25, divide(4, 5))), const_2)), choose(25, const_2))`
- `according to a recent student poll , 3 / 4 out of 20 members of the finance club are interested in a career in investment banking . if two students are chosen at random , what is the probability that at least one of them is interested in investment banking ?`  →  `c` (`18 / 19`)
  - formula: `divide(subtract(choose(20, const_2), choose(subtract(20, multiply(20, divide(3, 4))), const_2)), choose(20, const_2))`

### Category: `geometry`

#### `(geometry, arith_basic)` — *review*  (1,072 items)

65% of `geometry` items land here — formula is arithmetic-shaped, not encoded geometry. Some are valid (e.g. `area / length = width`); some are non-geometry mis-categorizations (MD-ES audit flagged a resistance-in-parallel item leaking in). Manual filter required.

- `what is the ratio between perimeters of two squares one having 3 times the diagonal then the other ?`  →  `d` (`<missing-option:d>`)
  - formula: `divide(3, divide(3, const_3))`
- `how many internal diagonals does a hexagon ( six sided polygon ) have ?`  →  `d` (`9`)
  - formula: `multiply(subtract(multiply(const_2, const_4), const_3), divide(multiply(const_2, const_4), const_2))`
- `the product of the squares of two positive integers is 400 . how many pairs of positive integers satisfy this condition ?`  →  `d` (`<missing-option:d>`)
  - formula: `subtract(add(const_2, const_3), const_2)`
- `a cube of edge 6 cm is cut into cubes each of edge 3 cm . the ratio of the total surface area of one of the small cubes to that of the large cube is equal to :`  →  `a` (`1 : 4`)
  - formula: `divide(const_4, const_100)`
- `a circle graph shows how the budget of a certain company was spent : 20 percent for transportation , 9 percent for research and development , 5 percent for utilities , 4 percent for equipment , 2 percent for supplies , and the remainder for salaries . if the area of each secto…`  →  `c` (`<missing-option:c>`)
  - formula: `subtract(const_360, divide(multiply(add(add(add(add(20, 9), 5), 4), 2), const_360), const_100))`
- `a cubical block of metal weighs 8 pounds . how much will another cube of the same metal weigh if its sides are twice as long ?`  →  `e` (`64`)
  - formula: `multiply(8, multiply(const_2, const_4))`
- `the points a ( 0 , 0 ) , b ( 0 , 4 a - 5 ) and c ( 2 a + 3 , 2 a + 6 ) form a triangle . if angle abc = 90 , what is the area of triangle abc ?`  →  `d` (`119`)
  - formula: `divide(multiply(subtract(multiply(divide(add(6, 2), const_2.0), 4), 5), add(multiply(divide(add(6, 5), 5), subtract(4, 5)), 3)), 5)`
- `if y is the smallest positive integer such that 1260 multiplied by y is the square of an integer , then y must be`  →  `b` (`35`)
  - formula: `multiply(add(const_2, const_3), const_2)`

#### `(geometry, arith_power)` — *variety*  (311 items)

Area = side² / volume = side³ shaped without the encoded op. Variety on existing area/volume generators.

- `there is a rectangular prism made of 1 in cubes that has been covered in tin foil . there are exactly 128 cubes that are not touching any tin foil on any of their sides . if the width of the figure created by these 128 cubes is twice the length and twice the height , what is t…`  →  `e` (`10`)
  - formula: `add(multiply(power(divide(128, multiply(1, multiply(1, const_2))), inverse(const_3)), multiply(1, const_2)), multiply(1, const_2))`
- `cubes with each side one inch long are glued together to form a larger cube . the larger cube ' s face is painted with red color and the entire assembly is taken apart . 25 small cubes are found with no paints on them . how many of unit cubes have at least one face that is pai…`  →  `e` (`100`)
  - formula: `subtract(power(add(const_1, add(const_1, const_3)), const_3), 25)`
- `the size of a television screen is given as the length of the screen ' s diagonal . if the screens were flat , then the area of a square 21 - inch screen would be how many square inches greater than the area of a square 17 - inch screen ?`  →  `e` (`76`)
  - formula: `divide(subtract(power(21, const_2), power(17, const_2)), const_2)`
- `the ratio of the volumes of two cubes is 27 : 64 . what is the ratio of their total surface areas ?`  →  `d` (`<missing-option:d>`)
  - formula: `power(divide(27, 64), divide(const_1, const_3))`
- `the diagonals of two squares are in the ratio of 3 : 7 then find the ratio of its areas ?`  →  `c` (`9 : 49`)
  - formula: `divide(const_4, power(7, 3))`
- `the radius of the two circular fields is in the ratio 3 : 10 the area of the first field is what percent less than the area of the second ?`  →  `c` (`91 %`)
  - formula: `subtract(power(10, const_2), power(3, const_2))`
- `if the radius of a circle that centers at the origin is 5 , how many points q on the circle have integer coordinates ?`  →  `c` (`12`)
  - formula: `subtract(subtract(multiply(5, 5), add(5, const_4)), power(const_2, const_2))`
- `if the radius of a circle is increased by 10 % then the area is increased by :`  →  `e` (`21 %`)
  - formula: `multiply(subtract(divide(const_100, const_100), power(subtract(divide(const_100, const_100), divide(10, const_100)), const_2)), const_100)`

#### `(geometry, arith_sqrt)` — *gap-fill*  (354 items)

Likely Pythagorean theorem (G8 — `pythagorean_theorem`, `pythagorean_apply_3d`) and edge-from-area items. Worth ingesting if filtering for perfect-square radicands is clean.

- `the roof of an apartment building is rectangular and its length is 4 times longer than its width . if the area of the roof is 900 feet squared , what is the difference between the length and the width of the roof ?`  →  `e` (`<missing-option:e>`)
  - formula: `subtract(multiply(sqrt(divide(900, 4)), const_4), sqrt(divide(900, 4)))`
- `if the height of a right cone c is 3 and the diameter of the base of c is 8 , what is the distance from the apex of c to any point on the circumference of the base of c ?`  →  `b` (`<missing-option:b>`)
  - formula: `sqrt(add(power(divide(8, const_2), const_2), power(3, const_2)))`
- `if the side length of square b is sqrt ( 3 ) times that of square a , the area of square b is how many times the area of square a ?`  →  `d` (`<missing-option:d>`)
  - formula: `power(sqrt(3), const_2)`
- `how many squares are there between 2011 to 2300 ? ? ? ?`  →  `a` (`['a ) 3', 'b ) 4', 'c ) 5', 'd ) 6', 'e ) 7']`)
  - formula: `subtract(floor(sqrt(2300)), floor(sqrt(2011)))`
- `what will be the cost of house to paint which area equal to 484 sq ft , if the price per foot of building is rs . 20`  →  `b` (`1760`)
  - formula: `multiply(multiply(sqrt(484), const_4), 20)`
- `you buy a piece of land with an area of â ˆ š 1024 , how long is one side of the land plot ?`  →  `e` (`32`)
  - formula: `sqrt(1024)`
- `a man walked diagonally across a square lot . approximately , what was the percent saved by not walking along the edges ?`  →  `b` (`<missing-option:b>`)
  - formula: `divide(multiply(subtract(add(power(const_1, const_2), power(const_1, const_2)), sqrt(add(power(const_1, const_2), power(const_1, const_2)))), const_100), add(power(const_1, const_2), power(const_1, const_2)))`
- `the area of a parallelogram is 147 sq m and its base is thrice the corresponding height . then the length of the base is ?`  →  `d` (`7`)
  - formula: `sqrt(divide(147, const_2))`

#### `(geometry, arith_log_fact)` — *review*  (17 items)

(no pre-classification — newly added bucket)

- `in a certain circle there are 9 points . what is the number of the triangles connecting 4 points of the 9 points ?`  →  `d` (`<missing-option:d>`)
  - formula: `divide(factorial(9), multiply(factorial(divide(const_10, const_2)), factorial(4)))`
- `12 points lie on a circle . how many cyclic quadrilaterals can be drawn by using these points ?`  →  `c` (`<missing-option:c>`)
  - formula: `divide(factorial(12), multiply(factorial(const_4), factorial(subtract(12, const_4))))`
- `in a certain circle there are 10 points . what is the number of the triangles connecting 2 points of the 10 points ?`  →  `b` (`45`)
  - formula: `divide(factorial(10), multiply(factorial(divide(const_10, const_2)), factorial(2)))`
- `in a certain circle there are 10 points . what is the number of the triangles connecting 4 points of the 10 points ?`  →  `e` (`210`)
  - formula: `divide(factorial(10), multiply(factorial(divide(const_10, const_2)), factorial(4)))`
- `in how many r ways can 5 people from a group of 6 people be seated around a circular table`  →  `e` (`<missing-option:e>`)
  - formula: `multiply(factorial(subtract(5, const_1)), 6)`
- `in a certain circle there are 10 points . what is the number of the triangles connecting 5 points of the 10 points ?`  →  `e` (`252`)
  - formula: `divide(factorial(10), multiply(factorial(divide(const_10, const_2)), factorial(5)))`
- `two tracks are parallel . the first track has 6 checkpoints and the second one has 10 checkpoints . in how many ways can the 6 checkpoints of first track be joined with the 10 checkpoints of the second to form a triangle ?`  →  `e` (`<missing-option:e>`)
  - formula: `add(multiply(divide(divide(factorial(10), factorial(subtract(10, const_2))), factorial(const_2)), divide(factorial(6), factorial(subtract(6, const_1)))), multiply(divide(factorial(10), factorial(subtract(const_10, const_1))), divide(divide(factorial(6), const_2), factorial(subtract(6, const_2)))))`
- `if a 1 mm thick paper is folded so that the area is halved at every fold , then what would be the thickness of the pile after 50 folds ?`  →  `d` (`<missing-option:d>`)
  - formula: `log(multiply(1, const_2))`

#### `(geometry, geom_2d_encoded)` — *variety*  (650 items)

Cleanest 2D-geometry slice. Strong fit for `area_rectangle_*`, `area_triangle`, `area_circle`, `circle_circumference`, `area_parallelogram_trapezoid`, `perimeter_polygon`. Variety on top of our existing G3–G7 area/perimeter generators.

- `a rectangular block 15 cm by 30 cm by 75 cm is cut into an exact number of equal cubes . find the least possible number of cubes ?`  →  `d` (`<missing-option:d>`)
  - formula: `divide(add(subtract(divide(rectangle_area(const_360, const_1000), const_10), multiply(const_1000, multiply(const_3, const_2))), add(multiply(const_3, const_1000), multiply(75, const_10))), divide(add(subtract(divide(rectangle_area(const_360, const_1000), const_10), multiply(const_1000, multiply(const_3, const_2))), add(multiply(const_3, const_1000), multiply(75, const_10))), const_10))`
- `the ratio of the radius of two circles is 1 : 3 , and then the ratio of their areas is ?`  →  `c` (`1 : 9`)
  - formula: `divide(circle_area(1), circle_area(3))`
- `the circumferences of two circles are 264 meters and 352 meters . find the difference between the areas of the larger and the smaller circles ?`  →  `c` (`<missing-option:c>`)
  - formula: `subtract(circle_area(divide(352, multiply(const_2, const_pi))), circle_area(divide(264, multiply(const_2, const_pi))))`
- `the side of a square is increased by 10 % then how much % does its area increases ?`  →  `a` (`21`)
  - formula: `divide(multiply(subtract(square_area(add(const_100, 10)), square_area(const_100)), const_100), square_area(const_100))`
- `marty ' s pizza shop guarantees that their pizzas all have at least 75 % of the surface area covered with toppings , with a crust of uniform width surrounding them . if you order their best seller – a circular pizza with a diameter of 16 inches – what is the maximum width you …`  →  `b` (`<missing-option:b>`)
  - formula: `divide(subtract(16, multiply(sqrt(divide(divide(multiply(circle_area(divide(16, const_2)), 75), const_100), const_pi)), const_2)), const_2)`
- `the parameter of a square is equal to the perimeter of a rectangle of length 18 cm and breadth 10 cm . find the circumference of a semicircle whose diameter is equal to the side of the square .`  →  `a` (`44.0`)
  - formula: `divide(circumface(divide(square_edge_by_perimeter(rectangle_perimeter(18, 10)), const_2)), const_2)`
- `the length and breadth of a rectangle is increased by 10 % and 30 % respectively . what is the increase in the area ?`  →  `a` (`43.0`)
  - formula: `divide(multiply(subtract(rectangle_area(add(10, const_100), add(30, const_100)), rectangle_area(const_100, const_100)), const_100), rectangle_area(const_100, const_100))`
- `a rectangular grass field is 95 m * 55 m , it has a path of 2.5 m wide all round it on the outside . find the area of the path and the cost of constructing it at rs . 2 per sq m ?`  →  `b` (`1550`)
  - formula: `multiply(subtract(rectangle_area(add(95, multiply(2.5, 2)), add(55, multiply(2.5, 2))), rectangle_area(95, 55)), 2)`

#### `(geometry, geom_3d_encoded)` — *variety*  (283 items)

3D geometry — fits `volume_rect_prism_formula`, `volume_unit_cubes`, `surface_area_from_net`, `volume_cylinder`, `volume_composite`. Variety on top of our G5–G6 generators.

- `five equal cubes , each of side 5 cm , are placed adjacent to each other . the volume of the new solid formed will be`  →  `b` (`<missing-option:b>`)
  - formula: `multiply(volume_cube(5), add(const_4, const_1))`
- `the sides of a cube measures 8 cm . if the surface area of the cube is the same as a ball , what is the radius of the ball ? round to the nearest whole number .`  →  `d` (`6`)
  - formula: `floor(sqrt(divide(divide(surface_cube(8), const_4), const_pi)))`
- `a rectangular metallic piece of paper that covers exactly the area of a cube . the length of the piece of paper is 48 inches and the width is 72 inches . what is the volume of the cube in cubic feet is 1 feet is 12 inches ?`  →  `a` (`a 8`)
  - formula: `divide(48, divide(divide(divide(volume_cube(divide(divide(sqrt(rectangle_area(48, 72)), const_3), const_2)), 12), 12), 12))`
- `the volumes of two cubes are in the ratio 27 : 125 , what shall be the ratio of their surface areas ?`  →  `c` (`<missing-option:c>`)
  - formula: `divide(surface_cube(divide(divide(27, const_3), const_3)), surface_cube(divide(125, divide(125, add(const_4, const_1)))))`
- `a larger cube has 8 cubic inch as a volume and in the cube there are 8 smaller cubes such that their volume is 1 cubic inch . what is the difference between the surface areas ’ sum of the 8 smaller cubes and the surface area of the larger cube , in square inch ?`  →  `a` (`['a ) 24', 'b ) 64', 'c ) 81', 'd ) 108', 'e ) 120']`)
  - formula: `subtract(multiply(surface_cube(cube_edge_by_volume(1)), 8), surface_cube(cube_edge_by_volume(8)))`
- `the radius of a cylindrical vessel is 7 cm and height is 3 cm . find the whole surface of the cylinder ?`  →  `c` (`<missing-option:c>`)
  - formula: `surface_cylinder(7, 3)`
- `the diameter of a cylindrical tin is 4 cm and height is 5 cm . find the volume of the cylinder ?`  →  `c` (`20`)
  - formula: `divide(volume_cylinder(divide(4, const_2), 5), const_pi)`
- `the radius of a cone is 49 m , slant height is 35 m . find the curved surface area ?`  →  `b` (`5390`)
  - formula: `volume_cone(49, 35)`

#### `(geometry, prob)` — *review*  (3 items)

(no pre-classification — newly added bucket)

- `here is 15 dots . if you select 3 dots randomly , what is the probability that 3 dots make a triangle ?`  →  `b` (`412 / 455`)
  - formula: `negate_prob(divide(add(multiply(choose(const_5, 3), const_3), multiply(choose(const_3, 3), const_5)), choose(15, 3)))`
- `what is the probability of getting equilateral triangles from the vertices of regular hexagon ?`  →  `a` (`1 / 10`)
  - formula: `divide(const_2, choose(add(const_3, const_3), const_3))`
- `a bag has 1 , 3 , 5 , 7 , 9,11 and 13 cm sticks . find the probability that they they will form a triangle if 3 sticks are drawn ?`  →  `a` (`13 / 35`)
  - formula: `divide(add(7, permutation(3, 7)), choose(7, 3))`

#### `(geometry, trig)` — *review*  (1 items)

(no pre-classification — newly added bucket)

- `a 45 ° - 45 ° - 90 ° right triangle has hypotenuse of length h . what is the area of the triangle r in terms of h ?`  →  `e` (`<missing-option:e>`)
  - formula: `inverse(sine(45))`

### Category: `other`

#### `(other, arith_basic)` — *review*  (2,022 items)

Catch-all. Need to sample to figure out what's actually here.

- `the ratio between the number of sheep and the number of horses at the stewar farm is 3 to 7 . if each of horse is fed 230 ounces of horse food per day and the farm needs a total 12880 ounces of horse food per day . what is number sheep in the form ? ?`  →  `a` (`24`)
  - formula: `multiply(divide(divide(12880, 230), 7), 3)`
- `a certain music store stocks 800 cellos and 600 violas . of these instruments , there are 100 cello - viola pairs , such that a cello and a viola were both made with wood from the same tree ( each tree can make at most one viola and one cello , so there are no pairs other than…`  →  `b` (`1 / 4,800`)
  - formula: `multiply(divide(100, 800), divide(const_1, 600))`
- `two family reunions are happening at the park avenue hotel , the oates reunion and the hall reunion . all 100 guests at the hotel attend at least one of the reunions . if 40 people attend the oates reunion and 70 people attend the hall reunion , how many people attend both reu…`  →  `c` (`10`)
  - formula: `subtract(add(40, 70), 100)`
- `three numbers are in the ratio 3 : 4 : 5 and their l . c . m is 2400 . their h . c . f is :`  →  `a` (`40`)
  - formula: `add(multiply(multiply(3, 5), const_100), multiply(4, 5))`
- `in what ratio should a variety of rice costing rs . 6.5 per kg be mixed with another variety of rice costing rs . 8.75 per kg to obtain a mixture costing rs . 7.50 per kg ?`  →  `c` (`5 / 4`)
  - formula: `divide(divide(subtract(8.75, 7.50), subtract(8.75, 6.5)), subtract(const_1, divide(subtract(8.75, 7.50), subtract(8.75, 6.5))))`
- `income and expenditure of a person are in the ratio 5 : 4 . if the income of the person is rs . 14000 , then find his savings ?`  →  `b` (`2800`)
  - formula: `subtract(14000, multiply(divide(4, 5), 14000))`
- `at a kennel with 80 dogs , 45 of the dogs wear tags and 40 wear flea collars . if 6 of the dogs wear both , then how many of the dogs wear neither a collar nor tags ?`  →  `a` (`1`)
  - formula: `subtract(80, subtract(add(45, 40), 6))`
- `a certain manufacturer of cake , muffin , and bread mixes has 100 buyers , of whom 50 purchases cake mix , 40 purchase muffin mix , and 19 purchase both cake mix and muffin mix . if a buyer is to be selected at random from the 100 buyers , what is the probability that the buye…`  →  `e` (`29 / 100`)
  - formula: `divide(subtract(100, subtract(add(50, 40), 19)), 100)`

#### `(other, arith_power)` — *review*  (77 items)

(no pre-classification — newly added bucket)

- `two vessels having volumes in the ratio 3 : 5 are filled with water and milk solutions . the ratio of milk and water in the two vessels are 5 : 1 and 6 : 4 respectively . if the contents of both the vessel are empties into a larger vessel , find the ratio of milk and water in …`  →  `b` (`11 : 5`)
  - formula: `power(5, 3)`
- `the duplicate ratio of 3 : 4 is ?`  →  `d` (`9 : 16`)
  - formula: `divide(power(3, const_2), power(4, const_2))`
- `the triplicate ratio of 1 : 4 is ?`  →  `d` (`1 : 64`)
  - formula: `divide(power(1, 4), power(4, 4))`
- `how many different positive integers are factors of 64 ?`  →  `d` (`16`)
  - formula: `add(power(const_2, const_2), const_2)`
- `if p and q are both odd prime numbers andp < q , then how many different positive integer factors does 2 pqhave ?`  →  `d` (`8`)
  - formula: `power(2, const_3)`
- `how many zeros does 65 ! end with ?`  →  `b` (`15`)
  - formula: `add(add(divide(65, add(const_4, const_1)), divide(subtract(65, add(const_4, const_1)), power(add(const_4, const_1), const_2))), divide(subtract(65, add(const_4, const_1)), power(add(const_4, const_1), const_3)))`
- `the triplicate ratio of 1 : 2 is ?`  →  `b` (`1 : 8`)
  - formula: `divide(power(1, 2), power(const_3.0, 2))`
- `if the ratio of the sum of the first 6 terms of a g . p . to the sum of the first 3 terms of the g . p . is 217 , what is the common ratio of the g . p ?`  →  `d` (`6`)
  - formula: `power(subtract(217, const_1), divide(const_1, 3))`

#### `(other, arith_sqrt)` — *review*  (63 items)

(no pre-classification — newly added bucket)

- `the ratio of 2 numbers is 3 : 4 and their h . c . f . is 4 . their l . c . m . is ?`  →  `c` (`48`)
  - formula: `sqrt(divide(4, add(power(4, 3), add(power(2, 3), power(3, const_2)))))`
- `a student committee on academic integrity has 90 ways to select a president and vice president from a group of candidates . the same person can not be both president and vice president . how many candidates are there ?`  →  `d` (`10`)
  - formula: `divide(add(const_1, sqrt(add(multiply(const_4, 90), power(negate(const_1), const_2)))), const_2)`
- `the ratio of 2 numbers is 3 : 7 and their h . c . f . is 12 . their l . c . m . is ?`  →  `c` (`252`)
  - formula: `sqrt(divide(12, add(power(7, 3), add(power(2, 3), power(3, 3)))))`
- `the ratio of three numbers is 2 : 3 : 4 and their sum is 180 . the second number of the three numbers is ?`  →  `b` (`60`)
  - formula: `sqrt(divide(180, add(power(4, 3), add(power(2, 3), power(3, 3)))))`
- `a bag contains 12 red marbles . if someone were to remove 2 marbles from the bag , one at a time , and replace the first marble after it was removed , the probability that neither marble would be red is 25 / 36 . how many marbles are in the bag ?`  →  `b` (`48`)
  - formula: `divide(12, subtract(const_1, sqrt(divide(25, 36))))`
- `the ratio of three numbers is 5 : 1 : 4 and their sum is 1000 . the last number of the three numbers is ?`  →  `d` (`400`)
  - formula: `sqrt(divide(1000, add(power(4, 1), add(power(5, 1), power(1, 1)))))`
- `a student committee on academic integrity has 72 ways to select a president and vice president from a group of candidates . the same person can not be both president and vice president . how many candidates are there ?`  →  `c` (`9`)
  - formula: `divide(add(const_1, sqrt(add(multiply(const_4, 72), power(negate(const_1), const_2)))), const_2)`
- `the ratio of 2 numbers is 2 : 8 and their h . c . f . is 40 . their l . c . m . is ?`  →  `d` (`320`)
  - formula: `sqrt(divide(40, add(power(8, 2), add(power(2, 2), power(2, 2)))))`

#### `(other, arith_log_fact)` — *review*  (55 items)

(no pre-classification — newly added bucket)

- `12 different biology books and 8 different chemistry books lie on a shelf . in how many ways can a student pick 2 books of each type ?`  →  `e` (`1848`)
  - formula: `multiply(divide(divide(factorial(12), factorial(subtract(12, 2))), 2), divide(divide(factorial(8), factorial(subtract(8, 2))), 2))`
- `what is the characteristic of the logarithm of 0.0000134 ?`  →  `b` (`- 5`)
  - formula: `floor(divide(log(0.0000134), log(const_10)))`
- `bag contains 7 green and 8 white balls . if two balls are drawn simultaneously , the probability that both are of the same colour is - .`  →  `a` (`7 / 15`)
  - formula: `divide(add(divide(factorial(7), multiply(factorial(subtract(7, const_2)), factorial(const_2))), divide(factorial(8), multiply(factorial(subtract(8, const_2)), factorial(const_2)))), divide(factorial(add(7, 8)), multiply(factorial(subtract(add(7, 8), const_2)), factorial(const_2))))`
- `7 log 7 ( 8 ) = ?`  →  `e` (`8`)
  - formula: `divide(log(multiply(7, 7)), log(const_10))`
- `find the probability in a group of 10 students sitting in a row , two of them always sit next to each other`  →  `d` (`0.2`)
  - formula: `divide(multiply(factorial(subtract(10, const_1)), factorial(const_2)), factorial(10))`
- `13 different biology books and 8 different chemistry books lie on a shelf . in how many ways can a student pick 2 books of each type ?`  →  `e` (`2184`)
  - formula: `multiply(divide(divide(factorial(13), factorial(subtract(13, 2))), 2), divide(divide(factorial(8), factorial(subtract(8, 2))), 2))`
- `the value of log 5 ( 1 / 125 ) is :`  →  `b` (`- 3`)
  - formula: `log(divide(log(subtract(1, multiply(add(const_4, const_1), const_1000))), log(add(const_4, const_1))))`
- `there are 8 executives , including the ceo and cfo , that are asked to form a small team of 5 members . however , the ceo and cfo may not both be assigned to the team . given this constraint , how many ways are there to form the team ?`  →  `c` (`36`)
  - formula: `subtract(factorial(subtract(8, 5)), add(multiply(5, const_3), const_3))`

#### `(other, arith_gcd_lcm)` — *review*  (34 items)

(no pre-classification — newly added bucket)

- `the ratio of numbers is 3 : 4 and their h . c . f is 4 . their l . c . m is ?`  →  `d` (`48`)
  - formula: `lcm(multiply(3, 4), multiply(4, 4))`
- `john distributes his pencil among his 4 friends rose , mary , ranjan , and rohit in the ratio 1 / 1 : 1 / 3 : 1 / 4 : 1 / 5 . what is the minimum no . of pencils that the person should have ?`  →  `d` (`107`)
  - formula: `add(add(add(divide(lcm(lcm(lcm(1, 1), 1), 3), 1), divide(lcm(lcm(lcm(1, 1), 1), 3), 1)), divide(lcm(lcm(lcm(1, 1), 1), 3), 1)), divide(lcm(lcm(lcm(1, 1), 1), 3), 3))`
- `john distributes his pencil among his 4 friends rose , mary , ranjan , and rohit in the ratio 1 / 5 : 1 / 3 : 1 / 4 : 1 / 5 . what is the minimum no . of pencils that the person should have ?`  →  `a` (`59`)
  - formula: `add(add(add(divide(lcm(lcm(lcm(5, 1), 1), 3), 5), divide(lcm(lcm(lcm(5, 1), 1), 3), 1)), divide(lcm(lcm(lcm(5, 1), 1), 3), 1)), divide(lcm(lcm(lcm(5, 1), 1), 3), 3))`
- `if 7 and 9 are factors of 60 n , what is the minimum value of n ?`  →  `e` (`21`)
  - formula: `lcm(7, 9)`
- `the ratio of numbers is 4 : 5 and their h . c . f is 4 . their l . c . m is :`  →  `a` (`80`)
  - formula: `lcm(multiply(4, 4), multiply(5, 4))`
- `john distributes his pencil among his 4 friends rose , mary , ranjan , and rohit in the ratio 1 / 2 : 1 / 3 : 1 / 3 : 1 / 5 . what is the minimum no . of pencils that the person should have ?`  →  `a` (`41`)
  - formula: `add(add(add(divide(lcm(lcm(lcm(2, 3), 3), 5), 2), divide(lcm(lcm(lcm(2, 3), 3), 5), 3)), divide(lcm(lcm(lcm(2, 3), 3), 5), 3)), divide(lcm(lcm(lcm(2, 3), 3), 5), 5))`
- `john distributes his pencil among his 4 friends rose , mary , ranjan , and rohit in the ratio 1 / 2 : 1 / 3 : 1 / 4 : 1 / 5 . what is the minimum no . of pencils that the person should have ?`  →  `d` (`77`)
  - formula: `add(add(add(divide(lcm(lcm(lcm(2, 1), 1), 3), 2), divide(lcm(lcm(lcm(2, 1), 1), 3), 1)), divide(lcm(lcm(lcm(2, 1), 1), 3), 1)), divide(lcm(lcm(lcm(2, 1), 1), 3), 3))`
- `if 7 and 6 are factors of 60 n , what is the minimum value of n ?`  →  `c` (`7`)
  - formula: `lcm(7, 6)`

#### `(other, geom_2d_encoded)` — *review*  (26 items)

(no pre-classification — newly added bucket)

- `the dimensions of a field are 10 m by 10 m . a pit 5 m long , 5 m wide and 3 m deep is dug in one corner of the field and the earth removed has been evenly spread over the remaining area of the field . what will be the rise in the height of field as a result of this operation ?`  →  `e` (`1 m`)
  - formula: `divide(multiply(10, 5), subtract(rectangle_area(10, 10), rectangle_area(5, 5)))`
- `what is the square root of 324 ?`  →  `a` (`a ) 18`)
  - formula: `circle_area(divide(324, multiply(const_2, const_pi)))`
- `what is the square root of 3,249 ?`  →  `e` (`e ) 57`)
  - formula: `circle_area(divide(3,249, multiply(const_2, const_pi)))`
- `what is the square root of 96,721 ?`  →  `e` (`311`)
  - formula: `circle_area(divide(96,721, multiply(const_2, const_pi)))`
- `what is the square root of 151,321 ?`  →  `b` (`389`)
  - formula: `circle_area(divide(151,321, multiply(const_2, const_pi)))`
- `what is the square root of 83,521 ?`  →  `c` (`289`)
  - formula: `circle_area(divide(83,521, multiply(const_2, const_pi)))`
- `what is the square root of 0.16`  →  `a` (`0.4`)
  - formula: `circle_area(divide(0.16, multiply(const_2, const_pi)))`
- `what is the factorial of 5 ?`  →  `c` (`120`)
  - formula: `circle_area(divide(5, multiply(const_2, const_pi)))`

#### `(other, geom_3d_encoded)` — *review*  (2 items)

(no pre-classification — newly added bucket)

- `two spheres of their radios in the ratio 4 : 3 . find its volumes ratio ?`  →  `d` (`<missing-option:d>`)
  - formula: `divide(volume_sphere(4), volume_sphere(3))`
- `two spheres of their radios in the ratio 4 : 3 . find its volumes ratio ?`  →  `d` (`<missing-option:d>`)
  - formula: `divide(volume_sphere(4), volume_sphere(3))`

#### `(other, prob)` — *review*  (35 items)

Probability items mis-categorized into 'other'?

- `each factor of 210 is inscribed on its own plastic ball , and all of the balls are placed in a jar . if a ball is randomly selected from the jar , what is the probability that the ball is inscribed with a multiple of 40 ?`  →  `a` (`1 / 16`)
  - formula: `divide(divide(choose(40, const_1), 40), power(const_3, const_2))`
- `a bag holds 2 red marbles and 3 green marbles . if you removed two randomly selected marbles from the bag , without replacement , what is the probability that both would be red ?`  →  `a` (`1 / 10`)
  - formula: `divide(choose(2, 2), choose(add(2, 3), const_2))`
- `a bag holds 3 red marbles and 3 green marbles . if you removed two randomly selected marbles from the bag , without replacement , what is the probability that both would be red ?`  →  `b` (`1 / 5`)
  - formula: `divide(choose(3, 3), choose(add(const_2.0, 3), const_2))`
- `a bag contains 3 red , 4 blue and 4 green balls . if 2 ballsare picked at random , what is the probability that both are red ?`  →  `c` (`3 / 55`)
  - formula: `divide(choose(3, 2), choose(add(add(3, 4), 4), 2))`
- `each factor of 200 is inscribed on its own plastic ball , and all of the balls are placed in a jar . if a ball is randomly selected from the jar , what is the probability that the ball is inscribed with a multiple of 40 ?`  →  `a` (`1 / 6`)
  - formula: `divide(divide(choose(40, const_1), 40), power(const_3, const_2))`
- `a bag contains 3 red , 2 blue and 3 green balls . if 2 ballsare picked at random , what is the probability that both are red ?`  →  `c` (`3 / 28`)
  - formula: `divide(choose(3, 2), choose(add(add(3, 2), 3), 2))`
- `each factor of 220 is inscribed on its own plastic ball , and all of the balls are placed in a jar . if a ball is randomly selected from the jar , what is the probability that the ball is inscribed with a multiple of 55 ?`  →  `e` (`1 / 4`)
  - formula: `divide(divide(choose(55, const_1), 55), power(const_3, const_2))`
- `each factor of 230 is inscribed on its own plastic ball , and all of the balls are placed in a jar . if a ball is randomly selected from the jar , what is the probability that the ball is inscribed with a multiple of 46 ?`  →  `e` (`1 / 4`)
  - formula: `divide(divide(choose(46, const_1), 46), power(const_3, const_2))`

### Category: `probability`

#### `(probability, arith_basic)` — *variety*  (274 items)

Probability items whose formula is raw arithmetic. Same scope.

- `6 animals of a circus has to be placed inside in 6 cages one in each cage . if 4 of the cage are too small for 6 of the animal then find the number of ways of caging the animal .`  →  `a` (`a . 240`)
  - formula: `multiply(multiply(6, 4), const_10)`
- `there are 15 balls in a jar . you take out 3 blue balls without putting them back inside , and now the probability of pulling out a blue ball is 1 / 3 . how many blue balls were there in the beginning ?`  →  `d` (`7 .`)
  - formula: `add(divide(subtract(15, 3), 3), 3)`
- `the maitre ' d at an expensive manhattan restaurant has noticed that 60 % of the couples order dessert and coffee . however , 20 % of the couples who order dessert do n ' t order coffee . what is the probability q that the next couple the maitre ' d seats will not order dessert ?`  →  `b` (`25 %`)
  - formula: `multiply(subtract(const_1, divide(divide(60, multiply(multiply(const_2, const_5), multiply(const_2, const_5))), subtract(const_1, divide(20, multiply(multiply(const_2, const_5), multiply(const_2, const_5)))))), multiply(multiply(const_2, const_5), multiply(const_2, const_5)))`
- `a box contains 3 red balls and 3 black balls . one by one , every ball is selected at random without replacement . what is the probability that the fourth ball selected is black ?`  →  `d` (`2`)
  - formula: `divide(add(3, 3), 3)`
- `a man speaks truth 3 out of 4 times . he throws a die and reports it to be a 6 . what is the probability of it being a 6 ?`  →  `d` (`3 / 8`)
  - formula: `divide(multiply(divide(3, 4), divide(const_1, 6)), add(multiply(divide(3, 4), divide(const_1, 6)), multiply(divide(const_1, const_4), divide(const_5, 6))))`
- `a box contains 4 red balls and 4 black balls . one by one , every ball is selected at random without replacement . what is the probability that the fourth ball selected is black ?`  →  `e` (`2`)
  - formula: `divide(add(4, 4), 4)`
- `a certain junior class has 100 students and a certain senior class has 80 students . among these students , there are 6 siblings pairs each consisting of 1 junior and 1 senior . if 1 student is to be selected at random from each class , what is the probability that the 2 stude…`  →  `d` (`3 / 4000`)
  - formula: `divide(1, const_3)`
- `4 dice are thrown simultaneously on the board . find the probability which show the same face ?`  →  `c` (`1 / 216`)
  - formula: `multiply(divide(const_3, add(const_3, const_3)), divide(const_3, add(const_3, const_3)))`

#### `(probability, arith_power)` — *variety*  (55 items)

Likely 'probability of event N times in a row' — independent-events compound probability. G7.

- `if a coin is flipped , the probability that the coin will land heads is 1 / 2 . if the coin is flipped 5 times , what is the probability that it will land tails up on the first 4 flips and not on the last flip ?`  →  `b` (`1 / 32`)
  - formula: `divide(const_1, power(2, 5))`
- `a life insurance company insured 25,000 young boys , 14,000 young girls and 16,000 young adults . the probability of death within 10 years of a young boy , young girl and a young adult are 0.02 , 0.03 and 0.15 respectively . one insured person dies . what is the probability th…`  →  `b` (`25 / 166`)
  - formula: `divide(multiply(0.02, power(const_5, const_2)), add(add(multiply(0.02, power(const_5, const_2)), multiply(0.03, multiply(add(const_3, const_4), const_2))), multiply(power(const_4, const_2), 0.15)))`
- `if a certain coin is flipped , the probability that the coin will land heads is 1 / 2 . if the coin is flipped 5 times , what is the probability that it will land heads up on the first 4 flips but not on the last flip ?`  →  `e` (`1 / 32`)
  - formula: `power(divide(1, 2), 5)`
- `when tossed , a certain coin has equal probability of landing on either side . if the coin is tossed 5 times , what is the probability that it will land on either side all 5 times ?`  →  `d` (`1 / 16`)
  - formula: `divide(const_1, power(const_2, const_4.0))`
- `when tossed , a certain coin has equal probability of landing on either side . if the coin is tossed 3 times , what is the probability that it will land on the same side each time ?`  →  `b` (`1 / 4`)
  - formula: `divide(const_1, power(const_2, 3))`
- `if a certain coin is flipped , the probability that the coin will land heads is 1 / 2 . if the coin is flipped 5 times , what is the probability that it will land heads up on the first 2 flips but not on the last 3 flips ?`  →  `e` (`1 / 32`)
  - formula: `power(divide(1, 2), 5)`
- `if a certain coin is flipped , the probability that the coin will land heads is 1 / 2 . if the coin is flipped 6 times , what is the probability that it will land heads up on the first 3 flips but not on the last 3 flips ?`  →  `d` (`1 / 64`)
  - formula: `power(divide(1, 2), 6)`
- `a string of 30 light bulbs is wired in such a way that if any individual light bulb fails , the entire string fails . if for each individual light bulb the probability of failing during time period t is 0.1 , what is the probability that the string of light bulbs will fail dur…`  →  `e` (`1 - ( 0.90 ) ^ 30`)
  - formula: `subtract(const_1, power(subtract(const_1, 0.1), 30))`

#### `(probability, arith_log_fact)` — *out_of_scope*  (37 items)

Factorial-only probability is HS combinatorics depth.

- `a bag contains 6 black and 3 white balls . one ball is drawn at random . what is the probability that the ball drawn is white ?`  →  `b` (`1 / 3`)
  - formula: `divide(add(divide(divide(factorial(3), factorial(subtract(3, const_2))), factorial(const_2)), divide(divide(factorial(6), factorial(subtract(6, const_2))), factorial(const_2))), divide(divide(factorial(add(6, 3)), factorial(subtract(add(6, 3), const_2))), factorial(const_2)))`
- `a , b , c , d , e , f , g , h sitting in a row what is the probability that a , b , d are sitting together ?`  →  `b` (`3 / 28`)
  - formula: `divide(multiply(factorial(add(subtract(add(const_4, const_4), const_3), const_1)), factorial(const_3)), factorial(add(const_4, const_4)))`
- `at a conference table , 6 employees gather around a table . one of the employees is the manager and he sits at the head of the table . two of the employees sit on either side of the table . how many different seating arrangements can be made with these 6 employees ?`  →  `d` (`120`)
  - formula: `factorial(subtract(6, const_1))`
- `5 letters have to be put in 5 different envelopes numbered 1 through 5 such that each of the letters go into only 1 envelope . the letter is said to be put in correct position if for example letter 1 goes into envelope 1 . now what is the probability that all letters be put in…`  →  `d` (`11 / 30`)
  - formula: `subtract(add(subtract(inverse(factorial(const_2)), inverse(factorial(const_3))), inverse(factorial(const_4))), inverse(factorial(5)))`
- `a , b , c , d , e , f , g , h sitting in a row what is the probability that a , b , c sitting together ?`  →  `c` (`3 / 28`)
  - formula: `divide(multiply(factorial(add(subtract(add(const_4, const_4), const_3), const_1)), factorial(const_3)), factorial(add(const_4, const_4)))`
- `in how many ways 6 boys and 4 girls can be seated in a row so that they are alternative ?`  →  `c` (`17280`)
  - formula: `multiply(factorial(6), factorial(4))`
- `in how many ways 4 boys and 1 girls can be seated in a row so that they are alternate .`  →  `c` (`24`)
  - formula: `multiply(factorial(4), factorial(1))`
- `preethi has 6 flavors of ice cream in his parlor . how many options are there for dorathy to pick a one - flavor , two - flavor , 3 - flavor , 4 - flavor , 5 flavor or 6 flavor order ?`  →  `e` (`63`)
  - formula: `add(add(add(add(divide(factorial(6), factorial(5)), divide(factorial(6), multiply(const_2, factorial(4)))), divide(factorial(6), multiply(factorial(3), factorial(3)))), divide(factorial(6), multiply(const_2, factorial(4)))), divide(factorial(6), factorial(5)))`

#### `(probability, geom_3d_encoded)` — *review*  (6 items)

Tiny — geometric probability?

- `how many cubes of 5 cm edge can be cut out of a cube of 15 cm edge`  →  `c` (`27`)
  - formula: `divide(volume_cube(15), volume_cube(divide(5, const_100)))`
- `how many cubes of 8 cm edge can be cut out of a cube of 16 cm edge`  →  `c` (`8`)
  - formula: `divide(volume_cube(16), volume_cube(divide(8, const_100)))`
- `how many cubes of 5 cm edge can be cut out of a cube of 20 cm edge`  →  `b` (`64`)
  - formula: `divide(volume_cube(20), volume_cube(divide(5, const_100)))`
- `how many cubes of 3 cm edge can be cut out of a cube of 18 cm edge`  →  `c` (`216`)
  - formula: `divide(volume_cube(18), volume_cube(divide(3, const_100)))`
- `how many cubes of 4 cm edge can be cut out of a cube of 28 cm edge`  →  `a` (`343`)
  - formula: `divide(volume_cube(28), volume_cube(divide(4, const_100)))`
- `how many cubes of 2 cm edge can be cut out of a cube of 24 cm edge`  →  `b` (`1728`)
  - formula: `divide(volume_cube(24), volume_cube(divide(2, const_100)))`

#### `(probability, prob)` — *variety*  (223 items)

Explicit prob ops. G7 fit for `probability_simple_event`, `probability_compound_event`, `simulate_compound`. Note: permutation / combination beyond a few hundred items per category is HS — filter for K–8-range inputs.

- `two mba admissions committees are to be formed randomly from 6 second year mbas with 3 members each . what is the probability e that jane will be on the same committee as albert ?`  →  `d` (`40 %`)
  - formula: `multiply(divide(multiply(choose(const_4, const_1), const_2), choose(6, 3)), multiply(multiply(const_5, const_5), const_4))`
- `a store has 10 bottles of juice , including 5 bottles of apple juice . in the evening , 6 bottles of juice are sold one by one . what is the probability of selling 4 bottles of apple juice among the 6 bottles ? assume that every bottle has an equal chance of being bought .`  →  `d` (`5 / 21`)
  - formula: `divide(choose(5, 4), choose(10, 5))`
- `what is the probability of getting exactly 1 head in a single throw of five fair coins ?`  →  `d` (`5 / 32`)
  - formula: `divide(const_2, choose(add(const_3, const_3), const_3))`
- `there are 8 students . 4 of them are men and 4 of them are women . if 4 students are selected from the 8 students . what is the probability that the number of men is equal to that of women ?`  →  `a` (`18 / 35`)
  - formula: `divide(multiply(choose(4, divide(4, const_2)), choose(4, divide(4, const_2))), choose(8, 4))`
- `a coin is tossed 6 times . what is the probability of getting exactly 5 heads ?`  →  `d` (`3 / 32`)
  - formula: `multiply(power(divide(const_1, const_2), 5), multiply(choose(6, 5), divide(const_1, const_2)))`
- `a bag contains 6 red , 3 yellow and 4 green balls . 3 balls are drawn randomly . what is the probability that the balls drawn contain balls of different colours ?`  →  `d` (`1 / 4`)
  - formula: `divide(multiply(choose(4, const_2), choose(add(6, 3), const_1)), choose(add(add(6, 3), 4), 6))`
- `a fair coin is tossed 3 times . what is the probability of getting at least 2 heads ?`  →  `d` (`1 / 2`)
  - formula: `divide(add(add(add(choose(3, const_2), choose(3, const_3)), choose(3, const_4)), choose(3, 3)), power(const_2, 3))`
- `a bag contains 3 red , 6 yellow and 8 green balls . 3 balls are drawn randomly . what is the probability that the balls drawn contain balls of different colours ?`  →  `d` (`18 / 85`)
  - formula: `divide(multiply(choose(const_4.0, const_2), choose(add(const_3.0, 6), const_1)), choose(add(add(3, 6), 8), 3))`

