# Math City — Implementation Plan

> Living document. Update as decisions are made and phases progress.
> Source of truth for product scope: [prd.md](prd.md).

---

## Status

- **Phase:** Phase 6 — Full Question Bank. Broad K–8 coverage; live counts (sub-concepts / widgets / datasets) live in the Status block of [curriculum.md](curriculum.md), auto-refreshed by [tools/curriculum/sync_implementation_status.py](tools/curriculum/sync_implementation_status.py). Remaining gaps: transformations (G8), scatter plots / two-way tables (G8), compound probability (needs `tree_diagram` widget).
- **Last updated:** 2026-05-18
- **Toolchain in the web sandbox:** The repo ships a `SessionStart` hook ([.claude/hooks/session-start.sh](.claude/hooks/session-start.sh)) that installs **Flutter 3.41.7** (same pin as CI) and runs `flutter pub get` automatically at the start of every Claude-Code-on-the-web session. **`flutter`, `dart`, `flutter analyze`, and `flutter test` are available** — always verify changes locally before committing; don't push code unseen and "rely on CI" as a substitute. The hook runs in async mode, so on a cold start there's a ~1–2 minute window where Flutter is still downloading; if a `flutter` command fails immediately at session start, wait a moment and retry once. A `PostToolUse` hook on `Write|Edit` ([.claude/settings.json](.claude/settings.json)) auto-runs `dart format` on any `.dart` file Claude touches, so the CI format step can't be tripped by drift.
- **Last action:** **Chunk 64 — K-G1 measurement vocabulary (3 text-only generators)** — 3 new text-only generators in a new file [measurement_vocab_generators.dart](lib/domain/questions/generators/measurement_vocab_generators.dart) that complete the K-G1 measurement-vocab spine. `describe_attribute` (K, 8 scenarios crossing length/weight/capacity/temperature; "Which attribute do you measure to find out X?" → MC over the 4 attribute words). `compare_two_objects` (K, 5 pair scenarios with two distinct lengths/weights in 2..20; "X is a units. Y is b units. Which is X/heavier/bigger?" — phrasing matches the unit, distractor pool of 4 always includes both subjects + "they are the same" / "cannot tell"). `order_three_objects_length` (G1, 4 triple scenarios with 3 distinct lengths in 2..30; 50/50 between "shortest" and "longest" flavours; answer is the subject at the matching extreme). All three are text-only despite curriculum.md flagging `required:shape` — the visual is implicit in the named subjects; can be retrofitted with a Shape composition widget later. Short labels: measure?, longer?, sort 3. **Caught test fallout:** `describe_attribute` is a new K-grade root in the measurement category — a 6th K-grade row-0 root. The starter pack rolls back to the first 4 by category display order, so `identify_shape_2d` (geometry) is now pushed out alongside `classify_count_categories` (stats). New starter pack: `{count_to_10, teen_numbers_as_ten_plus, add_within_5, describe_attribute}`. Updated 3 tests across [proficiency_unlock_test.dart](test/state/proficiency_unlock_test.dart) and [dag_engine_test.dart](test/domain/concepts/dag_engine_test.dart): the two starter-pack assertions plus the "add_within_10 wins" test which gains `describe_attribute` in its introduced set so measurement isn't the new fewest-active. Tests: +3 groups @ 300 iterations each. **522 total pass; analyze clean. 348/374 sub-concepts (93%), 23/30 widgets.**
- **Last action (prior):** **Chunk 63 — LineFigure widget + G4 lines/angles classification (3 generators)** — 1 new widget, 3 new generators in a new file [line_figure_generators.dart](lib/domain/questions/generators/line_figure_generators.dart). `LineFigureKind` enum: line / ray / segment + parallelLines / perpendicularLines / intersectingLines, each with a `displayName`. `LineFigureSpec` carries `kind`. `LineFigure` widget ([lib/presentation/diagrams/line_figure.dart](lib/presentation/diagrams/line_figure.dart)) is a `CustomPainter` rendering schematic figures: single line has arrows on both ends; ray has a dot + arrow; segment has dots on both ends. Pair figures: parallel = two horizontal arrowed lines; perpendicular = horizontal + vertical with a right-angle marker at the intersection; intersecting = two diagonals crossing at a non-right angle. Wired into `DiagramRenderer`. Generators: `identify_lines_rays_segments` (G4, 3-way MC over line/ray/line segment with "point" as a 4th confidence-builder distractor), `parallel_perpendicular_lines` (G4, 3-way MC over parallel/perpendicular/intersecting with "overlapping" as the 4th distractor so the pool has 4 entries), `classify_2d_by_lines_angles` (G4, two question flavors drawn 50/50 across seeds: "Does this quadrilateral have all right angles?" / "Does this quadrilateral have all sides the same length?" → Yes/No on a Shape:quad diagram). **Override cleanup:** removed two substitutes since their dropped prereqs now live: `right_acute_obtuse_angle: ['add_within_100']` (curriculum prereq `identify_lines_rays_segments` met), `classify_2d_hierarchy: ['classify_quadrilaterals']` (curriculum prereq `classify_2d_by_lines_angles` met). New §6.2 row in [curriculum.md](curriculum.md) for `LineFigure`; `WIDGET_TO_FILE` updated. Short labels: line/ray?, ∥ ⊥ ×?, right ∠?. Tests: +3 groups @ 300 iterations each. **519 total pass; analyze clean. 345/374 sub-concepts (92%), 23/30 widgets.**
- **Last action (prior):** **Chunk 62 — TapeDiagram + DoubleNumberLine widgets + 2 G6 ratio generators** — 2 new widgets, 2 new generators in a new file [ratio_diagram_generators.dart](lib/domain/questions/generators/ratio_diagram_generators.dart). `TapeDiagramSpec` carries `topUnits`/`bottomUnits` + optional row labels. `TapeDiagram` widget ([lib/presentation/diagrams/tape_diagram.dart](lib/presentation/diagrams/tape_diagram.dart)) renders two stacked horizontal bars split into unit cells of equal width so the a:b ratio shows as relative bar lengths. `DoubleNumberLineSpec` carries `topValues`/`bottomValues` (parallel int lists). `DoubleNumberLine` widget ([lib/presentation/diagrams/double_number_line.dart](lib/presentation/diagrams/double_number_line.dart)) renders two parallel number lines with corresponding labelled ticks (left-pad title labels). Both wired into `DiagramRenderer`. Generators share 5 themed ratio contexts (cups/sugar, apples/oranges, red/blue marbles, miles/gallons, boys/girls): `ratio_table` (G6, ratio a:b with a,b ∈ [2,5] distinct, scale k ∈ [2,5]; prompt "$a {ctx1} for every $b {ctx2}, if you use $a·k {ctx1}, how many {ctx2}?"; misconception = unscaled b "forgot the scale"), `double_number_line` (G6, same data shape but always 4 tick positions {0, a, 2a, 3a} on top with the question asking for the off-the-diagram 4·b position; misconception = 3·b "read the last labelled value"). Short labels: ratio tbl, 2× line. `WIDGET_TO_FILE` already had TapeDiagram + DoubleNumberLine; §6.2 rows exist. Tests: +2 groups @ 300 iterations each (parses the prompt to recover a/b/k and asserts the answer follows). **516 total pass; analyze clean. 342/374 sub-concepts (91%), 22/29 widgets.**
- **Last action (prior):** **Chunk 61 — BaseTenBlocks widget + teen_numbers_as_ten_plus (K)** — 1 new widget, 1 new generator in a new file [base_ten_blocks_generators.dart](lib/domain/questions/generators/base_ten_blocks_generators.dart). `BaseTenBlocksSpec` carries `hundreds`/`tens`/`ones` int counts. `BaseTenBlocks` widget ([lib/presentation/diagrams/base_ten_blocks.dart](lib/presentation/diagrams/base_ten_blocks.dart)) renders Diene's-style place-value blocks: 10×10 flats, 1×10 rods, and unit cubes laid out in a horizontal `Wrap`, each block drawn as a grid of `CustomPaint` cells so kids see magnitude visually. Wired into `DiagramRenderer`. Generator: `teen_numbers_as_ten_plus` (K, "This shows N as 1 ten and some ones. How many ones?" — tens=1, ones ∈ 1..9, answer = ones; misconception distractor = the full teen number N, the "I counted everything" mistake). **Override cleanup:** removed `place_value_2digit: <String>[]` since `teen_numbers_as_ten_plus` now lives — curriculum prereq is met. Short label: teen = 10+. **Caught test fallout:** as with Chunks 41/55/59, adding a new K-grade root in a fifth category (place_value) shifted the deterministic starter pack to `{count_to_10, teen_numbers_as_ten_plus, add_within_5, identify_shape_2d}` — `classify_count_categories` got pushed out (5th by category display order, only 4 slots). Updated 2 starter-pack tests across [proficiency_unlock_test.dart](test/state/proficiency_unlock_test.dart) and [dag_engine_test.dart](test/domain/concepts/dag_engine_test.dart). `WIDGET_TO_FILE` already had BaseTenBlocks; §6.2 row exists. Tests: +1 group @ 300 iterations. **514 total pass; analyze clean. 340/374 sub-concepts (91%), 20/29 widgets.**
- **Last action (prior):** **Chunk 60 — Shape-widget classification + hierarchy + 3D Pythagorean (4 generators)** — 4 new generators added to [shape_generators.dart](lib/domain/questions/generators/shape_generators.dart), all riding on the Chunk 59 Shape widget. `classify_quadrilaterals` (G3, show a quad at canonical orientation; 5-way MC over square/rectangle/parallelogram/rhombus/trapezoid). `line_of_symmetry` (G4, "How many lines of symmetry?" — integer answer; `_symmetryCounts` map covers 12 ShapeKinds at the orientation we draw, range 0..8; circle excluded since infinite). `classify_2d_hierarchy` (G5, "True or False: every {child} is a {parent}" — 10 hardcoded hierarchy facts across the quad family; diagram shows an instance of the *child* so the kid sees a concrete example; MC includes 'Only sometimes' / 'Cannot tell' confidence-builders). `pythagorean_apply_3d` (G8, "A rectangular box has l, w, h — find the diagonal" — 9 hand-picked Pythagorean triples in 3D so l² + w² + h² is a perfect square; diagram is the schematic cube since Shape widget doesn't yet take box dimensions). **Override:** new `classify_2d_hierarchy: ['classify_quadrilaterals']` substitutes the still-unimplemented `classify_2d_by_lines_angles` prereq. Short labels: name quad, symmetry, is a __?, a²+b²+c². Tests: +4 groups @ 300 iterations each (line-of-symmetry test mirrors the `_symmetryCounts` map; pythagorean test parses the prompt and asserts `d² = l² + w² + h²`). **513 total pass; analyze clean. 339/374 sub-concepts (91%), 19/29 widgets.**
- **Last action (prior):** **Chunk 59 — Shape widget + K-G2 shape-recognition family (4 generators)** — 1 new widget, 4 new generators in a new file [shape_generators.dart](lib/domain/questions/generators/shape_generators.dart). `ShapeKind` enum covers 14 kinds (circle, 4 triangle variants, 5 quad variants, pentagon/hexagon/octagon, plus 4 schematic 3D solids: cube/sphere/cylinder/cone) with `sideCount` and `displayName` helpers. `ShapeSpec` carries `kind` + optional `label` + a `showRightAngleMark` flag for right triangles. `Shape` widget ([lib/presentation/diagrams/shape.dart](lib/presentation/diagrams/shape.dart)) is a `CustomPainter` rendering an outline-only figure with a light fill at a fixed canonical orientation per kind (rectangle visibly wider than tall, trapezoid with the longer base down, etc.) so kids tell kinds apart at a glance; 3D solids are drawn as wireframe schematics. Wired into `DiagramRenderer`. Generators: `identify_shape_2d` (K, MC over 6 names: circle/triangle/square/rectangle/pentagon/hexagon — triangles deliberately drawn in 3 visual variants under one "triangle" answer so kids learn the name spans shapes), `identify_shape_3d` (K, MC over the 4 K-grade 3D names), `shape_attributes_basic` (G1, "how many sides?" — pool spans 3-, 4-, 5-, 6-, 8-sided polygons; misconception ±1 via the integer-distractor default), `identify_polygons` (G2, MC over triangle/quadrilateral/pentagon/hexagon — pool maps side-count to formal polygon name). **Override cleanup:** removed two substitutes since their dropped prereqs now live: `triangle_inequality_recognize: ['perimeter_polygon']` (curriculum prereq `shape_attributes_basic` met), `partition_into_rows_columns: ['add_within_100']` (curriculum prereq `identify_shape_2d` met). **Caught test fallout:** as in Chunks 41 and 55, adding a new K-grade root in a fourth category (geometry) shifted the deterministic starter pack. Updated 3 tests across [proficiency_unlock_test.dart](test/state/proficiency_unlock_test.dart) and [dag_engine_test.dart](test/domain/concepts/dag_engine_test.dart): the K starter pack is now exactly `{count_to_10, add_within_5, identify_shape_2d, classify_count_categories}` — four K-grade row-0 concepts filling all four starter slots with no roll-over; the "add_within_10 wins over fewer-active categories" test gains `identify_shape_2d` in its introduced set so geometry isn't the new fewest-active. Short labels: name 2D, name 3D, sides?, polygon?. `WIDGET_TO_FILE` already had Shape; §6.2 row exists. Tests: +4 groups @ 300 iterations each. **509 total pass; analyze clean (modulo one pre-existing test-file lint). 335/374 sub-concepts (90%), 19/29 widgets.**
- **Last action (prior):** **Chunk 58 — Protractor widget + G4 protractor family (2 generators)** — 1 new widget, 2 new generators in a new file [protractor_generators.dart](lib/domain/questions/generators/protractor_generators.dart). `ProtractorSpec` carries `angleDeg ∈ [0, 180]` + a `showAngleLabel` flag. `Protractor` widget ([lib/presentation/diagrams/protractor.dart](lib/presentation/diagrams/protractor.dart)) is a `CustomPainter` rendering a half-circle with tick labels at every 10°, the fixed base ray along 0°, a second ray at the marked angle (treating 0° as east and CCW positive), an arc inside the angle, and an optional label inside the wedge. Wired into `DiagramRenderer`. Generators: `measure_angle_protractor` (G4, angle ∈ [15, 165] step 5° excluding 90; primary misconception 180 − a "read the wrong scale"), `draw_angle_protractor` (G4, same parameter ranges with the angle label shown inside the wedge — drills the "match angle measure to figure" skill by asking the kid to confirm the labelled value back; same misconception distractor). **Override cleanup:** removed `angle_addition: ['add_within_100']` since `measure_angle_protractor` now lives — curriculum prereq is met. Short labels: measure ∠, draw ∠. `WIDGET_TO_FILE` already had Protractor; §6.2 row exists. Tests: +2 groups @ 300 iterations each. **505 total pass; analyze clean (modulo one pre-existing test-file lint). 331/374 sub-concepts (88%), 18/29 widgets.**
- **Last action (prior):** **Chunk 57 — Ruler widget + G2-G3 length-measurement family (3 generators)** — 1 new widget, 3 new generators in a new file [ruler_generators.dart](lib/domain/questions/generators/ruler_generators.dart). `RulerSpec` carries `totalLength` (whole units), `markedLength` (in units of `1/subdivisions`), `unitLabel`, and `subdivisions` ∈ {1, 2, 4}. `Ruler` widget ([lib/presentation/diagrams/ruler.dart](lib/presentation/diagrams/ruler.dart)) is a `CustomPainter` rendering a horizontal ruler with major-tick labels at every whole unit, minor unlabelled ticks at every subdivision, and a coloured "object bar" above the ruler from 0 to the marked length. Shrink-to-fit pattern. Wired into `DiagramRenderer`. Generators: `measure_with_ruler_inches` (G2, object length ∈ [1, 8] inches; misconception "counted ticks instead of intervals"), `measure_with_ruler_cm` (G2, object length ∈ [2, 14] cm; ruler size scales to {12, 15, 20} based on length), `measure_to_half_quarter_inch` (G3, subdivisions ∈ {2, 4}, re-rolls until `markedLength % subdivisions != 0` so the object always lands on a fractional tick; answer in canonical mixed-number form like `2 1/2` or `3/4`; misconception distractors include "dropped the fractional part" and "read the next whole tick"). **Override cleanup:** removed three substitutes since `measure_with_ruler_inches` now lives: `line_plot_whole: ['add_within_20']`, `length_word_problems: ['add_within_100']`, `length_diff_units: ['sub_within_100']` — curriculum prereqs are met. Two new overrides for the new generators: `measure_with_ruler_inches` and `measure_with_ruler_cm: ['add_within_20']` drop the still-unimplemented `measure_length_units` (G1 iterated-units variant of the ruler). Short labels: ruler in, ruler cm, ruler ½/¼. `WIDGET_TO_FILE` already had Ruler; §6.2 row already exists. Tests: +3 groups @ 300 iterations each. **503 total pass; analyze clean (modulo one pre-existing test-file lint). 329/374 sub-concepts (88%), 17/29 widgets.**
- **Last action (prior):** **Chunk 56 — Money widget + G1-G2 money family (4 generators)** — 1 new widget, 4 new generators in a new file [money_generators.dart](lib/domain/questions/generators/money_generators.dart). `MoneyDenom` is a value enum (penny/nickel/dime/quarter + $1/$5/$10/$20) with cents in integers + a kid-facing label. `MoneySpec` carries an ordered list of denominations. `Money` widget ([lib/presentation/diagrams/money.dart](lib/presentation/diagrams/money.dart)) renders coins as colour-tinted labelled circles (copper for penny, three shades of grey for nickel/dime/quarter) and bills as rounded rectangles, all in a horizontal `Wrap`. Schematic — no third-party photo-realistic art (license-safe per curriculum.md §6.2). Wired into `DiagramRenderer`. Generators: `coins_id_value` (G1, single labelled coin; "What is the value of this coin?" → cents int; misconception is neighbouring-coin value), `count_coins` (G2, 3-6 coins summed to a total in [10, 99]; misconception is "counted coins, ignored value" = coin count), `count_bills_coins` (G2, 1-2 bills + 2-4 coins, total ≤ $25.99 and not a whole-dollar amount; answer formatted as `$N.NN`; misconception is "treated bill face values as cents"), `change_from_purchase` (G2, pay ∈ {$1, $5, $10, $20}, cost in 5¢ steps; answer in canonical $N.NN form; primary misconception is "added instead of subtracted"). **Override cleanup:** removed the `money_word_problems: ['add_within_100']` substitute since `count_bills_coins` now lives — curriculum prereq is met. Short labels: coin?, coin sum, `$ + ¢`, change. `WIDGET_TO_FILE` updated; §6.2 row for Money already exists in curriculum.md. Tests: +4 groups @ 300 iterations each in [money_generators_test.dart](test/domain/questions/money_generators_test.dart) including format-string parsing for the `$N.NN` answer. **500 total pass; analyze clean (modulo one pre-existing test-file lint). 326/374 sub-concepts (87%), 16/29 widgets.**
- **Last action (prior):** **Chunk 55 — PictureGraph widget + K-G3 stats family (4 generators)** — 1 new widget, 4 new generators in a new file [picture_graph_generators.dart](lib/domain/questions/generators/picture_graph_generators.dart). `PictureGraphSpec` carries title + per-row labels + per-row values + a single icon glyph + optional scale (1 for unscaled K-G2, >1 for scaled G3). `PictureGraph` widget ([lib/presentation/diagrams/picture_graph.dart](lib/presentation/diagrams/picture_graph.dart)) renders as a Flutter `Table` with two columns: row label on the left, a horizontal string of `value ~/ scale` icon glyphs on the right; an "Each 🍎 = 2" key line below the graph when scale > 1. Wired into `DiagramRenderer`. Generators share 4 themed contexts (favourite fruit / pets at home / cars on the street / today's weather log): `classify_count_categories` (K, 2 rows, counts ∈ [1, 5] pairwise distinct, "how many X?" → MC over the values; misconception distractor reads the wrong row), `three_category_data` (G1, 3 rows, counts ∈ [1, 6] pairwise distinct, 50/50 single-row vs. "how many in all"; test asserts both flavors appear), `picture_graph_read` (G2, 4 rows, counts ∈ [2, 9] pairwise distinct, 50/50 single-row vs. compare-pair "how many more X than Y"), `scaled_picture_graph` (G3, scale ∈ {2, 5, 10}, drawn-icon counts ∈ [1, 7], answer = `drawnIcons × scale`; primary misconception is the unscaled icon count). **Override cleanup:** removed three substitutes since their dropped prereqs now live: `bar_graph_read: ['add_within_100']` (curriculum prereq `picture_graph_read` met), `mode: ['add_within_100']` (curriculum prereq `classify_count_categories` met), `two_way_table_construct: ['add_within_100']` (same). **Caught test fallout:** as in Chunk 41, adding new K-grade roots in a third category (stats) shifted the deterministic starter pack. Updated 3 tests across [proficiency_unlock_test.dart](test/state/proficiency_unlock_test.dart) and [dag_engine_test.dart](test/domain/concepts/dag_engine_test.dart): the K starter pack is now `{count_to_10, add_within_5, classify_count_categories, count_to_20}` (three row-0 K-grade concepts + counting:1 as the rollover); the post-mastery pick after `add_within_5` becomes `sub_within_5` (add_sub now has 0 active concepts so wins the fewest-active-category tiebreak). Short labels: sort+count, 3 cats, pic graph, pic scaled. New §6.2 row in curriculum.md for `PictureGraph` (paired with §6.1's `BarChart`); `WIDGET_TO_FILE` updated. Tests: +4 groups @ 300 iterations each in [picture_graph_generators_test.dart](test/domain/questions/picture_graph_generators_test.dart). **496 total pass; analyze clean (modulo one pre-existing test-file lint). 322/374 sub-concepts (86%), 15/29 widgets.**
- **Last action (prior):** **Chunk 54 — Angle + TriangleAngles widgets, retrofit 6 text-only angle generators, 3 new generators** — 2 new widgets, 3 new generators in a new file [angle_generators.dart](lib/domain/questions/generators/angle_generators.dart), 6 retrofits, 1 unused-import cleanup. `AngleSpec` carries `rayAnglesDeg` (rays from a single vertex, 0° = east, CCW positive) + `wedgeLabels` (per-wedge label between consecutive rays). `Angle` widget ([lib/presentation/diagrams/angle.dart](lib/presentation/diagrams/angle.dart)) is a `CustomPainter` rendering rays + a small arc with a label in each labelled wedge. `TriangleAnglesSpec` carries the three interior angle measures + per-vertex labels + a `showExteriorAtC` flag. `TriangleAngles` widget ([lib/presentation/diagrams/triangle_angles.dart](lib/presentation/diagrams/triangle_angles.dart)) computes vertex positions via the law of sines (so a 90-45-45 looks visibly right-angled), labels A/B/C at the vertices, places the angle labels inside each corner along the bisector, and optionally draws a dashed exterior extension at C for `exterior_angle_triangle`. Both wired into `DiagramRenderer`. **New generators:** `right_acute_obtuse_angle` (G4, three-way classification — Acute / Right / Obtuse — drawn uniformly so all classes appear across seeds; obtuse range stops at 165° to keep the visual clean). `angle_addition` (G4, "An angle has been split into two adjacent angles measuring $a° and $b°. What is the total?" → $a+b$ with a 3-ray figure showing both sub-angles). `fraction_on_number_line` (G3, NumberLine 0..1 divided into $d$ ticks, mark at $n/d$; distractor pool widened with fallbacks `n/(d+2)`, `(n+2)/d`, etc. so the simplest case `1/2` still has 3 distinct distractors). **Retrofits — diagrams added to existing text-only generators:** `supplementary_angles` (3-ray straight line, `a°` and `?`), `complementary_angles` (3-ray right angle, `a°` and `?`), `vertical_angles` (4-ray crossing lines, given wedge `a°` and the `?` wedge selected from index 1 or 2 depending on the question mode), `adjacent_angles` (3-ray right angle or straight line), `triangle_angle_sum` (triangle with two labelled angles + `?`), `exterior_angle_triangle` (triangle with dashed exterior extension at C). `parallel_lines_transversal` left text-only this chunk (deferred to a follow-up chunk for a parallel-lines spec). **Override cleanup:** removed the `supplementary_angles` / `complementary_angles: ['add_within_100']` substitutes since `right_acute_obtuse_angle` now lives — curriculum prereq is met. Two new overrides for the new G4 generators: `right_acute_obtuse_angle: ['add_within_100']` drops the still-unimplemented `identify_lines_rays_segments`; `angle_addition: ['add_within_100']` drops `measure_angle_protractor` (still no Protractor widget). Short labels: ∠ kind?, ∠ + ∠, frac line. `WIDGET_TO_FILE` updated; new §6.2 row in [curriculum.md](curriculum.md) for `TriangleAngles` (paired with §6.1's `Angle`); §6.1 Angle-row updated to reference §6.2. Tests: +5 groups @ 300 iterations each in [angle_generators_test.dart](test/domain/questions/angle_generators_test.dart) — including retrofit-correctness checks asserting `AngleSpec.rayAnglesDeg` / `TriangleAnglesSpec.angleDeg*` match the question's stated measures. **492 total pass; analyze clean (modulo one pre-existing test-file lint). 318/374 sub-concepts (85%), 14/28 widgets.**
- **Last action (prior):** **Chunk 53 — Graph generators reusing CoordinatePlane** — 5 new generators in a new file [graph_extra_generators.dart](lib/domain/questions/generators/graph_extra_generators.dart). `two_pattern_relationships` (G5, "Pattern A adds $sA each step, Pattern B adds $sB; what point is on Pattern $X at step $n?" — plots both patterns as labeled point sets on Q1 plane). `graph_proportional_slope` (G8, line through origin with slope k ∈ {±2, ±3, ±4, ±5}; "What is k?" — misconception distractors include sign-flip and magnitude-only). `qualitative_graph_features` (G8, three-way classification: Increasing / Decreasing / Constant; line with positive/negative/zero slope; test asserts all three classes appear). `interpret_slope_intercept_data` (G8, "Line of best fit y = mx + b. Predict y when x = N." with the scatter dots and dashed best-fit line both drawn; primary misconception: forgot the intercept). `simulate_compound` (G7, text-only: "two coins flipped N times, both heads M times — experimental P(HH)?" → M/N as canonical fraction). All 5 ride on existing widgets (CoordinatePlane for the 4 visual ones). Short labels: 2 patterns, find slope, incr/decr, pred y, P sim. Tests: +5 groups @ 200 iterations each. **484 total pass; analyze clean. 315/374 sub-concepts (84%).**
- **Last action (prior):** **Chunk 52 — Array-grid / Clock / FractionBar / NumberLine batch** — 12 new generators in a new file [array_grid_extra_generators.dart](lib/domain/questions/generators/array_grid_extra_generators.dart), all using existing widgets. `count_objects_to_10` and `count_objects_to_20` (K, AreaGrid as a row-and-column count visual). `equal_groups_intro` (G2, "n equal groups × m objects each"). `array_repeated_addition` (G2, "Which repeated addition shows the total?" — answer is `cols + cols + ...` rows times). `mult_meaning_groups` (G3, "Which multiplication describes this array?"). `div_meaning_share` and `div_meaning_grouping` (G3, sharing vs. grouping framings of the same array). `distributive_mult_over_add` (G3, "a × c = a × (x + y) = ?"). `time_to_minute` (G3, Clock with minute ∈ 0..59 — every minute, not just 5s). `elapsed_time` (G3, "It is hh:mm. What time will it be in n hours and m minutes?" — start ∈ 1..11 hr, on a 5-min grid). `fraction_denom_10_100` (G4, FractionBar denom = 10, numerator ∈ 1..9; lesson is reading tenths). `approximate_irrational` (G8, "Between which two consecutive integers does √n lie?" with NumberLine showing the irrational position). **Override cleanup:** removed `mult_facts_3 → mult_facts_within_100` (since `mult_meaning_groups` now lives) and `decimal_notation_tenths` / `percent_intro` overrides (since `fraction_denom_10_100` now lives). Distractor pool widened on `array_repeated_addition` to handle rows=cols cases where the wrong-axis distractor collides with the correct answer. Short labels: count obj 10/20, groups, array +, × meaning, share, ÷ groups, distrib, clock 1m, elapsed, tenths, √n approx. Tests: +12 groups @ 200 iterations each. **479 total pass; analyze clean. 310/374 sub-concepts (83%).**
- **Last action (prior):** **Chunk 51 — Diagram-using generators on existing widgets** — 9 new generators in a new file [diagram_extra_generators.dart](lib/domain/questions/generators/diagram_extra_generators.dart), all riding on existing widgets (FractionBar, NumberLine, AreaGrid). `partition_halves_fourths` (G1, FractionBar with denom ∈ {2, 4}; same shape as `fraction_a_over_b` but range-restricted to K-1 fractions). `partition_thirds` (G2, FractionBar with denom = 3). `unit_fraction_intro` (G3, FractionBar with numerator = 1, denom ∈ {2..8}). All three use `AnswerFormat.fraction` + `AnswerShape.exactString` like the existing fraction reader. `number_line_add_sub` (G2, NumberLine with marked starting point + labelled hop showing +b or −b; range auto-fits the operation). `decimal_on_number_line` (G4, NumberLine 0..1 with divisions ∈ {10, 20}, marked at one tick — `AnswerFormat.decimal`). `integers_on_number_line` (G6, NumberLine [-10, 10] with marked non-zero integer; U+2212 minus rendering). `area_rectangle_count_squares` (G3, AreaGrid fully shaded, count squares). `partition_into_rows_columns` (G2, same widget). `inference_from_sample` (G7, text-only proportional inference scaled from a clean sample of 50 or 100). **Override cleanup batch:** removed/cleaned 6 prereq substitutions since their dropped prereqs now live: `fraction_a_over_b` (unit_fraction_intro), `absolute_value` + `opposites_and_zero` (integers_on_number_line), `compare_order_rationals` (integers_on_number_line), `plot_first_quadrant` (number_line_add_sub), `area_rectangle_formula` (area_rectangle_count_squares); `volume_rect_prism_formula` now substitutes `area_rectangle_count_squares` instead of `mult_facts_within_100`. New override: `partition_into_rows_columns: ['add_within_100']` drops `identify_shape_2d` (still no Shape widget). Short labels: ½ ¼, ⅓, 1/b, ± line, dec line, ± int line, count □, rows × cols, estimate. Tests: +9 groups @ 200 iterations each; decimal distractor pool was widened to handle divisions=20 cases where the complementary-tick distractor collides with the correct answer. **467 total pass; analyze clean. 298/374 sub-concepts (80%).**
- **Last action (prior):** **Chunk 50 — Dataset-marked word problems and stats judgment items** — 8 new text-only generators in a new file [word_problems_extra_generators.dart](lib/domain/questions/generators/word_problems_extra_generators.dart). `length_word_problems` (G2, "$name has a rope $a $unit long. $name {attaches $b more | cuts off $b}. How long now?" — 50/50 +/− with 5 themed item-unit pairs). `money_word_problems` (G2, two flavors: coin count "3 dimes + 2 nickels = ? cents" / dollar spending "had $$x, spent $$y, left?" — primary misconception for the coin flavor is "counted coins, ignored value"). `liquid_volume_mass` (G3, 5 scenarios crossing mL/L/g; pitcher pours / bottle drinks / tank drains / bag spills / jar removes). `mult_div_word_2step` (G4, two flavors: "$a boxes × $b apples − $c given away" or "$a + $b apples ÷ $c baskets"; re-rolls for exact division in the second flavor). `area_perimeter_word` (G4, "garden is l × w m" → asks area or perimeter 50/50; primary misconception distractor is the *other* measure). `convert_units_multistep` (G5, "$big $bigUnit $small $smallUnit, convert to $smallUnit"; 5 conversions across length/time/mass). `statistical_question` (G6, judgment item: "Which of these is a statistical question (anticipates variability)?" — 5 hardcoded stat/non-stat pairs; correct is one stat, distractors are 3 non-stats from other pairs). `sampling_representativeness` (G7, "Is this a fair sample?" — 5 hardcoded scenarios mixing biased and unbiased; Yes/No + 2 confidence-builder distractors). **Prereq overrides:** `length_word_problems: ['add_within_100']` drops `measure_with_ruler_inches` (no Ruler widget yet); `money_word_problems: ['add_within_100']` drops `count_bills_coins` (no Money widget yet). **Tooling fix:** `_dartString` in [build_catalog.dart](tools/curriculum/build_catalog.dart) now emits raw strings for shortLabels containing `$` (e.g. "$ word" for `money_word_problems`) — clean satisfaction of the `use_raw_strings` lint without manual escapes. Short labels: len word, $ word, liquid, ×÷ 2-step, area word, unit 2-step, stat Q?, fair?. Tests: +8 groups @ 200 iterations each. **458 total pass; analyze clean. 289/374 sub-concepts (77%).**
- **Last action (prior):** **Chunk 49 — G1-G6 algebra and unit-conversion fill-in** — 8 new text-only generators in a new file [algebra_extra_generators.dart](lib/domain/questions/generators/algebra_extra_generators.dart). `missing_addend_within_20` (G1, "___ + a = sum" or "a + ___ = sum"; distinct from `add_sub_unknown_position` which also covers the subtraction variants). `missing_factor` (G3, "What number, when multiplied by 6, gives 42?" — word-form parallel to the existing `div_as_unknown_factor` symbolic form). `numerical_pattern_rule` (G4, "Look at the pattern: 5, 10, 15, 20. What is the rule?" → "Add 5 each time"; MC over add-step / add-step±1 / "multiply by 2" / "multiply by step" with the curated-difficulty pool). `signed_quantities_context` (G6, "Maria owes $10. Which integer represents this?" → −10; 4 hard-coded scenarios crossing money/elevation/temperature/score axes). `write_expression_from_words` (G6, 7 templates: "the sum of n and b", "b more than n", "n divided by b", etc.; MC over 4 expression strings with the kid-textbook ·, ÷, − symbols). `identify_parts_expression` (G6, "In the expression 4x + 7, what is the coefficient of x?" — 3 question flavors drawn uniformly: coefficient / constant / # of terms). `convert_units_within_system` (G4, "How many inches in 4 feet?" → 48; 7 unit pairs across length/time/weight in both customary and metric). `volume_prism_fractional_edges` (G6, V = (1/d) × w × h with d ∈ {2, 3, 4} and (w·h) % d == 0 so the answer stays integer). **Override cleanup:** removed the `solve_one_step_eq_addition` and `solve_one_step_eq_mult` substitutions since `missing_addend_within_20`, `missing_factor`, and `write_expression_from_words` now live; `expand_linear_expression` and `equivalent_expressions_props` overrides now substitute `write_expression_from_words` instead of falling back to `mult_facts_within_100`. Short labels: ?+b=r, ?×b=c, rule?, context ±, words→expr, coef/const, unit conv, V frac. Tests: +8 groups @ 200 iterations each. **450 total pass; analyze clean. 281/374 sub-concepts (75%).**
- **Last action (prior):** **Chunk 48 — K read/write numerals + word-problem batch** — 8 new text-only generators in a new file [numerals_word_generators.dart](lib/domain/questions/generators/numerals_word_generators.dart). `read_numerals_0_20` and `write_numerals_0_20` (K, paired generators in opposite directions: word → digit MC and digit → word MC over `numberToWords(n)`). `interpret_remainder_word` (G4, 3-flavor: "FULL boxes" → drop remainder, "vans NEEDED" → round up, "LEFT OVER" → just the remainder; test asserts all 3 flavors appear across seeds). `fraction_word_problems` (G5, "Maria eats a/d of a pizza, then b/d more; how much total?" → (a+b)/d; subtraction version "how much left?" → a-b)/d). `multistep_ratio_word` (G7, 50/50 between d=rt and unit-pricing scaling; misconception: added instead of multiplied / added scale instead of multiplied). `rationals_four_op_word` (G7, 50/50 temperature delta and submarine depth, with U+2212 minus-sign rendering for negative starting values; misconception: did the opposite operation). `word_problem_two_step_eq` (G7, "Sam buys apples at $p each + $q delivery, total $r" → solve px + q = r for x; misconception: forgot to subtract the fee). `system_word_problem` (G8, "two numbers add to s, diff is d, find the larger/smaller" → larger=(s+d)/2). **Override cleanup:** removed `compare_numerals_1_10: ['count_to_10']` since `read_numerals_0_20` now lives and the curriculum prereq is met. Short labels: read 0-20, write 0-20, rem story, frac word, rate word, rat word, 2-step eq, sys word. Tests: +8 groups @ 200 iterations each. **442 total pass; analyze clean. 273/374 sub-concepts (73%).**
- **Last action (prior):** **Chunk 47 — G2/G3/G7/G8 misc fill-in** — 7 new text-only generators in a new file [misc_extra_generators.dart](lib/domain/questions/generators/misc_extra_generators.dart). `am_pm` (G2, "8:30 in the morning is ___" → a.m./p.m.; 5 a.m. and 6 p.m. context phrases hard-coded; MC: a.m. / p.m. / Both / Neither). `length_diff_units` (G2, "A pencil is 17 cm long. A pencil-cap is 4 cm long. How much longer?" → 13; 5 themed scenarios with cm or inches; misconception distractor: sum). `triangle_inequality_recognize` (G7, "Can a triangle have sides 3, 4, 5?" → Yes/No; 50% valid, 50% invalid with side-sort + re-roll; confidence-builder distractors "Only if right triangle" / "Only if equilateral"). `adjacent_angles` (G7, "Two adjacent angles together form a {right angle | straight line}. One is a°. What is the other?" → 90−a or 180−a; misconception is "used the wrong total"). `exterior_angle_triangle` (G8, "Triangle has interior angles a° and b°. Exterior angle adjacent to the third?" → a + b by the exterior-angle theorem; misconception is the third interior angle 180−a−b). `inspect_system_no_solution` (G8, "How many solutions does this system have?" — three-way MC over Exactly-one / None / Infinite; same logic as `solve_linear_eq_no_or_inf` but on a system of two equations). `unit_rate_with_fractions` (G7, "Sam ran 1/2 mile in 1/4 hour. mi/hr?" → rate ∈ [2, 5]; distance & time both fractions, rate forced to whole int; misconception: multiplied instead of divided). **Prereq overrides:** `length_diff_units: ['sub_within_100']` drops unimplemented `measure_with_ruler_inches` (needs Ruler widget); `triangle_inequality_recognize: ['perimeter_polygon']` drops unimplemented `shape_attributes_basic`. Short labels: a.m./p.m., len diff, △ valid?, adj ∠, ext ∠, sys soln?, frac rate. Tests: +7 groups @ 200 iterations each. **434 total pass; analyze clean (modulo one pre-existing test-file lint). 265/374 sub-concepts (71%).**
- **Last action (prior):** **Chunk 46 — K-G5 place-value and algebra-property fill-in** — 11 new text-only generators in a new file [place_value_extra_generators.dart](lib/domain/questions/generators/place_value_extra_generators.dart). New `numberToWords(n)` helper handles `n ∈ [0, 999_999_999]` with hyphenated compounds ("twenty-three") and standard thousand/million groupings — used by both `read_write_*` generators. K/G1 algebra fill-in: `decompose_10` (K, "10 = 3 + ___" or "10 = ___ + 3"), `associative_add` (G1, "If (a+b)+c = s, then a+(b+c) = ?"), `add_2digit_multiple_of_10` (G1, "32 + 40 = ?"), `add_up_to_4_2digit` (G2, 3-or-4 addends in [10, 50]), `add_sub_fluency_within_20` (G2, mixed + and − within 20, primary misconception distractor is the other operation). G2/G4 place-value: `read_write_3digit` and `read_write_multidigit` (G2/G4 word → digit-string MC using curly quotes for the embedded number-name; distractors mimic common misreads — reversed, swapped-position, "thousand-padding"), `compare_3digit` and `compare_multidigit` (G2/G4, multi-digit numbers shown with comma separators). G5 place-value relationships: `place_value_relationship_10x` ("How many tens are in n hundreds?" → 10n; walks 3 pairs of adjacent places), `powers_of_10` ("10^4 = ?" → 10000; exp ∈ [1, 6]). New `_distinctStrings` helper falls back to off-by-N integer strings when the candidate pool runs thin — caught two test failures (`compare_3digit` and `read_write_multidigit`) where the bespoke distractors collided. All prereqs met from prior chunks — no overrides needed. Short labels: 10=a+?, (a+b)+c, 2d+×10, + 4 nums, ±/20 fluent, name 3d, compare 3d, name multi, compare md, 10× place, 10^n. Tests: +11 groups (one per generator) + 1 group for the `numberToWords` helper @ 200 iterations each. **427 total pass; analyze clean (modulo one pre-existing test-file lint). 258/374 sub-concepts (69%).**
- **Last action (prior):** **Chunk 45 — G3 multiplication-fact batch** — 14 new text-only generators in a new file [mult_facts_extra_generators.dart](lib/domain/questions/generators/mult_facts_extra_generators.dart). Nine per-row tables `mult_facts_2..10` share a `_multFactsRow(n, conceptId)` factory: `n × b = ?` with `b ∈ [2, 9]`, 50/50 swap so kids sometimes see `b × n` (same skill, different surface); misconception distractor `correct + n` (one extra `n` in the count). `mult_1digit_by_multiple_of_10` (G3, `4 × 30 = ?`); misconception distractors include "forgot ×10" and "×100". `commutative_mult` (G3, "If 4 × 7 = 28, then 7 × 4 = ?" → 28; factors forced distinct). `associative_mult` (G3, "If (2 × 3) × 5 = 30, then 2 × (3 × 5) = ?"; factors from {2,3,4,5} with re-roll until product ≤ 60). `div_as_unknown_factor` (G3, "___ × 6 = 42" or "6 × ___ = 42"; misconception "subtracted instead"). `arithmetic_patterns_in_tables` (G3, "What comes next? 4, 8, 12, 16, ?"; arithmetic sequence, step ∈ [2, 10]). **Prereq override:** `mult_facts_3: ['mult_facts_within_100']` substitutes for unimplemented `mult_meaning_groups` (needs array_grid widget) — the per-row tables work as reinforcement of the mixed-row generator. Short labels: ×2, ×3, …, ×10, × ×10, a·b=b·a, (ab)c, a·?=c, pattern. Tests: +14 groups @ 200 iterations each, verifying factor-N invariant on every row, swap orientations both appear, associative regrouping evaluates to the same product on both sides. **415 total pass; analyze clean. 247/374 sub-concepts (66%).**
- **Last action (prior):** **Chunk 44 — More G1–G2 number-and-ops fill-in** — 5 more text-only generators added to [add_sub_extra_generators.dart](lib/domain/questions/generators/add_sub_extra_generators.dart). `equal_sign_meaning` (G1, "Is 8 + 3 = 11 true?" — 50/50 true/false, half the time the RHS is off by 1; MC: True/False/"It's missing a number"/"Cannot tell"). `commutative_add` (G1, "If 4 + 7 = 11, then 7 + 4 = ?" → answer = stated sum). `add_2digit_1digit` (G1, "23 + 5 = ?" — re-rolls until sum ≤ 99). `sub_multiples_of_10` (G1, "60 − 20 = ?" — both multiples of 10, a > b). `mental_add_10_or_100` (G2, "47 + 100 = ?" — delta ∈ {10, 100}, ± direction). **Override cleanup:** `add_within_100` override removed since `add_2digit_1digit` now satisfies the curriculum prereq. Short labels added. Tests: +5 groups @ 200 iterations each. **401 total pass; analyze clean. 233/374 sub-concepts (62%).**
- **Last action (prior):** **Chunk 43 — K–2 number-and-operations fill-in batch** — 6 text-only generators in a new file [add_sub_extra_generators.dart](lib/domain/questions/generators/add_sub_extra_generators.dart). `count_within_1000` (G2, "What comes after 754?"), `even_odd` (G2, "Is 13 even or odd?" — 4-choice MC with "Both" / "Neither" as confidence distractors), `compare_2digit` (G1, greater/smaller MC over two distinct 2-digit ints), `make_10_pair` (K, "What plus 7 equals 10?"), `add_3_addends_within_20` (G1, three single-digit addends summing to ≤ 20), `add_sub_unknown_position` (G1, 4 missing-number patterns: `? + b = r`, `a + ? = r`, `r − ? = a`, `? − b = a`). All prereqs met from prior chunks — no new overrides needed. Short labels added. Tests: +6 groups @ 200 iterations each, including a substitute-and-verify check on `add_sub_unknown_position` that ensures the equation with the answer plugged in is mathematically true. **396 total pass; analyze clean. 228/374 sub-concepts (61%).**
- **Last action (prior):** **Chunk 42 — More K–2 counting concepts** — 7 more counting/skip-counting generators in the existing [counting_generators.dart](lib/domain/questions/generators/counting_generators.dart): `count_to_100_by_10` (K, "Counting by 10s: 10, 20, 30, __?"), `count_to_120` (G1, "What comes after 117?" — same shape as count_to_10 but in [100, 120] range), `count_forward_from_n` (K, "Start at 27, count forward 4 steps"), `ten_more_ten_less` (G1, ±10 with wrong-direction primary distractor), `skip_count_5` / `skip_count_10` / `skip_count_100` (all share a `_skipCountByStep` helper that emits a 4-term sequence with the 3rd position blanked). Override cleanup: removed `time_to_5_min: ['time_to_hour_half']` since `skip_count_5` now lives and the full curriculum prereq `[time_to_hour_half, skip_count_5]` is met. Short labels for all 7. Tests: +5 groups (count_to_100_by_10, count_to_120, count_forward_from_n, ten_more_ten_less, parametrized skip_count_5/10/100) @ 200 iterations each. **390 total pass; analyze clean. 222/374 sub-concepts (59%).**
- **Last action (prior):** **Chunk 41 — K–1 counting concepts batch** — 6 new K-and-G1 generators in a new file [counting_generators.dart](lib/domain/questions/generators/counting_generators.dart), all text-only single-integer answers. `count_to_10` / `count_to_20` / `count_to_100_by_1` share a `_counterUpTo` helper that picks `n ∈ [1, max−1]` and asks "What number comes right after n?" — distractors are off-by-one and skip-one misconceptions. `one_more_one_less_within_20` flips a coin for direction; "wrong direction" is the primary misconception distractor. `compare_numerals_1_10` asks "Which is greater / smaller?" between two distinct ints in `[1, 10]`; distractors include the wrong one, the sum, and the difference. `skip_count_2` shows a 4-term sequence with the 3rd position blanked. Two override cleanups landed alongside: removed `multiples_of_n: ['mult_facts_within_100']` since `skip_count_2` now lives, restoring the curriculum prereq; added a new `compare_numerals_1_10: ['count_to_10']` to substitute the unimplemented `read_numerals_0_20` (numeral-to-word-form doesn't fit our integer-only MC pattern). Short labels: "count 10" / "count 20" / "count 100" / "±1" / "compare" / "skip 2s". **Caught test fallout:** the drip-feed starter pack now alternates between the `counting` and `add_sub` categories at G0 (per the existing fewest-active-category tiebreak), so 3 tests that hard-coded the old `[add_within_5, sub_within_5, add_within_10, sub_within_10]` order needed updating. Also updated the "mastery unlocks DAG child" test to include counting concepts in `introduced` so the category-tiebreak doesn't pull the pick away from add_sub. Tests: +4 groups @ 200 iterations each + 4 test updates. **385 total pass; analyze clean. 215/374 sub-concepts (57%).**
- **Last action (prior):** **Chunk 40 — ratio_to_coordinate_pairs + dependent_independent_vars (G6)** — 2 small G6 generators in a new file [ratio_coord_generators.dart](lib/domain/questions/generators/ratio_coord_generators.dart), reusing existing widgets unchanged. `ratio_to_coordinate_pairs` picks a ratio `a:b` in lowest terms with `a, b ∈ [1, 4]` and `a != b`; plots the three equivalent ordered pairs `(a, b)`, `(2a, 2b)`, `(3a, 3b)` on a Q1 `CoordinatePlane`; asks "What ratio do these points show?" — MC over `a:b` / `b:a` / `(a+1):b` / `a:(b+1)`. `dependent_independent_vars` is text-only: 5 hard-coded real-world scenarios (handyman pay vs hours, distance vs time, plant height vs days, tank water vs minutes, apple cost vs count) each tagging their dependent and independent variable explicitly. 50/50 asks "which is the independent / dependent variable?" with 4-choice MC: the two named variables plus "Both vary together" and "Neither is a variable" as standard confusable distractors. No prereq overrides needed (both `[equivalent_ratios, plot_first_quadrant]` and `[solve_one_step_eq_mult, plot_first_quadrant]` are met). Short labels: "ratio plot" / "indep var". Tests: +2 groups @ 300 iterations each. **381 total pass; analyze clean. 209/374 sub-concepts (56%).**
- **Last action (prior):** **Chunk 39 — TwoWayTable widget + two_way_table_construct + two_way_relative_frequency (G8)** — 1 new widget, 2 generators in [two_way_table_generators.dart](lib/domain/questions/generators/two_way_table_generators.dart). `TwoWayTableSpec` carries title + row labels + col labels + 2D counts matrix + a `showTotals` flag. `TwoWayTable` widget ([lib/presentation/diagrams/two_way_table.dart](lib/presentation/diagrams/two_way_table.dart)) renders as a Flutter `Table` (not CustomPainter) with header row + header column + body cells + an extra Totals row/column when enabled; grand total in the corner. Italic style on the totals to visually separate them from the body. Wired into `DiagramRenderer`. 3 themed contexts (students × sports, adults vs kids × pet ownership, grade 5 vs 6 × walked to school). All 4 cells in `[3, 20]` and pairwise distinct so the "wrong cell" misconception distractors don't collide with the answer. `two_way_table_construct` (G8) asks "How many {row} who {col_verb / don't col_verb}?" — answer is one cell; distractors are other cells + row total + col total + grand total. `two_way_relative_frequency` (G8) asks "Of all {row}, what fraction {col}?" (50% within-row) or "Of all {col-positive}, what fraction are {row}?" (50% within-column); answer is `cell/total` as a canonical fraction; distractors include the other-axis denominator, grand-total denominator, swapped fraction, and complement. Prereq override: `two_way_table_construct: ['add_within_100']` drops the unimplemented K-stage `classify_count_categories`. Short labels: "2-way?" / "2-way %". Tests: +2 groups @ 300 iterations each — for construct, verify cells distinct and answer is one of them; for relative-frequency, verify the answer matches `cell/(rowOrColTotal)` for some `(cell, axis)` pair in canonical form. **379 total pass; analyze clean. 207/374 sub-concepts (55%), 12/27 widgets.**
- **Last action (prior):** **Chunk 38 — TreeDiagram widget + compound probability (G7)** — 1 new widget, 2 generators in [tree_diagram_generators.dart](lib/domain/questions/generators/tree_diagram_generators.dart). `TreeDiagramStage` + `TreeDiagramSpec` value types — each stage carries a label + list of outcome strings; spec exposes `leafCount` (product of branching factors). `TreeDiagram` widget ([lib/presentation/diagrams/tree_diagram.dart](lib/presentation/diagrams/tree_diagram.dart)) is a `CustomPainter` rendering a vertical tree top-to-bottom: root node, branch edges (labelled with outcome names), and leaf nodes at the bottom with compound outcome labels ("H, R" / "T, B" / etc.). Same `LayoutBuilder` shrink-to-fit pattern. Wired into `DiagramRenderer`. 4 experiments hard-coded (Coin × Coin, Coin × Spinner-3, Spinner-3 × Coin, Spinner-3 × Spinner-3); all ≤ 9 leaves to keep the tree readable on phones. Generators: `tree_diagram` (G7, "How many possible outcomes?" — answer is the product of branching factors; misconception distractors: sum, individual stage counts, off-by-one); `compound_event_probability` (G7, "What is P(specific outcome)?" — answer `1/leafCount` as a canonical fraction; misconception distractors include `1/n1`, `1/n2`, `2/leaves`, plus `1/(leaves±1)` fallbacks to ensure 3 unique survive dedup even for symmetric 2×2 experiments where the simpler misconceptions collapse). **Caught a Dart const-eval gotcha:** `const _experiments` triggered const-evaluation of `TreeDiagramStage`'s `outcomes.length` assertion, which Dart 3 rejects (List.length not const). Changed to `final _experiments`. Short labels: "tree" / "P(A∩B)". `WIDGET_TO_FILE` dict in sync script updated with `"TreeDiagram": "tree_diagram"`. Tests: +2 groups @ 300 iterations each, verifying leaf-count product and unit-fraction probability. 377 total pass; analyze clean. **205/374 sub-concepts (55%), 11/27 widgets.**
- **Last action (prior):** **Chunk 37 — Congruence + similarity via transformations (G8)** — 2 more G8 generators in [polygon_coord_generators.dart](lib/domain/questions/generators/polygon_coord_generators.dart), no widget changes. Both show preimage (solid) + image (dashed) and ask a yes/no question with verbose-but-clean MC options. `congruence_via_transformations` picks a rigid transformation 50% of the time (translation by non-zero `(dx, dy) ∈ [-3, 3]²`, reflection across x-axis, or rotation 180°) → "Yes — same size and same shape"; otherwise applies a dilation by k ∈ {2, 3} → "No — sizes are different". Distractors: the other label + "Only if reflected" + "Cannot tell from a diagram". `similarity_via_transformations` similarly 50%-similar: similarity transformation (dilation by 2, translation, or 90° rotation) → "Yes — same shape (corresponding sides in proportion)"; or non-uniform stretch (scale only x or only y by 2) → "No — corresponding sides are NOT in proportion". Distractors mirror the same pattern. Tests verify the geometric invariants directly: congruent figures have equal multisets of squared side lengths; similar figures have uniform squared-side ratios; non-similar figures have non-uniform ratios. No prereq overrides needed. Short labels: "congruent?" / "similar?". **375 total pass; analyze clean. 203/374 sub-concepts (54%).**
- **Last action (prior):** **Chunk 36 — Transformations: rotation + dilation (G8)** — 2 more G8 generators in the existing `polygon_coord_generators.dart`, no widget changes. `transformations_rotation` picks 90° / 180° / 270° CCW about origin uniformly; applies the corresponding `(x, y) → (−y, x)` / `(−x, −y)` / `(y, −x)` rule; preimage vertices forced non-zero in both coords to keep the image in `[-6, 6]²`; misconception distractors are the two other rotations plus "unchanged" and "swap-without-sign". `transformations_dilation` picks scale factor k ∈ {2, 3}; preimage in `[-3, 3]²` so image fits inside `[-9, 9]²` (rendered on a `[-9, 9]` plane); misconception distractors are dilation by k±1, asymmetric stretch (one coord only), and additive instead of multiplicative. Both reuse the same preimage-solid + image-dashed visual scheme as translation/reflection. No prereq overrides needed. Short labels: "rotate" / "dilate". Tests: +2 groups @ 300 iterations each, verifying the rotation rule per-degree-bucket and the per-vertex k× scaling. **373 total pass; analyze clean. 201/374 sub-concepts (54%).** With these two landed, `congruence_via_transformations` (prereq T+R+Rot) and `similarity_via_transformations` (prereq dilation + congruence) now have their full curriculum prereqs met — natural next-chunk follow-ups.
- **Last action (prior):** **Chunk 35 — CoordinatePlane polygon support + polygon_on_coordinate_plane (G6) + transformations_translation + transformations_reflection (G8)** — extended `CoordinatePlaneSpec` with `polygons: List<CoordinatePlanePolygon>` (auto-closed); each polygon has a `style` ∈ {solid (translucent fill + outline), dashed (outline only, secondary colour)}. The widget renders polygons beneath lines and points so labelled vertices stay readable on top. 3 new generators in [polygon_coord_generators.dart](lib/domain/questions/generators/polygon_coord_generators.dart): `polygon_on_coordinate_plane` (G6) draws an axis-aligned rectangle ABCD inside `[-6, 6]²` with width and height ∈ [2, 6]; asks "area" or "perimeter" 50/50. `transformations_translation` (G8) and `transformations_reflection` (G8) both show a preimage triangle (solid) + its image (dashed). Translation by `(dx, dy) ∈ [-3, 3]²` (non-zero). Reflection across x-axis or y-axis (50/50) with the preimage constrained to one side so the image visibly jumps. Each generator asks for the image coordinates of vertex A, B, or C; MC over coordinate strings; distractors include sign / axis confusions. **Test caught a real bug:** the reflection distractor `(-x, -y)` collides with the correct answer when the non-reflected coord is 0 — fixed by forcing the non-reflected coordinate to be non-zero, mirroring the `pickNonZero` pattern from `plot_four_quadrants`. **Override cleanup:** removed the `pythagorean_distance_coords: ['pythagorean_apply_2d', 'coord_distance_same_line']` override since `polygon_on_coordinate_plane` now lives. Short labels: "polygon coords" / "translate" / "reflect". Tests: +3 groups @ 300 iterations each, including axis-aligned-rectangle invariants for the G6 generator and per-axis sign-flip invariants for the reflection generator. **371 total pass; analyze clean. 199/374 sub-concepts (53%).**
- **Last action (prior):** **Chunk 34 — G8 line-graphing follow-ups: identify_linear_vs_nonlinear + solve_system_by_graphing** — 2 G8 generators in a new file [linear_function_generators.dart](lib/domain/questions/generators/linear_function_generators.dart). No widget changes — both ride on what Chunk 33 already built. `identify_linear_vs_nonlinear` is text-only: shows a 4-row table of (x, y) pairs in the prompt and asks "linear or nonlinear?" — 50% linear (`y = mx + b` with m ∈ {±1, ±2}, b ∈ [-3, 3]); 50% nonlinear from a uniformly-sampled set of `y = x² / x³ / |x|` (with offset) with a safety re-roll if the nonlinear generator accidentally produces an arithmetic sequence (e.g., `|x|` over consecutive non-negative ints). Distractors are the other label + two confidence-builder choices ("Cannot tell from a table" / "Only a graph can answer this"). `solve_system_by_graphing` draws two solid lines with distinct slopes m1, m2 ∈ {±1, ±2} that meet at a known integer intersection `(a, b) ∈ [-3, 3]²`; intercepts derived as `b − m·a` so each line passes through the target point. Anchor points at `x = a ± 2` keep the lines visible without depending on the renderer's clip. MC over coordinate strings; distractors are swap `(b, a)`, off-by-one in either coord, and "No solution" as the standard red herring. Prereq overrides: none — both curriculum prereqs (`graph_linear_equation`) are now satisfied. Short labels: "linear?" / "lines meet". Tests: +2 groups @ 300 iterations each, parsing the table to verify the linear/nonlinear classification matches Δy-constant criterion, and reconstructing each line's slope and intercept from the anchor points to verify both pass through the intersection coord. **368 total pass; analyze clean. 196/374 sub-concepts.**
- **Last action (prior):** **Chunk 33 — CoordinatePlane line drawing + graph_linear_equation + informal_line_of_fit (G8)** — extended `CoordinatePlaneSpec` with a `lines: List<CoordinatePlaneLine>` field; each line is defined by two distinct points and a `style` (solid or dashed). The widget now clips each line against the visible plot rect (parametric x-edge / y-edge intersection with corner-dedup) and renders dashed lines with a manual stipple loop. 2 new G8 generators in [graph_line_generators.dart](lib/domain/questions/generators/graph_line_generators.dart): `graph_linear_equation` (draws one solid line with slope m ∈ {±1, ±2} and intercept b ∈ [-3, 3] excluding 0 on a 4-quadrant `[-6, 6]` plane; asks "Which equation matches?" — 4-choice MC over equation strings formatted with the kid-textbook minus U+2212; distractors are sign-flipped slope, sign-flipped intercept, off-by-one slope, and swap-m-and-b), `informal_line_of_fit` (synthesises 8 scatter points along `y ≈ mx + b` and overlays a *dashed* best-fit line at the exact `(m, b)`; asks for the slope of the line — 4-choice MC over `{m, −m, m±1, 0}`). Equation formatter handles all the standard kid-textbook conventions: `1·x → x`, `−1·x → −x`, `b = 0 → omit`, signed b with separated `+`/`−`. Prereq overrides: none needed (`graph_linear_equation`'s `[linear_function_construct]` ✓; `informal_line_of_fit`'s `[scatter_plot_describe, graph_linear_equation]` ✓). **Override cleanup:** removed the `function_definition_check: ['linear_function_construct']` override since `graph_linear_equation` now satisfies the curriculum prereq. Short labels: "graph y=mx+b" / "best fit". Tests: +2 groups @ 300 iterations each, recovering m and b from the line's anchor points (which are deterministic at x = ±1) and asserting all 4 slopes appear across seeds. 366 total pass; analyze clean. **194/374 sub-concepts (52%).**
- **Last action (prior):** **Chunk 32 — Scatter plots reusing CoordinatePlane (no widget change)** — 2 G8 generators in a new file [scatter_plot_generators.dart](lib/domain/questions/generators/scatter_plot_generators.dart). No widget change needed: `CoordinatePlane` already renders points with `label: null` as plain dots, which is exactly what a scatter plot needs. `scatter_plot_construct` shows a table of 5 (x, y) pairs in the prompt + a `CoordinatePlane` with 4 of the 5 points plotted, and asks "Which point is missing?" — MC is 4 coordinate strings (the true missing + 3 misconception distractors: swap, off-by-one-x, off-by-one-y). All x and y values are distinct ints in `[1, 10]` with the additional constraint that no pair has `x == y` (so the swap-distractor is guaranteed to be a different point). `scatter_plot_describe` synthesises 8 points along one of four patterns (Positive association: y ≈ x; Negative association: y ≈ 12−x; No association: random y; Nonlinear V-shape: y ≈ |x−6|+1) and asks the kid to name the pattern; MC is the 4 pattern names. Distractors are the other 3 names. Prereq overrides: none needed (`scatter_plot_construct` prereq `plot_four_quadrants` ✓; `scatter_plot_describe` prereq `scatter_plot_construct` ✓). Short labels: "find pt" / "scatter?". Tests: +2 groups @ 300 iterations each, asserting plotted-points-are-table-subset for construct, and qualitative pattern signals (net up/down trend; V-shape symmetry around mid) for describe. 364 total pass; analyze clean. **192/374 sub-concepts.**
- **Last action (prior):** **Chunk 31 — BoxPlot widget + box_plot / compare_two_distributions** — 1 new widget, 2 generators in [box_plot_generators.dart](lib/domain/questions/generators/box_plot_generators.dart). `BoxPlotSummary` value type carries the five-number summary; `BoxPlotSpec` carries title + axisLabel + list of summaries (1 or 2) + axis range + tickStep. `BoxPlot` widget ([lib/presentation/diagrams/box_plot.dart](lib/presentation/diagrams/box_plot.dart)) renders each summary as a row: row label on the left, whiskers from min to Q1 and Q3 to max with end caps, filled box from Q1 to Q3, a tertiary-coloured median line inside the box; shared horizontal axis at the bottom with tick marks at every `tickStep`. Same `LayoutBuilder` shrink-to-fit pattern. Wired into `DiagramRenderer`. Generators share 3 themed contexts (test scores 0..100 step 10, student heights 50..75 step 5 inches, hours of sleep 4..12 step 1): `box_plot` (G6, draws one summary; question kind drawn uniformly from {median, Q1, Q3, IQR, range}; strict five-number summary so every derived stat is ≥ 1 and unambiguous; misconception distractors are the other quartiles + classic confusions like "added instead of subtracted" / "IQR vs range"), `compare_two_distributions` (G7, draws two stacked summaries A and B; metric drawn from {median, IQR, range}; re-rolls until A's metric differs from B's so the answer is unambiguous; distractors are {other label, "Both are the same", "Cannot tell from a box plot"}). Prereq overrides: none — `box_plot`'s `[median]` and `compare_two_distributions`'s `[describe_distribution]` are both implemented. Short labels: "box plot" / "A vs B". Tests: +2 groups @ 300 iterations each, asserting all 5 box-plot question kinds appear, both A and B win across seeds for the comparison, and the asked metric always differs between groups. 362 total pass; analyze clean. **190/374 sub-concepts, 10/27 widgets.** — 1 new widget, 2 generators in a new file [histogram_generators.dart](lib/domain/questions/generators/histogram_generators.dart). `HistogramSpec` value type carries `title` + `axisLabel` + `counts` + `binStart` + `binWidth` + `scale` + `maxY`. `Histogram` widget ([lib/presentation/diagrams/histogram.dart](lib/presentation/diagrams/histogram.dart)) is a `CustomPainter` distinct from `BarChart`: bars are *adjacent* (no gap, continuous x-axis) and tick labels fall on bin boundaries rather than between them — including a final right-edge label closing the last bin. Same `LayoutBuilder` shrink-to-fit pattern. Wired into `DiagramRenderer`. Generators share 3 themed contexts (test scores binWidth 10, student heights binWidth 2 inches, daily high temperatures binWidth 5 °F): `histogram` (G6, "How many students scored between 70 and 80?" — pick one bin, answer = its count; pairwise-distinct bin counts so the wrong-bin misconception distractors are unambiguous), `describe_distribution` (G6, "Which best describes the shape of this distribution?" — 4-choice MC over Symmetric / Skewed left / Skewed right / Uniform; counts built deterministically per shape with small jitter to keep each instance visibly recognizable while still varying across seeds). Prereq overrides: `histogram` needs none (`bar_graph_read` already met); `describe_distribution: ['histogram']` drops `box_plot` (no generator yet — the histogram alone is enough scaffolding for shape recognition). Short labels: "histogram" / "shape?". **Tooling:** added `"Histogram": "histogram"` to `sync_implementation_status.py`; split the BarChart/Histogram bundled row in [curriculum.md §6.1](curriculum.md) into two rows so markers track separately (widget denominator went 26 → 27). Tests: +2 groups @ 300 iterations each, asserting bin-boundary parsing, count-distinctness, and qualitative shape invariants per category. 360 total pass; `flutter analyze` clean. 188/374 sub-concepts.
- **Last action (prior):** **Chunk 30 — Histogram widget + histogram / describe_distribution** — 1 widget, 2 generators (test scores / heights / temperatures themes); `histogram` (G6, "How many students scored between 70 and 80?") and `describe_distribution` (G6, Symmetric / Skewed left / Skewed right / Uniform shape recognition). Prereq override: `describe_distribution` drops `box_plot` (since landed in Chunk 31). Curriculum row splits: BarChart and Histogram each have their own §6.1 row.
- **Last action (prior):** **Chunk 29 — Fractional line plots (G4–G5)** — extended `DotPlotSpec` with a `denominator` field (default 1; 2 or 4 in this chunk) so the widget can render line plots at halves or quarters of a unit. Internal `values` / `minX` / `maxX` are in units of `1/denominator`; major ticks (whole-number positions) get a longer tick + numeric label; minor subdivisions get a short tick only (kids count the subdivisions themselves — matches textbook style). 3 new generators in [dot_plot_generators.dart](lib/domain/questions/generators/dot_plot_generators.dart), sharing 3 fractional themes (pencil / ribbon / leaf lengths): `line_plot_fractional` (G4, "How many pencils are 3 1/4 inches long?" — count dots above one fractional tick; values land in `[denom, maxDisplay·denom]` internal units; asked value is one that occurs ≥ 1 time; misconception distractors are neighbour-tick counts ± 1 subdivision plus the modal-stack count), `line_plot_fraction_word` (G4, "What is the total length of all pencils that are 1 1/4 inches?" — `count × value` as a mixed number; asked value forced to be a *true* fraction (not whole-inch) so the "just gave the count" distractor can't accidentally equal the correct total; distractors include (count±1)·value and count·(value±subdivision)), `line_plot_5th_grade_ops` (G5, "What is the difference between the longest and shortest pencil length?" — `max − min` as a mixed number; re-rolls until the range is ≥ 1 inch; distractors include max+min, max alone, min alone, max−secondSmallest, and range±subdivision). Both fraction-answer generators use `AnswerFormat.mixedNumber` (default `AnswerShape.any` accepts equivalent forms). New `_distinctStringDistractors` helper throws if the candidate pool exhausts (loud failure rather than `option-N` junk strings) — verified safe across 300 iterations per generator. Prereq override: `line_plot_fractional: ['line_plot_whole', 'fraction_a_over_b']` drops curriculum's `partition_halves_fourths` (no generator) and substitutes `fraction_a_over_b` for the "what is 1/4" intuition. Short labels: "line ½/¼" / "line × frac" / "line range". Tests: +3 groups @ 300 iterations each, asserting denom variety, fraction-only asked values, range constraint, and answer correctness via `Fraction.toMixed()`. **358 total pass; `flutter analyze` clean. 50% sub-concept coverage hit (186 / 374).**
- **Last action (prior):** **Chunk 28 — DotPlot widget + line-plot / dot-plot readers** — 1 new widget, 2 generators in a new file `dot_plot_generators.dart`. `DotPlotSpec` value type ([diagram_spec.dart](lib/domain/questions/diagram_spec.dart)) carries `title` + `axisLabel` + a flat `values: List<int>` (duplicates represent multiple data points at the same value) + `minX` / `maxX`. `DotPlot` widget ([lib/presentation/diagrams/dot_plot.dart](lib/presentation/diagrams/dot_plot.dart)) is a `CustomPainter`: title up top, horizontal axis with one tick per integer in range, dots stacked above each tick (dot count = frequency at that value). Same `LayoutBuilder` shrink-to-fit pattern as the other diagrams; plot height auto-grows to fit the tallest dot stack. Wired into `DiagramRenderer`. Generators share 3 themed contexts (plant heights / books read / pets per family) and lean on per-theme prompt builders (`exactPrompt`, `atLeastPrompt`): `line_plot_whole` (G2, "How many plants are 6 inches tall?" — count dots above one tick; values in [1, 8], n ∈ [10, 15]; asked value guaranteed to occur so answer is always ≥ 1; misconception distractors include neighbour-column counts and the modal-column count "kid read the tallest stack"), `dot_plot` (G6, "How many plants are at least 6 inches tall?" — threshold filter rightward; values in [1, 9]; re-rolls until `1 ≤ answer ≤ n − 1` so the question isn't trivial; primary misconception distractor is the strictly-greater-than count "forgot 'at least'"; secondary is the at-most count "read the wrong direction"). Prereq override: `line_plot_whole: ['add_within_20']` substitutes for unimplemented `measure_with_ruler_inches` (would need a Ruler widget); curriculum's intended `count_to_100_by_1` would be a closer fit but has no generator either, so add_within_20 stands in as basic count-up-to-15 background. `dot_plot`'s prereq is `[line_plot_whole]` — implemented, no override needed. Short labels: "line plot" / "dot plot". **Tooling:** added `"DotPlot": "dot_plot"` to `sync_implementation_status.py`'s WIDGET_TO_FILE dict; split the bundled `BarChart / Histogram / DotPlot / LinePlot` row in [curriculum.md §6.1](curriculum.md) into two rows (BarChart+Histogram / DotPlot+LinePlot) so the ✅ markers track separately — denominator went 25 → 26. Tests: +2 groups @ 300 iterations each. 355 total pass; `flutter analyze` clean.
- **Last action (prior):** **Chunk 27 — BarChart widget + bar-graph readers** — 1 new widget, 3 generators in a new file `bar_graph_generators.dart`. `BarChartSpec` value type ([diagram_spec.dart](lib/domain/questions/diagram_spec.dart)) carries `title` + parallel `labels` / `values` lists + `scale` (y-axis gridline spacing) + `maxY`; the spec asserts `values.length == labels.length`, `maxY % scale == 0`. `BarChart` widget ([lib/presentation/diagrams/bar_chart.dart](lib/presentation/diagrams/bar_chart.dart)) is a `CustomPainter` that renders a title up top, a bold left axis with tick labels at every multiple of `scale`, horizontal gridlines, coloured bars per category, and category labels below each bar. Uses the same `LayoutBuilder` shrink-to-fit pattern as `CoordinatePlane` (scales `barWidth` and `barGap` together when the parent is narrower than the natural width). Wired into `DiagramRenderer`. Generators all reuse a shared pool of 5 themed contexts (favorite fruit / color / pet / sport + city-builder "Materials at the site" with bricks, paint cans, cones, signs): `bar_graph_read` (G2, "How many kids like apples?" with scale=1; 4 pairwise-distinct values in [1, 10]; distractors are the other three bar values to bait "read the wrong bar"), `bar_graph_compare` (G2, "How many more X than Y?" with re-rolled re-roll for a positive gap of ≥ 2 so the answer isn't trivially 1; misconception distractors include sum-instead-of-difference, individual bar values, difference of the wrong pair), `scaled_bar_graph_read` (G3, scale ∈ {2, 5, 10}; multipliers 1..7 are distinct; primary misconception distractor is "forgot to scale → gave gridline count", secondary is `multiplier × scale²`). Prereq override: `bar_graph_read: ['add_within_100']` drops `picture_graph_read` (no generator yet — picture-graph rendering needs an emoji/icon variant that's a follow-up). `bar_graph_compare` and `scaled_bar_graph_read` need no override (curriculum prereqs already met by `bar_graph_read` + `sub_within_100` / `mult_facts_within_100`). Short labels: "bar graph" / "bar diff" / "bar scaled". Tests: +3 groups @ 300 iterations each (theme & ask-index variety asserted, scale-multiple constraint, gap ≥ 2 constraint, "forgot scale" distractor presence). 353 total pass; `flutter analyze` clean.
- **Last action (prior):** **Chunk 26 — Distance generators on the CoordinatePlane (no widget extension)** — 2 generators reusing the Chunk-25 widget unchanged. `coord_distance_same_line` (G6) — "What is the distance from A to B?" with two labelled points sharing one coordinate (50% same x, 50% same y); answer is `|Δ|` on the differing axis, ∈ [2, 8]. Test asserts both same-x and same-y cases appear across seeds. `pythagorean_distance_coords` (G8) — same prompt shape, two points whose Δ-vector is the legs of a Pythagorean triple restricted to (3, 4, 5) and (6, 8, 10) so the answer is always integer. All 8 orientations covered (4 quadrants × 2 leg swaps); endpoints constrained to stay in `[-8, 8]`. Misconception distractors include `a² + b²` (forgot sqrt), `a + b` (added legs), and one of the legs. Prereq override added: `pythagorean_distance_coords: ['pythagorean_apply_2d', 'coord_distance_same_line']` — drops the unimplemented `polygon_on_coordinate_plane` and substitutes the just-implemented `coord_distance_same_line` for "compute distance from coords" intuition. Tests: +2 groups @ 300 iterations each. 350 total pass; `flutter analyze` clean. Caught a Dart-set-equality gotcha in the test (`{8, 6} == {6, 8}` is `false` for default `LinkedHashSet` since equality is by identity — switched to sorted-list comparison).
- **Last action (prior):** **Chunk 25 — CoordinatePlane widget + Q1/Q4 plot/read generators** — 1 widget, 3 generators, 1 build-tooling fix. New `CoordinatePlaneSpec` value type ([diagram_spec.dart](lib/domain/questions/diagram_spec.dart)) carries integer ranges + a list of optionally-labelled `CoordinatePlanePoint`s. `CoordinatePlane` widget ([lib/presentation/diagrams/coordinate_plane.dart](lib/presentation/diagrams/coordinate_plane.dart)) is a `CustomPainter` that renders the integer grid, bolds whichever axes are visible (0 inside the range), tick-labels every integer (skipping 0 to avoid overlap), and plots dots with letter labels offset above-right of each point. Wired into `DiagramRenderer`. Generators in new file `coordinate_plane_generators.dart` (`plot_first_quadrant` G5: "Which point is at (3, 5)?" → letter MC with diagram of 4 candidate points whose distractor locations bait swap-coords + off-by-one-in-x + off-by-one-in-y misconceptions; `read_first_quadrant` G5: "What are the coordinates of point A?" → coord-string MC with the same distractor strategy; `plot_four_quadrants` G6: same shape as Q1 plot but x, y ∈ [-5, 5] excluding 0 — test asserts all four quadrants appear across seeds). Prereq overrides for the two roots: `plot_first_quadrant: ['add_within_100']` (drops unimplemented `number_line_add_sub`); `plot_four_quadrants: ['plot_first_quadrant', 'opposites_and_zero']` (drops unimplemented `integers_on_number_line`, substitutes `opposites_and_zero` which carries the "negatives on a number line" intuition). **Two tooling fixes landed alongside:** (1) `build_catalog.dart` parser now strips the `✅` implemented-marker prefix that `sync_implementation_status.py` writes into curriculum.md — previously regenerating the catalog would fail validation; (2) `build_catalog.dart` now runs `dart format` on its generated output so the lint suite stays clean (a `prereqIds` list with 3+ entries previously landed on one line and tripped the 80-col lint). Tests: +3 groups @ 300 iterations each verifying prompt parses, diagram spec matches, correct label points to (x, y), all 4 quadrants exercised. 348 total pass; `flutter analyze` clean.
- **Last action (prior):** **Chunk 24 — G8 functions (no widget)** — 2 generators completing the no-widget tail of the G8 function/equation family. `solve_linear_eq_no_or_inf` (G8, `equation_generators.dart`) classifies `ax + b = cx + d` into "One solution" (a ≠ c), "No solution" (a = c, b ≠ d), or "Infinitely many solutions" (a = c, b = d), drawn uniformly across the three outcomes; MC includes a "Two solutions" misconception distractor as the fourth choice. `compare_functions_representations` (G8, `function_generators.dart`) presents two linear functions f and g in *distinct* randomly-chosen representations — equation `y = mx + b`, two points `(1, m+b)/(3, 3m+b)`, or table `x→y {(0,b), (1,m+b), (2,2m+b)}` — and asks which has the greater rate of change. Slopes restricted to distinct positive integers in [2, 6] so "greater rate of change" is unambiguous at G8. Both prereq chains already lived (`solve_linear_eq_one_solution` and `function_definition_check`), so no new prereq overrides. Tests: +2 groups @ 300 iterations each, both asserting all answer/representation classes appear across seeds. 345 total pass; `flutter analyze` clean. **The pure-algorithmic G8 function/equation family is now complete** — the remaining G8 row (`graph_linear_equation`, `qualitative_graph_features`, `solve_system_by_graphing`, `identify_linear_vs_nonlinear`) requires the `CoordinatePlane` widget.
- **Last action (prior):** **Chunk 23 — circles + 3D volumes (π = 3.14)** — 5 generators in `geometry_generators.dart`. **Design call locked: π is approximated as 3.14** (CCSS K–8 standard); generators emit answers as `Decimal` with parameters constrained so the exact `3.14 × …` computation terminates at ≤ 2 decimal places. Coefficient-of-π form (`10π`) is high-school-and-up — out of scope for v1. Generators: `circle_circumference` (G7, "Use π ≈ 3.14"; r ∈ [1,12]; misconception distractors include "treated diameter as circumference" πr, "confused with area" πr², and "dropped π" 2r), `area_circle` (G7; misconceptions: 2πr, 4πr² from d², πr), `volume_cylinder` (G8, V = πr²h; misconceptions: lateral surface 2πrh, cone formula (1/3)πr²h gated on r²·h divisible by 3, area without h), `volume_cone` (G8, V = (1/3)πr²h; re-rolls until r²·h is divisible by 3 so the answer terminates; misconceptions: cylinder formula, half instead of third), `volume_sphere` (G8, V = (4/3)πr³; restricts r ∈ {3,6,9} since 4 × 314 ≡ 2 mod 3, so r³ must absorb the factor of 3; misconceptions: forgot 4/3, forgot the cube, forgot ÷3). All five concepts' curriculum prereqs are now met by registered generators — no overrides needed. Tests: +5 groups @ 300 iterations each. 343 total pass; `flutter analyze` + `dart format` clean.
- **Last action (prior):** **Chunk 22 — angle relationships (G7–G8)** — 5 generators in `geometry_generators.dart`, all text-only following the geometry convention (the catalog marks them `algorithmicWithDiagram` but the math reads cleanly without the `Angle` widget). `supplementary_angles` (G7, "Two angles are supplementary. One is 65°. What is the other?" → 115°; excludes a=90 to avoid duplicate-answer cases; misconception distractor "used complementary 90−a" only added when a < 90). `complementary_angles` (G7, "One is 35°. Other?" → 55°; excludes a=45; primary misconception is "used supplementary 180−a"). `vertical_angles` (G7, two intersecting lines; 50% asks for the vertical angle (= a), 50% for an adjacent angle (= 180−a); test asserts both modes are covered across seeds). `triangle_angle_sum` (G8, "Triangle has angles 35° and 75°. Third?" → 70°; re-rolls until a+b ∈ [20, 170] so c stays positive and meaningful). `parallel_lines_transversal` (G8, four relationship types uniformly sampled — corresponding / alternate interior / alternate exterior / co-interior; the first three return a, co-interior returns 180−a; test asserts all four relationship strings appear). Prereq overrides: `supplementary_angles` and `complementary_angles` substitute `add_within_100` for `right_acute_obtuse_angle` (no generator yet — needs Angle diagram). Tests: +5 groups @ 300 iterations each. 338 total pass; `flutter analyze` clean.
- **Last action (prior):** **Chunk 21 — G8 linear-equation & function family** — 6 generators across two files. `solve_linear_eq_with_distrib_collect`, `solve_system_substitution`, `solve_system_elimination`, `slope_from_two_points`, `linear_function_construct`, `function_definition_check`. Two prereq overrides (slope_from_two_points, function_definition_check) drop the CoordinatePlane-requiring prereqs. Commit `c67e36b`.
- **Last action (prior):** **Chunk 20 — distributive GCF + theoretical-vs-experimental** — 2 generators plus one DAG cleanup. Number theory: `distributive_with_gcf` (G6, "Factor out the GCF: 12 + 18" → "6(2 + 3)" string MC; reuses `gcf_two_numbers`'s `g·p / g·q` parametrisation with coprime cofactors so the inside parens has no further common factor). Probability: `theoretical_vs_experimental` (G7, dual-shape parametrised generator over 4 fair-device scenarios — coin/die/4-spin/5-spin. Same scenario in both shapes; question asks for either THEORETICAL or EXPERIMENTAL P. Observed count forced away from expected so the two answers visibly differ — the *other* probability is the primary misconception distractor). Override cleanup: removed `factor_linear_expression: ['add_subtract_linear_expressions']` from the curriculum parser since `distributive_with_gcf` (the second curriculum.md prereq) now lives — `factor_linear_expression` once again uses its full curriculum prereq list. Tests: +2 groups @ 300 iterations each.
- **Last action (prior):** **Chunk 19 — inequalities + cross-category aliases** — 5 generators. Commit `4a44b43`.
- **Last action (prior):** **Chunk 18 — Pythagorean + late-grade cleanup** — 5 generators. Commit `ae795d3`.
- **Last action (prior):** **#7 Decimals sub-slice (chunk 3) — percent intro** — 3 new generators in a new file `percent_generators.dart` plus a new diagram widget:
  - **`PercentGridSpec` + `PercentGrid` widget** ([lib/presentation/diagrams/percent_grid.dart](lib/presentation/diagrams/percent_grid.dart)) — 10×10 grid with N cells shaded row-major. Distinct from `AreaGrid` (which is shaped as a row × col overlap rectangle for fraction × fraction). Wired into `DiagramRenderer`.
  - **`percent_intro` (G6)** — "What percent is shaded?" + grid diagram. Excludes 0/50/100 so the kid actually has to count. Misconception distractors: 100−n (swapped shaded/unshaded), n÷10 (read as out-of-10).
  - **`percent_of_quantity` (G6)** — "What is 25% of 80?" Parameters are drawn from two templates that both guarantee integer results: friendly percents {10,20,25,30,40,50,60,70,75,80,90} with quantity divisible by 100/gcd(p,100), OR any percent with quantity a multiple of 100. Misconception distractors: forgot the ÷100, swapped percent and quantity, "the rest" (quantity − answer).
  - **`find_whole_from_part_percent` (G6)** — natural inverse: "$part is $percent% of what number?" Same parameter discipline as `percent_of_quantity`.
  - **Prereq override:** `percent_intro: []` drops the unimplemented `fraction_denom_10_100` prereq — the grid widget itself visualises "N out of 100" directly, so the kid doesn't need the tenths/hundredths fraction-bar background.
  - **No keypad changes:** answers are bare integers (no `%` symbol), so the existing digit-only keypad just works.
  - **Tests:** +3 (one group per generator @ 300 iterations; 252 total). `flutter analyze` clean.
- **Last action (prior):** **#7 Decimals sub-slice (chunk 2)** — 7 new generators in `decimal_generators.dart`: `decimal_to_thousandths_read` (G5), `compare_decimals_thousandths` (G5), `round_decimals` (G5, half-away-from-zero to whole/tenth/hundredth), `div_decimal_by_whole` (G5, generated as quotient × divisor for exact results), `div_by_decimal` (G6, quotient always a whole int 2..20), `decimal_to_fraction` (G5, AnswerFormat.fraction + exactString since lesson is "in lowest terms"; re-rolls to require non-trivial reduction), `fraction_to_decimal` (G6, AnswerFormat.decimal; denominators restricted to the terminating set {2, 4, 5, 8, 10, 20, 25, 50}). New helper `_wholeDistractors` for integer-answer distractor sets that need dedup-and-fallback. Commit `d8b7b20`.
- **Last action (prior x2):** **#7 Decimals sub-slice (chunk 1)** — 7 new generators in a new file `lib/domain/questions/generators/decimal_generators.dart`:
  - **Notation (G4):** `decimal_notation_tenths`, `decimal_notation_hundredths` — "Write N tenths/hundredths as a decimal." Misconception distractors include the classic "0.7 vs 0.07" shift error.
  - **Compare (G4):** `compare_decimals_hundredths` — "Which is bigger: 0.45 or 0.5?" Matches the `compare_fractions_*` shape rather than `<`/`=`/`>` (sidesteps keypad-with-comparison-symbols UX). 40% of pairs are tenths-vs-hundredths to bait the "longer = bigger" misconception.
  - **Arithmetic (G5):** `add_decimals`, `sub_decimals`, `mult_decimal_by_whole`, `mult_decimals`. Subtraction generated as `(a+b) − b` to keep results ≥ 0. Multiplication generators re-roll if a picked operand canonicalises to a whole number, so the lesson is always genuinely decimal × {whole|decimal}.
  - **New value type:** `lib/domain/questions/decimal.dart` — scaled-integer internal representation (no `double`, so `0.1 + 0.2` is exactly `0.3`). Canonical form strips trailing zeros (`0.5`, not `0.50`). Mirrors the `Fraction` pattern — `equalsByValue` instead of overriding `==`, no `meta` dep needed.
  - **AnswerFormat.decimal** added; answer-checker accepts equivalent forms via `Decimal.equalsByValue` (so a player typing `1.50` matches canonical `1.5`).
  - **Keypad:** no changes needed — the existing `_extraCharsFor(answer)` derives the `.` button automatically from the canonical answer string.
  - **DAG cleanup:** removed the `rationals_add_sub` / `rationals_multiply_divide` prereq overrides — `add_decimals` / `mult_decimals` now exist, so the rationals prereqs match curriculum.md again. Added a `decimal_notation_tenths` override that drops the unimplemented `fraction_denom_10_100` prereq (so this slice's entry point into the decimals branch reaches kids).
  - **Tests:** +20 (Decimal value-type 14 + decimal generators 6 groups @ 300 iterations each). All 259 pass; `flutter analyze` clean.
- **Next action:** Pick the next sub-slice. What's still missing ([curriculum.md §5.1](curriculum.md)):
  - **Coordinate plane (next steps)** — `plot_*_quadrant` + `read_first_quadrant` landed in Chunk 25, `coord_distance_same_line` + `pythagorean_distance_coords` landed in Chunk 26. Still missing using just the existing widget: `ratio_to_coordinate_pairs` (G6 — plot the ratio 3:2 as ordered pairs), `dependent_independent_vars` (G6 — text-only OK, no diagram strictly needed). Needs widget extension (polygon support): `polygon_on_coordinate_plane`. Existing text-only generators `slope_from_two_points` and `linear_function_construct` (Chunk 21) could be upgraded to render via `CoordinatePlane` for richer UX, but that's polish, not coverage.
  - **Transformations (G8)** — `translate`, `reflect`, `rotate`, `dilate`. `CoordinatePlane` exists; needs polygon support (multi-point + edges + a second polygon to show the image).
  - **Data displays** — `BarChart` landed in Chunk 27, `DotPlot` in Chunk 28 (with `line_plot_whole` G2 + `dot_plot` G6). Natural follow-ups using existing widgets: extend `DotPlotSpec` with fractional positions to unlock `line_plot_fractional` G4, `line_plot_fraction_word` G4, `line_plot_5th_grade_ops` G5. `picture_graph_read` + `scaled_picture_graph` (emoji/icon variant — extend BarChart with "stacked icons" mode or sibling `PictureGraph` widget). `histogram` (extends BarChart with adjacent bars + numeric bin labels — likely `HistogramSpec` sibling). Still needs its own widget: `box_plot` / `compare_two_distributions` (`BoxPlot`), `scatter_plot_*` (`ScatterPlot` — could reuse `CoordinatePlane` with non-labelled points + larger range).
  - **Compound probability** — `tree_diagram`, `compound_event_probability`. Needs a `TreeDiagram` widget.
  - **3D volume w/ π** and **circles** landed in Chunk 23: `circle_circumference`, `area_circle`, `volume_cylinder`, `volume_cone`, `volume_sphere`. π = 3.14 convention is locked.
  - Angle relationships landed in Chunk 22: `supplementary_angles`, `complementary_angles`, `vertical_angles`, `triangle_angle_sum`, `parallel_lines_transversal`.
  - The remaining G8 function/equation row is now all widget-gated: `graph_linear_equation`, `qualitative_graph_features`, `solve_system_by_graphing`, `identify_linear_vs_nonlinear` all need `CoordinatePlane`. The Chunk 21 chain (`slope_from_two_points`, `linear_function_construct`, `function_definition_check`, `solve_linear_eq_with_distrib_collect`, `solve_system_*`) plus Chunk 24 (`solve_linear_eq_no_or_inf`, `compare_functions_representations`) close out the no-widget G8 work.
  - **Multiplication word problems** — needs concept-ID design call (curriculum.md doesn't pre-name them — see Phase 6 task list).
- **Deferred:** Audio SFX + background music (CC0 assets not sourced yet — stub in place). iOS verification. Both revisit before Phase 11 at latest.

---

## Locked Decisions

| Area | Choice | Rationale |
|---|---|---|
| **Framework** | Flutter (stable channel) + [Flame](https://flame-engine.org/) game engine | Single codebase for iOS+Android; Flame actively maintained in 2026 (Flame Game Jam 2026 ran in March); BSD-licensed; provides sprite/animation/gesture primitives we need for the spin wheel and avatar |
| **Language** | Dart 3.x | Required by Flutter |
| **State management** | [Riverpod](https://riverpod.dev/) 3.x | Recommended default for new Flutter projects in 2026; compile-time safety, low boilerplate, great testability |
| **Local persistence** | [Drift](https://pub.dev/packages/drift) (SQLite + compile-time-safe queries) | Best-supported in 2026 (Hive and Isar are now community-maintained after author stepped away); SQL is great for filtering questions by concept+difficulty band; predictable migrations |
| **Audio** | [`flame_audio`](https://pub.dev/packages/flame_audio) (wrapper over `audioplayers`) | Natural fit with Flame; supports SFX pools and background music |
| **Cloud save** | [`games_services`](https://pub.dev/packages/games_services) package — Google Play Games (Android) + Game Center / iCloud (iOS) | Only solution that satisfies the PRD's "no custom server" requirement on both platforms with a single API. Last updated Dec 2025 |
| **Concept granularity** | Track proficiency at **sub-concept** level (e.g. "2-digit addition with carry"), not at category level. Roll up to category for display only | Otherwise the adaptive wheel is too coarse: a kid who's mastered single-digit addition would falsely look ready for multi-digit. See PRD's Concept System section for the category→concept taxonomy |
| **Cosmetics economy** | Single sink for stars: **city builder only**. Avatar is free to customize and not tied to the economy | Phase 4 spike found no Flutter avatar library combines full-body rendering with rich accessory slots — building hat/costume/shoes/backpack overlays on top of a bust-only library was more work than the variety it would buy. City builder alone is plenty of long-arc motivation |
| **Avatar rendering** | **DiceBear Adventurer** style, rendered **fully offline** via local SVG composition using `flutter_svg`. The DiceBear Adventurer SVG component paths (MIT-licensed code, CC BY 4.0 design by Lisa Wischofsky) are bundled in-app as Dart string constants; the composer assembles them at runtime. Player picks from a curated subset of slots (hair style/color, skin tone, eyes, mouth, glasses, earrings, blush/freckles); free to re-edit anytime | Cleanest off-the-shelf look for kids. Fully offline — no network calls, no INTERNET permission needed. `dice_bear` pub package rejected because it calls `api.dicebear.com` at runtime — a showstopper for a kids app that must work without WiFi |
| **City rendering** | Isometric tiles, fixed grid, **auto-generated roads** between buildings | Mobile-friendly: tile-snap + auto-roads avoid fiddly placement; established CC0 isometric packs (e.g. Kenney City Kit) cover the style |
| **Milestones** | **Removed.** Replaced by per-item star prices and total-stars-earned thresholds for unlocking new building types and themed maps | Milestones were dead weight once the city builder provides natural long-arc progression — every land expansion or new building tier *is* a milestone moment |
| **Curriculum standard** | US Common Core State Standards (CCSS) for Mathematics, K–8 | Most thoroughly documented free K–8 standard; aligned to most public datasets; the resulting taxonomy stays standard-agnostic enough to work for kids on UK/IB/other systems. Full taxonomy in [curriculum.md](curriculum.md) |
| **Diagram strategy** | Mostly procedural SVG / `CustomPainter` widgets, parameterized by question params. Text-only fallback where rendering cost is prohibitive (3D nets, long-division layouts) | A small set of 8 high-ROI widgets (FractionBar, NumberLine, Clock, BarChart, RectangleArea, CoordinatePlane, Angle, Spinner) unlocks ~70% of non-arithmetic K–8 content. Cleaner than bundled images; works at any size; fits the "feels infinite" goal |
| **Question source mix** | ~85% algorithmic (with procedural diagrams) + ~10% bundled curated datasets + ~5% deferred. **No runtime LLM calls; no offline LLM batch generation in v1.** | Generators store compactly and feel limitless. Datasets fill rich-word-problem gaps where templates produce nonsense. Offline LLM batch is deferred to see how far the first two get us. See [curriculum.md §8](curriculum.md) |
| **Curriculum reference** | [curriculum.md](curriculum.md) is the canonical source of the concept catalog (taxonomy, prereq DAG, source strategy per sub-concept, diagram widget catalog, dataset inventory). plan.md only references it | Avoids duplication; keeps plan.md focused on phase execution |
| **Repo plan doc** | `plan.md` at repo root | Simple, greppable, lives next to `prd.md` |
| **AI agent doc** | `CLAUDE.md` at repo root | Emerging convention; auto-loaded by Claude Code each session |

---

## Architecture Overview

Four layers, top to bottom:

1. **Presentation (Flutter widgets)** — screens, navigation, forms (player creation, avatar editor, progress screen, settings, city screen).
2. **Game (Flame components)** — spin wheel, question presentation, animations, audio cues, isometric city renderer.
3. **Domain (pure Dart)** — game rules: concept-band classification, proficiency updates, wheel selection logic, star math, city growth model. **No Flutter or Flame imports here** — keeps it unit-testable and portable.
4. **Data (Drift + cloud-save)** — local SQLite for player profiles, proficiency records, owned items; question catalog as a read-only seeded table; cloud-save bridge for backup/restore.

State management (Riverpod) sits at the boundary between presentation and domain — providers expose domain objects to widgets reactively.

---

## Data Model (sketch — refined per phase as we go)

```
Player
  id, name, gradeLevel, createdAt
  avatarConfig          // JSON: DiceBear Adventurer slot picks (hair, skin, eyes, mouth, glasses, etc.)
  currentStars          // spendable balance
  lifetimeStarsEarned   // never decreases — drives progressive unlocks
  currentStreak, lastPlayedDate

ConceptProficiency
  playerId, conceptId, proficiency (0.0–1.0), lastUpdatedAt
  questionsAnswered, questionsCorrect

Concept (static catalog) — sub-concept granularity (e.g. "2-digit addition with carry")
  id, name, categoryId, gradeRange, description

ConceptCategory (static catalog) — display grouping only (e.g. "Addition & subtraction")
  id, name, displayOrder

Question (static catalog for non-arithmetic concepts; arithmetic generated at runtime)
  id, conceptId, difficultyBand (comfortable | challenging),
  prompt (text or template), correctAnswer,
  distractors (for multiple-choice), explanation (for wrong-answer screen)
  source (algorithmic | curated | ai_generated), license

// --- City builder (Phase 7+) ---

City (per player, per map)
  id, playerId, mapId,
  gridWidth, gridHeight,         // grows on land expansion (Phase 8)
  population

CityMap (static catalog)
  id, name, theme (countryside | city | futuristic | …),
  baseGridWidth, baseGridHeight,
  starCostToUnlock (0 for the beginner map),
  terrainSeed                    // deterministic terrain layout

BuildingType (static catalog)
  id, name, category (residential | services | utilities | commercial),
  starCost,
  unlockedAtLifetimeStars,
  populationContribution,         // residents this building can house, if any
  serviceProvision,               // map of {school: N, hospital: N, power: N, …}
  maxTier, assetRefByTier

BuildingPlacement
  id, cityId, buildingTypeId, currentTier, gridX, gridY, placedAt

GameSession (in-memory only)
  startedAt, players (list), roundsPlayed
  // Streak lives on Player, not here
```

**Notes:**
- `Item`, `Milestone`, and `AvatarAccessory` from earlier sketches are removed. The only star sink is the city builder; the avatar is free to customize.
- `currentStars` vs. `lifetimeStarsEarned`: spending stars decreases `currentStars`; both correct *and* spent stars count toward `lifetimeStarsEarned` for unlock gating (so spending doesn't lock players out of progression).
- `serviceProvision` lets the city growth model use simple aggregate ratios (e.g. "1 school per 50 residents") rather than per-building dependency graphs.

---

## Domain Specs (set during Phase 0)

### Initial concept scope for Phase 1

Phase 1 ships **two concepts**, both pure single-digit arithmetic:

| Concept ID | Description | Operand range | Result range |
|---|---|---|---|
| `add_1digit` | Single-digit addition | a, b ∈ [0, 9] | sum ∈ [0, 18] |
| `sub_1digit` | Single-digit subtraction (no negatives) | minuend ∈ [0, 18], subtrahend ∈ [0, 9], a ≥ b | diff ∈ [0, 18] |

Both are algorithmically generated at runtime (no curated dataset needed). Distractors for multiple-choice are constructed from common mistakes: off-by-one (±1), swapped operands, and a randomly-chosen value within ±5 of the correct answer.

Why these two: universally familiar across the entire 6–14 target age, trivially generatable, and two-concepts-on-the-wheel is enough to make the spin feel like a real choice without needing the full catalog.

### Phase 2 concepts

| Concept ID | Description | Operand range | Result range | Notes |
|---|---|---|---|---|
| `mul_1digit` | Single-digit multiplication | a, b ∈ [1, 9] | product ∈ [1, 81] | Skip ×0 and ×1 as trivial |
| `div_1digit` | Single-digit division (exact) | divisor ∈ [2, 9], quotient ∈ [1, 9] | quotient ∈ [1, 9] | Generated as quotient×divisor=dividend; no remainders |
| `add_2digit` | 2-digit addition | a, b ∈ [10, 99] | sum ∈ [20, 198] | |
| `sub_2digit` | 2-digit subtraction (no negatives) | minuend ∈ [10, 99], subtrahend ∈ [10, 99], a ≥ b | diff ∈ [0, 89] | |

All four are algorithmically generated at runtime. Distractors use the same strategy as Phase 1 (off-by-one, operand swap, random ±5).

Fractions, geometry, and word problems are introduced in Phase 5/6 (Curriculum & Question Bank — see [curriculum.md](curriculum.md)).

### Proficiency update formula (sketch — refine in Phase 2)

Proficiency `p` per (player, concept) lives in [0.0, 1.0]. After each answer:

```
p_new = clamp(p_old + α · (target - p_old), 0.0, 1.0)
```

where `target = 1.0` on correct, `target = 0.0` on wrong, and `α` is a learning rate. Proposed initial `α = 0.1`.

Properties of this update:
- Stable: one wrong answer can't tank a player's score
- Asymptotic toward target — quick at first, slows near 0 or 1
- O(1), no history needed
- Easy to unit-test (deterministic, monotonic)

**Band thresholds (initial, tunable in Phase 2):**

| p range | Band | Action |
|---|---|---|
| `p < 0.2` | not yet | Excluded from wheel |
| `0.2 ≤ p < 0.5` | challenging | On wheel; correct = 5 stars; multiple choice |
| `0.5 ≤ p < 0.85` | comfortable | On wheel; correct = 3 stars; typed input |
| `p ≥ 0.85` | mastered | Excluded from wheel |

**Initial value** when a player first encounters a concept:
- Concept grade ≤ player's stated grade: start at `p = 0.4` (challenging band)
- Concept grade > player's stated grade: start at `p = 0.05` (not yet, off the wheel)

Open Phase 2 knobs: tune `α`, asymmetric reward/penalty (e.g. wrong answers move p down faster than right answers move it up), threshold values, and whether to consider time-since-last-attempt (proficiency decays if not practiced).

---

## Project Structure (planned)

```
math_city/
├── prd.md
├── plan.md
├── CLAUDE.md
├── pubspec.yaml
├── lib/
│   ├── main.dart
│   ├── app.dart
│   ├── presentation/      # Flutter widgets: screens, navigation
│   │   ├── home/
│   │   ├── player/        # includes avatar editor (DiceBear slot pickers)
│   │   ├── city/          # city-builder screen (Phase 7+)
│   │   ├── progress/
│   │   └── settings/
│   ├── game/              # Flame components
│   │   ├── spin_wheel/
│   │   ├── city/          # isometric city renderer (Phase 7+)
│   │   ├── question_view/
│   │   └── effects/
│   ├── domain/            # pure Dart: rules, no Flutter imports
│   │   ├── concepts/
│   │   ├── proficiency/
│   │   ├── questions/
│   │   ├── stars/
│   │   ├── avatar/        # AdventurerConfig + curated slot catalogs
│   │   └── city/          # buildings, growth model, population math (Phase 7+)
│   ├── data/              # Drift schema, repositories, cloud-save bridge
│   │   ├── database.dart
│   │   ├── repositories/
│   │   └── cloud_save/
│   └── shared/            # theme, widgets, constants
├── assets/
│   ├── images/
│   ├── audio/
│   └── data/              # bundled question catalog (JSON, loaded into Drift)
├── test/
│   └── domain/            # bulk of tests live here — pure logic
└── tools/
    └── question_generation/  # offline scripts to expand the question bank
```

---

## Phase Roadmap

Each phase ends with something demonstrable. We do **not** start a phase until the previous one is "done enough" to ship internally.

### Phase 0 — Foundation (complete)
- [x] Flutter SDK installed (3.41.7); `flutter create` scaffold for iOS+Android with org `com.quarup`
- [x] Locked dependencies installed: `flame`, `flutter_riverpod`, `riverpod_annotation`, `drift`, `sqlite3_flutter_libs`, `path_provider`, `path`, `flame_audio`, `games_services`; dev deps `build_runner`, `drift_dev`, `very_good_analysis`. Skipped `riverpod_generator` + `riverpod_lint` + `custom_lint` due to a Riverpod-3 / analyzer incompatibility — revisit when ecosystem catches up
- [x] Linting (`very_good_analysis`); `dart format` clean
- [x] GitHub Actions CI: format check + analyze + test on push/PR (`.github/workflows/ci.yml`)
- [x] Initial concept scope for Phase 1 decided — see *Domain Specs* above
- [x] Proficiency-update math sketched — see *Domain Specs* above
- [x] **Exit criteria (Path B — Android only):** `flutter run` launched the placeholder Math City app on the Android emulator (Pixel 7, API 34) successfully. iOS verification deferred to Phase 11
- [ ] iOS exit criteria — deferred until Xcode is installed (no later than Phase 11)

### Phase 1 — Vertical Slice (target: ~2–3 weeks) [the most important phase]
**Goal: prove the core loop is fun.** Hardcoded single player, two concepts, no persistence beyond runtime. Concepts and proficiency math are specified in *Domain Specs* above.
- [x] Concept registry with the two Phase 1 concepts (`add_1digit`, `sub_1digit`)
- [x] Algorithmic question generator with operand ranges per spec; distractor strategy per spec
- [x] `SpinWheel` Flame component (4 segments, tap-to-spin animation, lands on a concept)
- [x] `QuestionScreen` with 4-option multiple choice
- [x] `ResultScreen` with star award + wrong-answer explanation
- [x] Loop: home → spin → question → result → home, with star counter persisted in memory
- [ ] Basic SFX (spin, correct, wrong) and one looping background track — deferred to Phase 10 (CC0 assets not sourced; stub in place)
- [x] **Exit criteria:** Test with a real kid in the target age range — passed.

### Phase 2 — Adaptive Concept System (target: ~2–3 weeks)

**Design decisions (locked):**
- **Player:** A single default player is seeded into Drift on first launch. Full player creation / profile picker is Phase 3.
- **Numeric input UX:** Comfortable-band questions use an on-screen number pad (calculator-style) with an explicit submit button. Device keyboard not used. Text-answer question types deferred.
- **Concept expansion:** Only algorithmically generatable concepts added this phase (see *Domain Specs — Phase 2 concepts* below). Fractions, geometry, and word problems deferred to Phase 5/6 (Curriculum & Question Bank).

- [x] Drift schema for `Player` and `ConceptProficiency`; seed default player on first launch
- [x] Proficiency update logic (correct → up, wrong → down with floor); unit tests
- [x] Band classifier (mastered / comfortable / challenging / not yet) with grade-aware thresholds
- [x] Wheel selection: weighted sample of comfortable + challenging bands only
- [x] Number-pad input mode for comfortable-band concepts (on-screen pad + submit button)
- [x] Add 4 new concepts: `mul_1digit`, `div_1digit`, `add_2digit`, `sub_2digit` (all algorithmic)
- [ ] **Exit criteria:** A returning player sees the wheel adapt — easy concepts disappear, harder ones appear, number-pad input shows up for concepts they've practiced

### Phase 3 — Player Profiles & Avatar (complete)
- [x] Player creation flow (name, grade, basic avatar)
- [x] Profile picker on app launch
- [x] Mid-session player switching at start of each round (SpinScreen AppBar)
- [x] CustomPainter chibi avatar (slots: skin tone, hair, eyes, shirt, pants — drawn in Flutter, no external assets)
- [x] **Exit criteria:** Two kids can share the device with separate stats and avatars

**Open question resolved:** Avatar art sourced as pure Flutter CustomPainter (no external sprites). Simple geometric "chibi" style — works at all sizes, zero asset licensing burden.

### Phase 4 — DiceBear Avatar + Persistent Stars (target: ~3–5 days)

**Goal:** Replace the Phase 3 CustomPainter chibi with a DiceBear Adventurer avatar that the player can edit anytime. Stars persist across app restarts so they're meaningful as a Phase 7 city-builder currency.

**Spike outcome (already done):** DiceBear chosen — Adventurer style. Multiavatar disqualified (identicon, not dress-up). avatar_maker rejected (no real advantage over DiceBear, same bust-only limitation). Avatar accessory shop dropped from scope — see *Locked Decisions* for rationale.

**Curated slot subset (v1 — tunable in this phase):**
- Hair style: ~8 options (mix of short + long picks from adventurer's 45)
- Hair color: ~6 options
- Skin color: 4 options (DiceBear default palette)
- Eyes: ~5 variants
- Mouth: ~5 variants
- Glasses: 5 variants + none
- Earrings: 4 variants + none
- Optional features: blush, freckles (toggles)

(Eyebrows, mustache, birthmark deliberately skipped — too granular for kids; keep the picker tight.)

**Tasks:**
- [x] Add dep: `flutter_svg` (SVG renderer for local DiceBear SVG composition). `dice_bear` pub package rejected — calls `api.dicebear.com` at runtime, requires INTERNET permission; replaced by bundled SVG path constants
- [x] `domain/avatar/AdventurerConfig` — pure Dart record of the curated slot picks; serializes to JSON for storage
- [x] `domain/avatar/adventurer_catalog.dart` — the curated lists of slot values (the "~8 hair styles, ~5 eye variants" sets)
- [x] `presentation/player/adventurer_avatar_widget.dart` — widget that renders an `AdventurerConfig` via `SvgPicture.string` (offline, no network)
- [x] Drift schema migration (v3): wipe and recreate; split `totalStars` → `currentStars` + `lifetimeStarsEarned`
- [x] Avatar editor screen: per-slot pickers (ChoiceChips + color swatches), live preview at top; reachable from profile picker (edit) and new-player creation
- [x] Star-award flow writes `currentStars` and `lifetimeStarsEarned` to Drift on each correct answer
- [x] Render the new avatar on home, spin, and creation screens; deleted old `avatar_widget.dart` + `avatar_config.dart`
- [x] Tests: `AdventurerConfig` serialization round-trip (4 cases pass); all 51 tests pass, `flutter analyze` clean
- [x] **Exit criteria:** Player creates a profile, picks an adventurer look, plays a round, sees the avatar on every screen, earns stars, kills and reopens the app — same avatar, same stars. From the profile picker they can re-open the editor and change their hair, and the new look shows up everywhere.

---

### Phase 5 — Curriculum & Generator Framework (target: one Claude Code conversation)

**Goal:** Wire the [curriculum.md](curriculum.md) catalog into the app and build the foundational architecture for question generators and diagram widgets. Throw out Phase 1–2 generators wholesale and rewrite as curriculum-aligned decompositions. Cover ~20 sub-concepts in the K–3 range broadly. Wire DAG-based drip-feed introduction with an unlock celebration.

**Research outcome (already done — see Status block):** [curriculum.md](curriculum.md) defines the 12-category, ~361-sub-concept K–8 taxonomy with per-sub-concept question-source strategy, diagram requirements, and prereq DAG.

**Locked design decisions for this phase:**
- **Generator migration: full rewrite, not rename.** The existing 6 Phase 1–2 generators (`add_1digit`, `sub_1digit`, `mul_1digit`, `div_1digit`, `add_2digit`, `sub_2digit`) are deleted from code and tests. Replaced with curriculum-aligned decompositions (e.g. `add_1digit` → `add_within_5` + `add_within_10` + `add_within_20`).
- **Catalog vs implemented: clean schema.** All ~361 concepts are seeded as plain rows; no `is_implemented` column. Wheel eligibility checks an in-memory generator registry. Known limitation: a player can in theory hit `mastered` on a leaf whose DAG children aren't yet implemented; that branch's growth pauses until Phase 6 fills the gap. Acceptable trade-off.
- **DAG drip-feed semantics.** New player starts with 2 implemented concepts (lowest grade, lowest within-category row order). One new concept is introduced per mastery event. A concept is eligible for introduction only when all its DAG prereqs are `mastered` *and* its generator is registered. **No cross-domain gating** — independently per branch. **Pick policy:**
  1. Among eligible concepts, pick the lowest `Grade` first.
  2. Tiebreak by category: prefer the category in which the player currently has the *fewest active concepts* (introduced-but-not-mastered) — keeps the wheel balanced across math domains.
  3. Within the chosen category, pick the lowest row position in the [curriculum.md](curriculum.md) §3.x table for that category. Per `curriculum.md` design principle 8, that row order is the curated within-grade difficulty signal. **No global cross-category difficulty order exists** — re-sorting one category never touches another.
- **Unlock UI moment.** A "New concept unlocked!" celebration card is shown after the result screen, but only on correct answers that triggered either a mastery event or a queue advance. Never on wrong answers — this avoids the confusing scenario where a kid gets a question wrong but sees a celebratory unlock.
- **Word-problem framework: deferred to Phase 6.** This phase already covers schema migration + framework architecture + ~12 migrated + ~10 new generators + 3 diagram widgets + DAG engine + unlock UI. The word-problem framework (name/item/verb pools + 1-step template engine) and its first dataset-tagged sub-concept moved to Phase 6.
- **Drift v4: wipe-and-recreate.** No real users yet, so the existing v3 migration pattern is fine. A proper additive-migration implementation is now a prerequisite task in Phase 11 (Cloud Save) — once player data lives off-device, wipes destroy real data.
- **Wheel segment count.** Fixed cap at 8. The wheel renders `min(introducedConceptCount, 8)` segments and never more.

**Tasks:**
- [x] Drift schema v4: `Concepts` table (catalog with `categoryId`, `primaryGrade`, `prereqIdsCsv`, `sourceStrategy`, `diagramRequirement`, `categoryRowOrder`) + `IntroducedConcepts` table. Wipe-and-recreate migration
- [x] ~~Generate `assets/data/concepts.json`~~ — **shipped as a Dart const** in `lib/domain/concepts/concept_registry.dart` (22 implemented + ancestor concepts). Drift seeds from this on first run via `_seedConceptCatalog`. Authoring the JSON pipeline + parser is queued as a Phase 6 cleanup item so the full ~361-row catalog can be ingested
- [x] `GeneratedQuestion` + `DiagramSpec` architecture in `lib/domain/questions/` (pure Dart, no Flutter imports). Sealed `DiagramSpec` family: `FractionBarSpec`, `NumberLineSpec`, `ClockSpec`
- [x] `GeneratorRegistry` — in-memory `Map<String, QuestionGenerator>` keyed by `conceptId`. Wheel-eligibility filter and DAG drip-feed both consult it
- [x] **Replaced** the existing 6 Phase 1–2 generators wholesale (old files + tests deleted):
  - `add_within_5`, `add_within_10`, `add_within_20`
  - `sub_within_5`, `sub_within_10`, `sub_within_20`
  - `mult_facts_within_100`
  - `div_facts_within_100`
  - `add_within_100`, `add_2digit_carry` (forced-carry constraint)
  - `sub_within_100`, `sub_2digit_borrow` (forced-borrow constraint)
  - Each uses the `integerDistractors` library + procedural step-by-step explanation template
- [x] DAG drip-feed engine in `lib/domain/concepts/dag_engine.dart`. On a mastery event picks the next concept whose DAG prereqs are all `mastered` and whose generator is registered, and adds it to introduced. Returns an `UnlockEvent` so the UI can react. Pick policy: lowest grade → category with fewest active concepts → row order
- [x] Starter-pack initialization: 2 easiest implemented concepts at-or-below the player's grade. Lazily populated on first read of `introducedConceptsProvider`
- [x] "New concept unlocked!" UI moment: amber card on the result screen after a correct answer that produced an `UnlockEvent`. Suppressed on wrong answers (the caller passes `unlockEvent: null` when `!isCorrect`)
- [x] First 3 high-ROI new generator families per [curriculum.md §5.1](curriculum.md):
  - **Multi-digit ± with regrouping** — `add_within_1000`, `sub_within_1000`, `add_multidigit_standard_alg`, `sub_multidigit_standard_alg`
  - **Fraction generators** — `fraction_a_over_b`, `equivalent_fractions_visual`, `compare_fractions_same_denom`, `add_fractions_like_denom` (uses `FractionBar`)
  - **Time-telling** — `time_to_hour_half`, `time_to_5_min` (uses `Clock`)
- [x] First 3 diagram widgets in `lib/presentation/diagrams/`: `FractionBar`, `NumberLine`, `Clock`, plus a `DiagramRenderer` dispatcher that switches on the sealed `DiagramSpec`. Each is parameterised; question screen places the diagram above the prompt card when `q.diagram != null`
- [x] Updated `wheelConceptsProvider`: filters to `introduced ∩ generator-registered ∩ in-band`, takes the easiest `min(n, 8)`. Sorted by difficulty so wheel layout is stable. Per-category color palette in `spin_screen.dart`
- [x] Tests (91 total, all passing):
  - Catalog invariants (unique IDs, prereq references resolve, prereq grade ≤ dependent grade, every concept's category resolves)
  - `integerDistractors` + `integerDistractorsWith` + `stringDistractorsFromPool`
  - Per-generator parameter range + answer correctness + distractor uniqueness (≥200 iterations each)
  - `add_2digit_carry` always forces ones-sum ≥ 10; `sub_2digit_borrow` always forces minuend ones < subtrahend ones
  - `DripFeedEngine` starter pack + pickNext semantics (synthetic catalog + real catalog)
  - `ProficiencyNotifier.recordAnswer` × `UnlockEvent` matrix: returns event only when correct answer crosses 0.85; null on wrong answer (even at 0.84); null on correct that doesn't cross; null on already-mastered
  - Starter pack persistence: fresh player gets `add_within_5` + `sub_within_5` introduced on first read, stored in DB
- [x] **Exit criteria:** code complete and play-tested on the Android emulator (2026-05-10). Wheel + drip-feed + unlock card all behaved as expected in-hand. Test suite verified the unlock-card flow (suppressed on wrong answers, fired only on correct mastery transitions); the 3 diagram widgets compile and analyze clean; old `add_1digit`-style generators are gone from code and tests.

---

### Phase 6 — Full Question Bank (target: ~4–6 weeks)

**Goal:** Round out coverage to the full K–8 catalog. Build the word-problem framework (deferred from Phase 5). Build remaining diagram widgets and generators. Ingest bundled datasets for word problems and conceptual judgment items.

**Approach (locked):** Generators-first within Phase 6 — extend algorithmic coverage ahead of the dataset ingestion pipeline so each landed generator immediately produces playable content. The dataset sub-track is plumbing that delivers nothing until it's all done; we come back to it after the algorithmic surface is broad. Hand-curation of ~1500 gap-fill items is **punted out of v1** — rely on algorithmic + GREEN datasets for coverage.

**Tasks (in execution order):**

*Catalog foundation*
- [x] **Pre-Phase-6 cleanup (carried over from Phase 5):** `tools/curriculum/build_catalog.dart` parses [curriculum.md](curriculum.md) §3.x tables and regenerates `lib/domain/concepts/concept_registry.dart` (Dart const). 362 concepts × 12 categories now seeded. Two override maps live in the parser: `_shortLabelOverrides` for kid-facing wheel labels, `_prereqOverrides` for transitional DAG simplifications that drop curriculum.md prereqs whose generators don't exist yet (each entry keyed for one-line removal as new generators land). Parser includes a build-time grade-DAG validator that mirrors the catalog-invariant test. Surfaced two curriculum.md inconsistencies on first run (`simplify_fraction` and `add_fractions_unlike_denom` both listed grade-6 GCF/LCM as prereqs); fixed by dropping the formal prereq, since informal simplification/common-denominator works pre-grade-6.

*Tooling*
- [x] **Debug screen for testing generators in isolation** ([lib/presentation/debug/concept_debug_screen.dart](lib/presentation/debug/concept_debug_screen.dart)). kDebugMode-only chip on the home screen → tree of implemented concepts grouped by category → tap to play one question against that generator. Top-bar segmented toggle picks Multiple-choice vs. Keypad answer mode (necessary because some bugs only manifest in one mode). Bypasses the wheel, the DAG drip-feed, the proficiency write, the star award, and the unlock card so a debug session leaves the player profile untouched. `ResultScreen` "Try another" button pops back to the picker. Both UI entry points and the screen itself are gated by Flutter's `kDebugMode` constant — tree-shaken out of release builds.

*Generators-first sub-track (priority list #5 onward, with diagram widgets interleaved as each generator family needs them)*
- [x] **Word-problem framework v1** in [lib/domain/questions/word_problems/](lib/domain/questions/word_problems/): 25 culturally-balanced names, 20 plural items (4 city-builder-themed: bricks, paint cans, traffic cones, road signs), `composeWordProblem` template helper. The `WordProblemContext` carries an `op` (add | sub) and an optional `requiresEdibleItems` flag so the `eats` context never picks bricks. Subjects referred to by repeated name (no pronouns) to sidestep gender encoding.
- [x] **`add_word_problems_within_100`** — covers both addition and subtraction (the curriculum row is `+/−`). 6 contexts total: 3 add (`collects`, `is_given`, `buys`) + 3 sub (`gives_away`, `eats`, `loses`). Quantities ≥ 2; sum ≤ 100 for add, result ≥ 2 for sub. Misconception distractor: opposite operation.
- [x] **Place-value (3 generators)**: `place_value_2digit`, `place_value_3digit`, `place_value_multidigit` (4–7 digit, comma-formatted). Single-digit answer pool; misconception bias = "wrong digit from the same number".
- [x] **Rounding (3 generators)**: `round_to_10`, `round_to_100`, `round_multidigit_any_place` (place ∈ {10, 100, 1k, 10k, 100k}). Misconception bias = "rounded the wrong direction".
- [x] **Signed-integer arithmetic (3 generators)**: `integers_add`, `integers_subtract`, `integers_multiply_divide`. Kid-textbook display (parens around trailing-operand negatives). Implemented-grade ceiling moved G5 → G7.
- [ ] Extend the word-problem framework: multiplication contexts (`builds`, `bakes/makes`, `saves`) → needs concept-ID design call (curriculum.md doesn't have `mult_word_problems_within_100` per design principle 4); then 2-step (`add_sub_2step_word_problems`) once the shape is settled.
- [ ] Remaining algorithmic generators per [curriculum.md §5.1](curriculum.md) priority list (#6 onward): signed-number arithmetic, coordinate-plane, order-of-operations / expression evaluation, percent / unit-rate / proportion, area / perimeter, one-/two-step equations, angles, Pythagorean theorem, probability, place-value / rounding / scientific notation, summary statistics
- [ ] Diagram widgets, built as their generators require them: `BarChart`, `RectangleArea`, `CoordinatePlane` (Q1 + Q4), `Angle`, `IntersectingLines`, `Spinner`, `Dice`, `Polygon`, `Shape`, `TapeDiagram`, `DoubleNumberLine`, `Circle`, `Protractor`, `Ruler`, `Money`, `BoxPlot`, `ScatterPlot`, `TwoWayTable`, `TreeDiagram`, `BaseTenBlocks`, `Box3D`, `Histogram`, `DotPlot`, `LinePlot`. Defer: `Net3D`, `ColumnArithmetic` (text-only explanations OK in v1)

*Dataset ingestion sub-track (revisit after the generator surface is broad)*
- [ ] Dataset ingestion pipeline (build-time only) in `tools/question_generation/`. For each GREEN dataset: fetch, transform to bundled JSON format, sub-concept-tag, write to `assets/data/`:
  - DeepMind `mathematics_dataset` — generate per-module batches (arithmetic, algebra, comparison, measurement, numbers, polynomials, probability), filter to K–8 difficulty, target ~50K items
  - GSM8K — filter to grades 3–6, sub-concept-tag via operator/keyword analysis
  - MathDataset-ElementarySchool — re-tag and dedupe; preserve `source` provenance
  - MathQA — filter `category` to grade-appropriate; keep `linear_formula` for distractor generation + answer verification
  - SVAMP — provenance-audit (drop ASDiv-derived items per agent-2 license analysis)
- [ ] Create `LICENSES_THIRD_PARTY.md`: attribution to every ingested dataset, plus the (eventual) art/audio assets

*Wrap-up*
- [ ] Adaptive system tuning: refine band thresholds per-grade-band, refine `α` learning rate based on playtesting, add asymmetric reward/penalty if data supports it
- [ ] Validate the full catalog with kids in grades K, 2, 4, 6, 8 — at least one per band — and tune
- [ ] **Exit criteria:** ~85% K–8 CCSS coverage live in the app; a kid in any grade K–8 can play continuously and see appropriate-difficulty content; no "no eligible concepts" dead ends; every wrong-answer screen shows a kid-readable step-by-step explanation.

*Punted out of v1:* Hand-curated ~1500 gap-fill questions for the §7.6 dataset gaps (K–2 word problems, K–5 geometry referencing procedural diagrams, statistical-question recognition, qualitative graph descriptions, grade-7 ratio real-world scenarios, grade-8 function items). Revisit post-launch if the algorithmic + dataset coverage leaves visible gaps in real play.

---

### Phase 7 — City Builder: Foundations (target: ~3–4 weeks)

**Goal:** Each player has their own persistent isometric city. They spend stars to place buildings; population grows when the right mix is built.

- [ ] Source/curate isometric building art (start: Kenney City Kit Industrial; supplement if needed). Confirm CC0 status, log in eventual `LICENSES_THIRD_PARTY.md`
- [ ] Drift schema (v4): `City`, `CityMap`, `BuildingType`, `BuildingPlacement` tables
- [ ] One beginner `CityMap` definition: ~10×10 tile grid, fixed terrain (grass + decorative water/stone)
- [ ] Initial `BuildingType` catalog (5 types): own house, apartment, school, hospital, power plant
- [ ] Isometric tile renderer (Flame component) — render terrain + placed buildings, support pinch-zoom and pan
- [ ] Build-mode UI: building catalog at the bottom, tap building → tap free tile → placed; insufficient stars greys out the option
- [ ] Move-mode UI: pick up an existing building, tap a new free tile to drop it (no star cost)
- [ ] Auto-road generation: roads automatically connect every placed building (recompute on placement / move). Render under buildings on the road tiles
- [ ] Population counter clearly visible on the city screen
- [ ] Population growth model (pure-Dart, well unit-tested): population grows toward `sum(populationContribution)` capped by service ratios (e.g. 1 school per 50 residents, 1 hospital per 100, 1 power plant per 200). Below the lowest-satisfied ratio, growth stalls
- [ ] One feedback message when stalled (cycle through if multiple constraints fail): "Your residents need a school to keep growing"
- [ ] "My City" screen accessible from the home screen
- [ ] **Exit criteria:** Player places 5 buildings, sees population grow, hits a service-ratio cap, reads the feedback message, builds the missing service, sees population resume growth — all surviving an app restart

---

### Phase 8 — City Builder: Depth (target: ~2–3 weeks)

**Goal:** City has long-arc progression — bigger land, more building types, themed maps, events.

- [ ] Land expansion: spend stars to grow the grid symmetrically outward by 2 tiles per side. Cap at, say, 20×20 for v1
- [ ] Progressive `BuildingType` unlocks gated by `lifetimeStarsEarned` (e.g. coffee shop at 200, gas station at 400, hotel at 800)
- [ ] Expand catalog to ~12–15 building types (add: coffee shop, restaurant, gas station, hotel, waste management, office, fire station — final list set during the phase)
- [ ] Building upgrade tiers: each `BuildingType` has up to 3 tiers; upgrading costs stars; **footprint stays the same**, only the texture/style changes
- [ ] Additional `CityMap`s with themes (countryside, big-city, futuristic) — each has its own `starCostToUnlock` and its own independent placement state per player
- [ ] Map switcher UI on the city screen
- [ ] Star-funded events: "Festival" (+population for X rounds), "Marketing campaign" (boosts attractiveness), 2–3 event types
- [ ] Aggregate-needs system extended: parameterize service ratios from `serviceProvision` data, so adding a new building type is data-only
- [ ] **Exit criteria:** A returning player has unlocked at least one new building type via lifetime stars, expanded their land once, switched between two map themes, and run an event

---

### Phase 9 — Player Progress Screen (target: ~1 week)
- [ ] Concept proficiency visualization (radar chart or color grid) — rolled up to category
- [ ] Strengths and growing edges sections with positive framing
- [ ] Lifetime stats: stars earned, sessions, questions answered
- [ ] **Exit criteria:** Player can see and feel proud of their own progress

---

### Phase 10 — Sound, Polish, Engagement (target: ~2–3 weeks)
- [ ] Final SFX library (CC0/royalty-free); per-event audio (spin, correct, wrong, building-placed, level-up)
- [ ] Background music (looping, with mute toggle)
- [ ] Animation polish (character reactions on correct/wrong; screen transitions)
- [ ] Daily streak tracking + bonus stars
- [ ] Daily challenge mechanic
- [ ] First-launch tutorial (skippable)
- [ ] Settings screen (audio mute, reset profile, change grade level, dyslexia-friendly font toggle if time)
- [ ] **Extra credit — animated city** (drop if time runs short, defer to post-launch):
  - [ ] Cars driving along the auto-roads
  - [ ] Pedestrians on sidewalks
  - [ ] Building lights turning on/off
  - [ ] Day/night cycle on the city screen
- [ ] **Exit criteria:** It feels like a real game, not a prototype

---

### Phase 11 — Cloud Save (target: ~1–2 weeks)
- [ ] **Prerequisite:** Replace the wipe-and-recreate Drift migration pattern (used through v3 and v4) with proper additive migrations. Once player data lives off-device, schema wipes destroy real data — this must land *before* the cloud-save round-trip is enabled
- [ ] Integrate `games_services` save game API
- [ ] Sign-in flow (Game Center / Play Games) — graceful skip if signed out
- [ ] Save-on-meaningful-event (round end, avatar edit, building placed, map unlocked)
- [ ] Load on app start; conflict resolution (prefer most recent)
- [ ] iOS verification (deferred from Phase 0 — `flutter run` on iOS simulator must succeed before this phase ships)
- [ ] **Exit criteria:** Install on a second device, sign in, see same player data including avatar, stars, and city state

---

### Phase 12 — Beta + Store Submission (ongoing)
- [ ] **Apple Developer Program** enrollment ($99/yr) — only when ready to submit; not blocking earlier work
- [ ] **Google Play Console** enrollment ($25 one-time)
- [ ] App Store Connect: register bundle ID, fill listing metadata, age rating questionnaire, kids-category settings ("Made for Kids")
- [ ] Play Console: register the app, fill listing metadata, "Designed for Families" enrollment, target-audience declaration
- [ ] Build artifacts: signed Android AAB; signed iOS archive (requires macOS + Xcode)
- [ ] App icon, screenshots (Android + iOS at multiple sizes), feature graphic, store description copy
- [ ] Privacy policy (minimal — we collect ~nothing — but COPPA-aware language; either template-based or briefly legal-reviewed)
- [ ] TestFlight (iOS) internal testing
- [ ] Play Console internal-testing channel
- [ ] Iterate on real beta feedback (1–2 cycles)
- [ ] Submit to stores; respond to reviewer feedback
- [ ] **Exit criteria:** App is live on both stores

---

## Open Questions / Decisions Deferred

These are not blockers for Phase 0 or 1 but need to be resolved by the phase noted:

- **By Phase 2 (resolved):** Proficiency update formula — using simple exponential moving average; see *Domain Specs*.
- **By Phase 4 (resolved):** Avatar library pick — DiceBear Adventurer chosen; avatar accessory shop dropped. See *Locked Decisions*.
- **By Phase 5 (resolved):** K–8 curriculum scope, taxonomy, and source strategy — see [curriculum.md](curriculum.md). 12 categories, ~361 sub-concepts; ~85% reachable algorithmically, ~10% via bundled GREEN-licensed datasets, ~5% deferred. No runtime LLM; no offline LLM batch in v1.
- **By Phase 5 (resolved):** Question dataset sourcing strategy — curate from GREEN-licensed datasets only (DeepMind `mathematics_dataset`, GSM8K, MathDataset-ElementarySchool, MathQA, SVAMP). CC-BY-NC content excluded. See [curriculum.md §7](curriculum.md).
- **By Phase 5:** Open taxonomy questions — see [curriculum.md §9](curriculum.md) (12 items needing human review during implementation: counting/place-value boundary, fluency tier modeling, geometry hierarchy duplication, word-problem axis, etc.).
- **By Phase 6 (resolved):** Hand-curation budget for the ~1500 gap-fill items — **punted out of v1**. Rely on algorithmic + GREEN datasets for coverage; revisit post-launch if real play exposes the §7.6 gaps. See Phase 6 task list for the punted scope.
- **By Phase 7:** Specific isometric-asset packs — Kenney's [City Kit Industrial](https://kenney.nl/assets/city-kit-industrial) is a strong starting point but coverage is limited. Identify supplementary packs (Kenney's other city packs, [OpenGameArt isometric tag](https://opengameart.org/art-search-advanced?keys=isometric)) before catalog work begins.
- **By Phase 8:** Specific list of building types and their service-ratio numbers (residents-per-school, etc.) — can only be tuned by play-testing.
- **By Phase 10:** Music + SFX sourcing (CC0 from Freesound.org and OpenGameArt.org).
- **By Phase 11:** Are we OK requiring the user to sign in to Game Center / Play Games for cloud save? Otherwise local-only is the only option.
- **By Phase 12:** Privacy policy text — minimal since we collect ~nothing, but COPPA considerations for under-13 audience need legal review or a template.

---

## Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Core loop isn't fun for kids | Medium | Critical | Phase 1 explicitly tested this with a real kid before any further investment — passed |
| Question dataset gap | Medium | High | Algorithmic generation handles arithmetic; for everything else, multiple datasets identified (GSM8K, MathDataset-ElementarySchool, Illustrative Mathematics) |
| ~~Avatar library doesn't cover all 8 slots~~ | — | — | Resolved: avatar accessories dropped from scope after Phase 4 spike — single star sink is the city builder |
| City UX on small screens (placement, panning, zoom on a phone) | Medium | High | Auto-roads (no precision needed); tile-snap with generous tap targets; pinch-zoom + pan; play-test on smallest target device early in Phase 7 |
| Isometric asset coverage gap | Medium | Medium | Kenney's City Kit packs are the starting point but limited; identify 1–2 supplementary CC0 packs in early Phase 7. If coverage is still sparse, narrow the v1 building catalog rather than ship inconsistent art |
| Population growth model feels arbitrary or unmotivating | Medium | Medium | Keep model simple (aggregate ratios, not per-building dependency graphs); tune via play-testing. Vague-but-themed feedback messages keep the player oriented even if numbers shift |
| City save state migration as schema evolves across phases | Medium | Medium | Drift migrations are version-checked; player's city is purely cosmetic so a wipe is a survivable last resort. Bias toward additive schema changes |
| Cloud save platform divergence | Low (single package) | Medium | `games_services` abstracts both — but test on both platforms early in Phase 11 |
| `games_services` package abandonment | Low | Medium | If it goes stale, fall back to platform-specific packages (`cloud_kit` for iOS, `googleapis` Drive for Android) |
| Hive/Isar abandonment pattern repeats with Drift | Low | Low | Drift is built on SQLite; worst-case migration to raw `sqflite` is straightforward |
| COPPA / children's-app compliance | Medium | High (could block store submission) | No data collection; address this explicitly in Phase 12 with a minimal privacy policy and store-listing kids-category settings |
| Apple Developer Program ($99/yr) and Play Console ($25 one-time) fees | Certain | Low–Medium | Real ongoing cost for a "free hobby project." If the Apple membership lapses, the iOS build is delisted from the App Store. Budget accordingly; consider whether one platform-only launch buys more time |
| Sub-concept catalog explosion | Medium | Low (mitigated) | Mitigated as of Phase 5: the full ~361-sub-concept K–8 catalog is now defined in [curriculum.md](curriculum.md) with prereq DAG. The progressive rollout is: Phase 1: 2 concepts; Phase 2: 6 concepts; Phase 5: ~20 concepts (K–3 broad — 12 from Phase 1–2 decomposition + ~8 new across multi-digit ±, fractions, time); Phase 6: full K–8 catalog (~85% coverage live). Schema seeds the full catalog from Phase 5 onward — no further schema migrations needed for catalog growth |

---

## How We Work (AI-Assisted Conventions)

Since this is a two-person project (you + Claude), some norms to keep us efficient:

- **PRD is product scope, plan.md is execution.** Don't conflate. If product scope changes, update [prd.md](prd.md). If execution approach changes, update this file.
- **Update `Status` section** at the top of this file at the end of each work session — current phase, last action, next action. Keeps the next session cold-startable.
- **Check off phase task boxes as we go** so we always know where we are.
- **Open questions go in the "Open Questions" section above** — don't let them get buried in chat. When answered, move the answer into the relevant phase or "Locked Decisions."
- **Bugs and concrete enhancements go to [GitHub Issues](https://github.com/quarup/math_city/issues)**, not plan.md. plan.md is for strategy + phase scope; Issues are for tactical backlog. Label set: type (`bug`, `enhancement`, `content`, `polish`), area (`wheel`, `question-screen`, `result-screen`, `generator`, `diagram`, `debug-tools`, `accessibility`, `city-builder`), priority (`p1`/`p2`/`p3`). When starting a sub-slice, pull relevant issues by label.
- **After landing a generator, widget, or dataset, run `python3 tools/curriculum/sync_implementation_status.py`** to refresh the ✅ markers and rollup counts in [curriculum.md](curriculum.md). Commit the curriculum.md change alongside the code change. See [CLAUDE.md](CLAUDE.md) "Keeping curriculum.md status in sync" for details.
- **Risks: revisit at start of each phase** — drop ones that no longer apply, add new ones surfaced by the work.
- **Code style:** see [CLAUDE.md](CLAUDE.md) for conventions, build/test commands, and architecture notes the AI should follow.

---

## References

**Engine / framework**
- [Flame Engine docs](https://docs.flame-engine.org/)
- [Flutter Casual Games Toolkit](https://docs.flutter.dev/resources/games-toolkit)
- [Riverpod docs](https://riverpod.dev/)
- [Drift docs](https://drift.simonbinder.eu/)
- [games_services package](https://pub.dev/packages/games_services)
- [flame_audio package](https://pub.dev/packages/flame_audio)

**Avatar library (chosen)**
- [DiceBear Adventurer style](https://www.dicebear.com/styles/adventurer/) — CC0 designs, MIT code
- [`dice_bear` Flutter package](https://pub.dev/packages/dice_bear) — Dart wrapper for the DiceBear API; renders SVG locally or via URL
- [`flutter_svg` package](https://pub.dev/packages/flutter_svg) — required to render the SVG output

**City builder asset candidates (Phase 7)**
- [Kenney City Kit Industrial](https://kenney.nl/assets/city-kit-industrial) — CC0 isometric
- [Kenney all assets](https://kenney.nl/assets) — search "city" / "isometric"
- [OpenGameArt isometric tag](https://opengameart.org/art-search-advanced?keys=isometric) — CC0 / CC-BY mix

**Curriculum & math content** (full inventory + licensing in [curriculum.md](curriculum.md) §7)
- [curriculum.md](curriculum.md) — canonical K–8 taxonomy, generator priority, diagram widget catalog, dataset inventory
- [Common Core State Standards — Mathematics](https://www.thecorestandards.org/Math/) — primary curriculum source
- [DeepMind `mathematics_dataset`](https://github.com/google-deepmind/mathematics_dataset) — Apache 2.0; procedurally generated arithmetic/algebra
- [GSM8K](https://github.com/openai/grade-school-math) — MIT; grade-school word problems with rationales
- [MathDataset-ElementarySchool](https://github.com/RamonKaspar/MathDataset-ElementarySchool) — MIT; pre-aggregated K–5 catalog
- [MathQA](https://math-qa.github.io/) — Apache 2.0; cleaned algebra MC for grades 6–8
- [SVAMP](https://github.com/arkilpatel/SVAMP) — MIT; curated grades 2–4 word problems
- [Open Up Resources 6–8 Math (1st/2nd ed)](https://openupresources.org/) — CC-BY 4.0; the only OER curriculum that's app-store-distribution-safe (the newer California editions are CC-BY-NC = excluded)

**Audio / general assets**
- [OpenGameArt.org](https://opengameart.org/) — CC0/CC-BY art assets
- [Freesound.org](https://freesound.org/) — CC-licensed audio
